# 2026-08-25 Closet Index Development Log

## Work Summary - 유지보수 리팩터링 client 릴리스 준비

테스트 기준선 복구부터 라우트·번들 분리, DataContext 책임 분리, repository 소비자 계약 축소, 사용되지 않는 Outfit clone client 제거까지 이어진 유지보수 변경을 하나의 client 릴리스로 정리했다. production DB의 clone RPC는 공개 client가 더 이상 참조하지 않는다는 배포 검증이 끝날 때까지 유지한다.

## Work Completed - 유지보수 리팩터링 client 릴리스 준비

- 현재 변경 29개를 코드·테스트·CI·database 문서·개발 로그로 분류하고 모두 이번 연속 리팩터링 작업에서 생성된 범위임을 확인했다.
- 개인 메모, 비밀 파일, build output, dependency 폴더 등 제외 대상과 별도 확인이 필요한 파일은 없었다.
- `git fetch` 후 local `main`과 `origin/main`이 서로 앞서거나 뒤처지지 않은 동일한 기준임을 확인했다.
- 전체 테스트 652건 중 645건 통과, 7건 건너뜀, 실패 0건을 다시 확인했다.
- GitHub Pages 조건의 production build를 통과했다. 엔트리 청크는 100.59kB, Supabase vendor는 204.56kB, React vendor는 232.44kB이며 크기 경고가 없었다.
- SPA fallback을 만든 뒤 `verify:pages`를 통과했고 local production JavaScript에서 clone RPC·오류 문자열 0건, 일반 create RPC·source query 각 1건을 확인했다.
- `git diff --check`를 통과했다. CRLF 변환 안내 외 whitespace 오류는 없었다.

## Decisions - 유지보수 리팩터링 client 릴리스 준비

- 누적 변경은 서로 의존하는 한 유지보수 릴리스로 함께 배포한다. 라우트 lazy loading과 vendor chunk, DataContext 분리, repository 계약 축소, clone client 제거를 일부만 선택하면 이미 검증한 타입·테스트·bundle 경계와 달라지기 때문이다.
- GitHub Pages workflow 자체에서 전체 테스트를 실행하도록 해 배포 build 전에 회귀를 차단한다.
- Outfit clone의 사용자 기능은 source-prefill 뒤 일반 create로 유지한다. 제거 대상은 별도 server clone client 계약뿐이다.
- `clone_closet_outfit` live RPC, grant, lifecycle COMMENT와 과거 migration은 이번 client 릴리스에서 변경하지 않는다.
- production DB cleanup은 compatible client 배포 성공, 공개 JavaScript 참조 0건, 실제 화면 확인 뒤 별도 migration으로 진행한다.

## Current Status - 유지보수 리팩터링 client 릴리스 준비

- 로컬 코드·테스트·Pages artifact 검증은 완료됐다.
- 변경은 아직 commit·push·배포되지 않았다.
- production DB와 공개 GitHub Pages는 변경 전 상태다.

## Next Steps - 유지보수 리팩터링 client 릴리스 준비

- 의도한 29개 파일과 이 개발 로그만 stage해 commit·push한다.
- GitHub Pages workflow의 test, build, artifact, deploy 완료와 배포 commit을 확인한다.
- 공개 HTML·JavaScript에서 새 asset과 clone 참조 0건을 확인하고 가능한 범위에서 실제 화면을 smoke test한다.
- 결과를 이 날짜의 개발 로그에 추가한 뒤 별도 cleanup migration 진행 여부를 확정한다.
