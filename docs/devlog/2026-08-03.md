# 2026-08-03 Closet Index Development Log

## Work Summary

Lookbook과 Calendar에서 착장을 더 크게 확인할 수 있도록 전체 표시 배율을 조정하고, 아우터 옆에 분리되는 티셔츠의 과도한 크기를 바로잡았다.

## Work Completed

- Lookbook과 Calendar가 공유하는 착장 레이어를 110%로 확대했다.
- 아우터가 있을 때 우측에 분리되는 티셔츠의 새 기본 배율을 115%에서 90%로 변경했다.
- 착장 구성 버전을 v5로 올려 변경 전 미리보기와 새 구성 기준을 구분했다.
- 사용자가 직접 저장한 우측 티셔츠 배율이 새 기본값으로 덮어써지지 않는지 회귀 테스트를 추가했다.

## Decisions

- 110%, 112%, 115% 비교 결과 차이는 작지만 100%보다는 개선이 분명했고, 잘림 위험이 가장 낮은 110%를 전체 표시 배율로 선택했다.
- 기존 데이터는 우측 티셔츠이면서 정확히 115%인 값만 과거 기본값으로 간주해 90%로 보정한다. 95%와 100% 등 다른 값은 사용자가 직접 조절한 값일 수 있으므로 보존한다.
- 확대는 개별 아이템 좌표를 다시 계산하지 않고 공용 3:4 착장 레이어에 적용해 Lookbook과 Calendar에서 동일한 결과를 유지한다.

## Current Status

- 로컬 코드 반영을 완료했다.
- 전체 테스트 221개와 프로덕션 빌드가 통과했다. 빌드에는 기존과 같은 청크 크기 경고가 있으나 생성은 정상 완료됐다.
- 로컬 개발 서버의 Calendar URL이 HTTP 200으로 응답하는 것을 확인했다. 이 세션에서는 연결된 브라우저 제어 기능을 호출할 수 없어 자동 모바일 스크린샷 검증은 진행하지 못했다.
- 원격 데이터의 우측 티셔츠 중 정확히 115%였던 8개를 90%로 보정했다. 보정 뒤 115% 잔여는 0개이며, 직접 조절된 것으로 판단한 95% 1개와 100% 9개는 그대로 보존됐다.

## Next Steps

- J가 iPhone에서 Lookbook과 Calendar의 110% 표시 크기와 가장자리 잘림 여부를 최종 체감 확인한다.
- 필요하면 실제 기기 결과를 기준으로 110% 확대를 화면별로 미세 조정한다.

## Work Summary - Outfit Preview UI 숨김

Outfit Preview 기능을 현재 제품 범위에서 보류한 결정과 실제 착장 상세 UI가 어긋나 있던 부분을 정리했다. Lookbook에서 착장을 열었을 때 중간에 노출되던 `MANAGE`·`착장 관리`·`저장 Preview` 영역을 숨겼다.

## Work Completed - Outfit Preview UI 숨김

- 착장 상세 화면에서 저장 Preview 상태, 생성·재생성 버튼, 누끼 미완성 안내와 Preview 생성 실패 안내를 제거했다.
- Preview 생성에만 사용되던 화면 상태, handler, import를 제거했다.
- `이 착장으로 새로 만들기`는 Preview와 무관한 기존 복제 기능이므로 별도 버튼으로 유지했다.
- Preview UI 전용 CSS를 제거했다.
- 착장 상세 회귀 테스트에 Preview 문구·버튼이 렌더링되지 않고 복제 링크와 삭제·보관 기능은 유지되는 검증을 추가했다.

## Decisions - Outfit Preview UI 숨김

- 이번 변경은 UI 노출만 보류 결정에 맞추며, 저장된 Preview metadata, 데이터 모델, 생성 라이브러리와 Edge Function은 삭제하지 않는다.
- 향후 Preview 기능을 다시 검토할 수 있도록 서버 계약은 보존한다.
- Outfit 복제와 최하단 삭제·보관은 Preview 기능과 별개이므로 계속 노출한다.

## Current Status - Outfit Preview UI 숨김

관련 화면 테스트 4개, 전체 Vitest 49개 파일·221개 테스트, TypeScript 검사와 production build가 통과했다. 인앱 브라우저의 로그인된 로컬 앱에서 데스크톱과 390px 모바일 착장 상세을 확인했고 `MANAGE`·`저장 Preview`·Preview 생성 버튼이 모두 사라진 상태에서 복제 링크와 최하단 삭제·보관이 유지됐다. 복제 링크를 눌러 `새 Outfit` 화면으로 이동하는 상호작용도 확인했다. 두 viewport 모두 가로 overflow 0, 콘솔 warning·error 0이었다. 빌드에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.

## Next Steps - Outfit Preview UI 숨김

- 공개 앱 반영은 별도 요청에 따라 commit·push·배포한다.

## Work Summary - Phase 4 Item Statistics 착수

Phase 4의 첫 구현 묶음으로 기존 기본 개수·전체 순위 Statistics를 Item 활용률 중심 화면으로 전환했다. 계획 문서의 P4-1A 계산 계약을 먼저 순수 함수와 fixture로 고정하고, 같은 계산 결과를 기간·계절·카테고리 필터 UI에 연결했다.

## Work Completed - Phase 4 Item Statistics 착수

- Lifetime과 달력 연도 기간 모델, 현재 연도 오늘 기준 종료일, 과거 연도 취득일 미상 제외 규칙을 구현했다.
- Outer·Top·Bottom·Dress·Shoes·Bag 또는 `-made` 카테고리 대상 규칙과 복수 계절·복수 카테고리 OR 필터를 구현했다.
- 현재 Active Item 활용률, Most Worn, Lifetime Never Worn·선택 연도 기록 없음, 카테고리별 Active 보유 수를 계산하도록 변경했다.
- 독립 `Innerwear`는 제외하고 `Top-T-shirts-innerwear`는 Top으로 포함했으며, Retired Item은 활용률 분모에서 제외하되 선택 기간에 기록이 있으면 Most Worn에 유지했다.
- 고정 Outfit 구성과 Wear Log를 연결해 같은 Wear Log를 Item마다 한 번 집계하고, Item별 전체 기록의 1월부터 12월 월별 횟수와 연중 착용 여부를 계산하도록 준비했다.
- Statistics 필터를 전용 versioned localStorage key에 저장해 HOME·Closet 필터와 독립적으로 복원하도록 했다.
- 결과 목록은 Item 이미지와 함께 최대 4개를 먼저 보여 주고, 기존 범위 밖이던 Outfit 전체 순위는 Statistics에서 제거했다.
- 실제 렌더링에서 기존 3열 `metric-grid` 때문에 생긴 빈 요약 칸을 발견해 Phase 4 요약에만 2열 modifier를 적용했다.
- 특수 규칙 fixture와 화면 상호작용 테스트를 추가하고 변경된 More 메뉴 설명의 회귀 기대값을 갱신했다.

## Decisions - Phase 4 Item Statistics 착수

- 이번 구현은 기존 Supabase `AppData` 인증 스냅샷에서 계산하며 schema, RLS, 원격 데이터는 변경하지 않는다. 현재 규모에서는 중복 집계 테이블보다 순수 계산 계약을 먼저 검증하는 계획을 따른다.
- 구매일과 Retired 상태로 과거 옷장을 복원했다고 표현하지 않는다. 과거 연도 화면은 `현재 보유 옷의 YYYY년 활용률`로 표시하고, 취득일 미상 제외 수를 함께 노출한다.
- 카테고리와 계절의 복수 선택은 OR로 처리하고 Item ID 기준으로 한 번만 분모에 넣는다. `Bag-made`처럼 기본 그룹과 `-made`를 동시에 만족해도 전체 활용률에는 중복 집계하지 않는다.
- 월별 분포 값은 선택 연도나 최근 12개월이 아니라 전체 Wear Log의 실제 월을 12칸에 누적한다. Item 상세의 저높이 그래프 UI는 이 계산 결과를 사용하는 후속 묶음으로 분리한다.

## Current Status - Phase 4 Item Statistics 착수

- P4-1A 계산 계약과 Statistics 홈의 첫 UI 연결을 완료했다.
- Phase 4 집중 테스트 10개, 전체 Vitest 49개 파일·229개 테스트, TypeScript 검사와 production build가 통과했다.
- 로그인된 로컬 Supabase 데이터로 데스크톱과 390px 모바일 Statistics를 확인했다. Lifetime에서 86%·296/344개, 2025년에서 36%·114/314개와 취득일 미상 2개 제외 문구로 필터 결과가 갱신됐으며, 양쪽 viewport 모두 가로 overflow와 console warning·error가 없었다.
- build에는 기존과 같은 500kB 초과 JavaScript chunk 경고만 남아 있다.
- P4-0 production·Notion 수량 재확인, Item 상세 월별 그래프, Statistics 결과와 동일 조건의 Closet 전체 목록 연결, Replacement Line Overview는 아직 완료하지 않았다.
- commit·push·배포 및 원격 Supabase 변경은 수행하지 않았다.

## Next Steps - Phase 4 Item Statistics 착수

- P4-0 읽기 전용 audit로 production Item·Outfit·Wear Log와 Replacement Line 기준 수량을 재확인한다.
- Statistics 결과의 `Closet에서 전체 보기`가 동일한 기간·계절·카테고리·결과 집합을 유지하도록 목록 전달 계약을 추가한다.
- Item 상세에 접근 가능한 12개월 세로 막대 그래프와 총 기록 기간·연중 착용 표시를 연결한다.
- 이후 P4-2A Replacement Line 읽기 전용 Overview 모델과 lazy repository query를 구현한다.

## Work Summary - Phase 4 P4-0 기준 감사

Phase 4 구현 전에 production과 읽기 전용 Notion 보관본의 실제 수량, Replacement Line 품질, Legacy Link 의미를 재확인했다. 단순 총합 비교로는 감춰지는 컷오버 이후의 Outfit·Wear Log 출처 차이까지 분리해 재현 가능한 기준선으로 고정했다.

## Work Completed - Phase 4 P4-0 기준 감사

- workspace와 production project를 고정하고 `select`만 수행하는 `audit:phase4` 스크립트를 추가했다.
- production Item 451개, Outfit 508개, Wear Log 783개와 취득일 있음 442개·미상 9개를 확인했다.
- Outfit은 Notion 연동 505개·앱 생성 3개, Wear Log는 Notion 연동 782개·앱 생성 1개임을 분리했다.
- Notion 보관본의 Item 451개, Outfit 507개, Wear Log 783개를 새로 추출하고 production에 없는 Outfit 2개·Wear Log 1개의 알려진 차이를 기록했다.
- Replacement Line 53개, membership 165개, 고유 Item 163개, 빈 Line 1개, 단일 Item Line 10개, 복수 Line Item 2개와 고아 membership 0개를 대조했다.
- Wardrobe `Replaces` relation을 추출하도록 보완하고 98개 reciprocal entry가 49개 무방향 pair이며 비대칭·self-link·깨진 참조가 0개인지 검사했다.
- Notion API에서 Replacement Line의 Formula·Rollup과 7개 View를 읽어 Phase 4에서 유지·재설계·Phase 6 보류할 개념을 별도 감사 문서에 정리했다.
- reciprocal pair 계산과 기준 불일치 검출을 검증하는 Node 테스트 3개를 추가했다.

## Decisions - Phase 4 P4-0 기준 감사

- Supabase는 컷오버 이후 쓰기 원본이므로 Notion과 개별 행이 계속 같다고 가정하지 않는다. 총합과 함께 Notion 연동·앱 생성 출처 수, 원본에만 남은 행 수를 기준선으로 검사한다.
- production에 없는 Notion Outfit·Wear Log를 되살리거나 앱 생성 데이터를 삭제하지 않는다. 이번 단계는 차이를 설명하고 기록하는 읽기 전용 감사로 제한한다.
- `Active Count`는 현재 Item 상태에서 직접 계산하고 `Newest Active Age`는 최근 Active 취득일로 재설계한다. category View는 P4-2A 필터·그룹으로 옮긴다.
- category별 `Risk Threshold`와 이를 사용하는 `멸종 관리` Formula·View는 교체·재구매 주기 범위이므로 Phase 6로 보류한다.
- 49개 Legacy Link는 무방향으로만 보존하며 취득일이나 Retired 상태를 사용해 방향 있는 edge를 자동 생성하지 않는다.

## Current Status - Phase 4 P4-0 기준 감사

- P4-0 체크리스트와 완료 조건을 충족했고 Phase 4 계획 문서에 실제 기준선을 반영했다.
- 마지막 읽기 전용 감사에서 production·Notion·출처 차이·Replacement Line 기준의 mismatch가 0개였다.
- 감사 순수 함수 테스트 3개, 전체 Vitest 49개 파일·229개 테스트, TypeScript 검사와 production build가 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.
- production과 Notion 쓰기는 0건이며 commit·push·배포도 수행하지 않았다.

## Next Steps - Phase 4 P4-0 기준 감사

- P4-1B의 Statistics 결과 전체 목록 전달 계약과 Item 상세 12개월 그래프를 구현한다.
- 이후 P4-2A에서 Replacement Line lazy query, Style Identity 그룹, 상태·품질 경고를 읽기 전용으로 구현한다.

## Work Summary - Phase 4 P4-1B 완료

Statistics의 최대 4개 미리보기에서 실제 전체 Item 목록으로 이어지는 흐름과 Item 상세의 월별 착용 분포를 완성했다. 기간·계절·카테고리 조건을 잃지 않고 목록과 상세를 왕복하며, 모바일 한 화면 안에서 1월부터 12월까지 전체 분포를 확인할 수 있게 했다.

## Work Completed - Phase 4 P4-1B 완료

- Most Worn과 Never Worn·연도별 기록 없음의 `Closet에서 전체 보기`를 전용 Statistics Item 목록에 연결했다.
- 결과 종류, Lifetime·연도, 복수 계절, 복수 카테고리를 URL query로 직렬화하고 안전하게 다시 해석하는 계약과 테스트를 추가했다.
- 전체 목록은 Statistics 계산 결과를 다시 사용해 미리보기와 동일한 순서·Item 집합·착용 횟수를 표시하며 기존 Closet 저장 필터를 변경하지 않는다.
- 전체 목록으로 앞으로 이동할 때는 맨 위에서 시작하고, 뒤로 돌아오면 Statistics의 저장된 필터와 세로 위치를 복원하도록 했다.
- Item 상세에 전체 Wear Log 기준 최초·최근 기록일, 총 착용 횟수, 월별 12칸 횟수, 기록 월 수와 연중 착용 여부를 추가했다.
- 12개월 그래프는 한 줄 고정, 실제 횟수 표기, 월별 접근성 이름과 focus 표시를 제공하며 0회인 달도 유지한다.
- 전체 연도에 기록이 있으면 `연중 착용 · 12/12개월`을 표시하도록 계산과 회귀 테스트를 추가했다.

## Decisions - Phase 4 P4-1B 완료

- Statistics 조건은 location state가 아닌 URL에 저장해 새로고침과 직접 접근에서도 같은 결과를 재계산한다.
- Statistics 전체 결과를 기존 Closet 필터에 주입하지 않는다. 두 화면의 필터 원본을 독립적으로 유지한다는 Phase 4 결정을 지킨다.
- 전체 목록은 별도 집계값을 저장하지 않고 기존 authenticated snapshot과 P4-1A 순수 계산 결과를 재사용한다.
- 월별 막대 높이는 해당 Item의 최대 월 횟수에 대한 상대값으로 표시하되, 실제 횟수를 막대 위 숫자와 접근성 이름으로 항상 제공한다.
- 긴 목록은 기존 lazy image loading과 `content-visibility`를 사용해 초기 렌더링 부담을 줄인다.

## Current Status - Phase 4 P4-1B 완료

- 집중 테스트 14개와 전체 Vitest 51개 파일·234개 테스트, TypeScript 검사, production build가 통과했다.
- 로그인된 로컬 production 데이터에서 2025년 Most Worn 119개 전체 목록과 첫 Item 상세의 총 120회·12개월 분포가 같은 Wear Log 원본으로 연결되는 것을 확인했다.
- desktop과 390px에서 전체 목록·Item 상세를 확인했다. 390px 그래프는 12개 월이 335px 안에 모두 표시됐고 가로 overflow, console warning·error가 없었다.
- 2025년 Statistics에서 전체 목록으로 이동한 뒤 돌아왔을 때 기간 필터와 세로 위치가 복원됐으며, 1월 그래프 요소가 focus를 받는 것도 확인했다.
- build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. commit·push·배포와 원격 데이터 변경은 수행하지 않았다.

## Next Steps - Phase 4 P4-1B 완료

- P4-2A Replacement Line Overview의 TypeScript model과 lazy Supabase repository query를 구현한다.
- Style Identity 그룹, Active/Retired 수, 최근 Active 취득일, 빈 Line·단일 Item·복수 Line 소속 경고를 읽기 전용 UI에 연결한다.

## Work Summary - Phase 4 P4-2A Replacement Line Overview

기존 Supabase Replacement Line 원본을 수정하지 않고 실제 구조와 생존 상태를 확인하는 읽기 전용 Overview를 구현했다. Statistics에서 별도 화면으로 들어가 Style Identity별 Line, membership 품질, Active·Retired Item을 모바일에서도 차분하게 살펴볼 수 있게 했다.

## Work Completed - Phase 4 P4-2A Replacement Line Overview

- `ReplacementLineRecord`, membership, snapshot TypeScript 모델과 전용 계산 모듈을 추가했다.
- Replacement Line 화면이 열릴 때만 `closet_replacement_lines`와 `closet_replacement_line_items`를 workspace 조건으로 병렬 SELECT하는 lazy repository query를 추가했다.
- 기존 authenticated Item snapshot과 membership을 메모리에서 결합해 Line·membership·고유 Item, Active·Retired, 최근 Active 취득일을 계산했다.
- Style Identity별 그룹과 접을 수 있는 Line 목록을 만들고, 펼친 Line에서 같은 크기의 Item thumbnail·취득일·상태·Item 상세 링크를 제공했다.
- 빈 Line, 단일 Item Line, 복수 Line 소속 Item, 현재 workspace Item으로 확인되지 않는 membership 경고를 구현했다.
- P4-0에서 검증한 49개 무방향 Legacy Link의 총량과 `0/49` 검토 상태를 표시했다.
- Statistics 홈에 `Line Overview 열기` 진입점을 연결했다.
- workspace snapshot에 없는 membership Item의 이름이나 상세를 노출하지 않는 계산 테스트와 두 테이블 모두 workspace 조건을 사용하는 repository 테스트를 추가했다.

## Decisions - Phase 4 P4-2A Replacement Line Overview

- P4-2A는 기존 select RLS와 authenticated publishable client만 사용한다. schema, RLS, 원격 데이터, Notion 원본은 변경하지 않는다.
- Line 데이터를 초기 AppData에 추가하지 않고 화면 진입 시점에만 조회해 일반 HOME·Closet·Statistics 초기 경로의 요청량을 늘리지 않는다.
- Legacy `Replaces` pair는 아직 앱 DB에 없으므로 Line membership이나 취득일에서 관계를 추측하지 않는다. 49개 총량만 사실대로 보여 주며 개별 pair는 P4-2B의 schema·일회 추출·RLS 계약과 함께 표시한다.
- 운영 Item UUID pair를 정적 frontend asset에 포함하거나 Notion token을 브라우저에 노출하는 방식은 사용하지 않는다.
- 현재 workspace의 Item snapshot으로 해석되지 않는 membership은 식별 정보를 숨기고 개수만 경고한다.

## Current Status - Phase 4 P4-2A Replacement Line Overview

- 실제 로그인된 로컬 Supabase 앱에서 53 Lines, 165 memberships, 163 unique Items가 P4-0 기준선과 일치했다.
- 현재 상태는 Active 116개·Retired 47개, 빈 Line 1개·단일 Item Line 10개로 계산됐고 workspace 외 또는 확인 불가 membership 경고는 없었다.
- desktop과 390px 모바일에서 Overview, Statistics 진입 링크, Line 펼침과 5개 Item 목록을 확인했다. 두 viewport 모두 가로 overflow가 없고 console warning·error도 없었다.
- 집중 테스트 14개와 전체 Vitest 54개 파일·239개 테스트, TypeScript 검사, production build가 통과했다.
- build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. commit·push·배포와 원격 데이터 변경은 수행하지 않았다.
- P4-2A 체크리스트 6개 중 5개를 완료했다. 개별 49개 Legacy Link 무방향 연결선은 원본 pair가 앱 DB에 없는 데이터 경계 때문에 보류했다.

## Next Steps - Phase 4 P4-2A Replacement Line Overview

- P4-2B에서 49개 무방향 pair의 schema, 정규화된 unique 제약, 일회 추출과 workspace RLS 계약을 먼저 확정한다.
- 동일 크기 Item 비교 카드와 A→B·B→A·동등·병렬·대체 관계 아님 선택을 저장 전 preview·confirm 흐름으로 구현한다.
- 원격 migration과 Legacy pair import는 로컬 계약·pgTAP 검증 뒤 J의 명시적 확인을 받고 적용한다.

## Work Summary - Phase 4 P4-2B Legacy Link Review 로컬 구현

Notion의 reciprocal `Replaces` 98개 entry를 방향 없는 49개 pair로 안전하게 보존하고, 사람이 직접 관계와 이유를 확인하는 검토 큐를 로컬에서 완성했다. 취득일이나 Retired 상태로 방향을 추측하지 않고 preview 뒤에만 저장되는 경계를 schema, repository, UI에 함께 적용했다.

## Work Completed - Phase 4 P4-2B Legacy Link Review 로컬 구현

- workspace와 두 Item을 canonical A/B 순서로 저장하는 `closet_replacement_legacy_links` migration을 추가하고, 중복 pair·self pair·workspace 불일치·불완전 검토 결과를 DB 제약으로 차단했다.
- authenticated 사용자는 자신의 workspace pair만 SELECT할 수 있고 직접 UPDATE는 할 수 없도록 RLS와 grant를 제한했다. 검토 저장은 membership, 선택지, 필수 이유, pending 상태를 다시 확인하는 security-definer RPC로만 열었다.
- 실제 Notion 스냅샷을 읽는 49-pair importer를 추가했다. 기본은 dry-run이며 `--apply`를 명시해야만 대상 테이블에 upsert하고 기존 검토 필드는 덮어쓰지 않는다.
- Line Overview가 migration 적용 전에는 P4-0 기준선 49개를 유지하고, 적용 뒤에는 실제 pending·reviewed 진행률과 검토 화면 진입점을 표시하도록 연결했다.
- 동일 크기 A/B Item 카드, 취득일·Active/Retired·공통 Line 참고 정보, 네 가지 관계 선택, 필수 이유, 저장 전 preview, 진행률과 중단 후 복원을 구현했다.
- table·column·RLS·grant·RPC·canonical pair·workspace 격리·이유 검증·재검토 차단을 다루는 pgTAP 계약 테스트와 repository·queue·화면 테스트를 추가했다.
- 실제 스냅샷 dry-run에서 98개 entry가 49개 canonical·reciprocal pair로 정규화되고 blocker가 0개인지 확인했다.

## Decisions - Phase 4 P4-2B Legacy Link Review 로컬 구현

- Legacy Link 자체는 무방향 원본으로 보존하고, 이번 단계에서 directed edge를 만들지 않는다. A→B와 B→A도 검토 결과로만 기록하며 P4-2C에서 확인된 edge 저장 계약으로 연결한다.
- canonical A/B 순서는 UUID 비교로 고정해 동일 pair의 역순 중복을 막되, 이 순서에 계보 의미를 부여하지 않는다.
- frontend에 일반 UPDATE 권한을 주지 않고 confirm RPC만 사용한다. 이미 reviewed인 pair는 RPC에서도 다시 저장할 수 없게 해 검토 결과의 우발적 덮어쓰기를 막는다.
- production migration과 49개 import는 원격 쓰기이므로 로컬 구현 범위에서 실행하지 않는다. 실제 적용은 J의 명시적 승인 뒤 migration, dry-run 재확인, import 순서로 진행한다.

## Current Status - Phase 4 P4-2B Legacy Link Review 로컬 구현

- P4-2B UI 체크리스트 6개를 완료했고 실제 49개 queue 항목만 production migration/import 승인 전 상태로 남았다.
- 집중 Vitest 7개, 전체 Vitest 56개 파일·243개 테스트, Node 계약 테스트 5개, TypeScript 검사와 production build가 통과했다.
- desktop과 390px demo 화면에서 동등 비교 카드, 필수 이유, preview 잠금, confirm 뒤 다음 pair 이동과 진행률 복원을 확인했다. 390px 문서 폭은 viewport 안에 들어왔고 console warning·error는 없었다.
- build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.
- 로컬 Docker가 실행 중이지 않아 새 pgTAP 파일은 작성했지만 실제 DB 실행은 하지 못했다. production migration·import, commit·push·배포는 수행하지 않았다.

## Next Steps - Phase 4 P4-2B Legacy Link Review 로컬 구현

- J의 명시적 승인을 받은 뒤 production migration을 적용하고 새 table·RLS·RPC를 원격에서 검증한다.
- 같은 2026-08-02 Notion 스냅샷으로 dry-run을 다시 확인한 뒤 49개 canonical pair만 import하고, 실제 Overview와 review queue 수량을 확인한다.
- 실제 49개 pair 검토를 시작하기 전에 첫 pair의 read·preview·confirm·resume 흐름을 production에서 한 번 검증한다.
- 이후 P4-2C에서 확인된 predecessor·successor만 directed edge로 저장하고 cycle·self-edge·workspace 불일치를 차단한다.

## Work Summary - Phase 4 P4-2B production 적용

J의 명시적 승인 뒤 P4-2B schema와 49개 Legacy Link를 production에 적용했다. migration 이력, DB 권한과 RLS, import 결과, 실제 로그인 앱의 read 경로를 순서대로 확인하고 실제 관계 판단은 하나도 저장하지 않았다.

## Work Completed - Phase 4 P4-2B production 적용

- production 대상이 healthy 상태의 `mworkroom` project인지 확인하고 적용 전 Item 451개, Line 53개, membership 165개와 Legacy table·RPC 부재를 재확인했다.
- Supabase migration 도구로 `phase4_legacy_link_review`를 적용하고 원격 migration version `20260802200342`에 맞춰 로컬 migration filename을 정렬했다.
- 원격에서 table·RPC 존재, RLS 활성, SELECT policy 1개와 14개 제약을 확인했다.
- post-migration advisor에서 세 foreign key의 covering index 부재를 발견해 후속 `phase4_legacy_link_foreign_key_indexes` migration을 적용하고, Item A·Item B·reviewed_by index 존재와 경고 해소를 확인했다.
- authenticated에는 table SELECT와 confirm RPC만 허용되고 INSERT·UPDATE·DELETE는 차단되며, anon RPC 실행도 차단되는 것을 실제 privilege query로 확인했다.
- 2026-08-02 Notion 스냅샷을 import 직전에 다시 dry-run해 98개 entry, 49개 canonical·reciprocal pair, blocker 0을 확인한 뒤 `--apply`로 49개만 upsert했다.
- import 뒤 전체 49개가 pending이고 검토값·역순·중복·다른 workspace·공통 Line 누락이 모두 0인지 확인했다.
- 실제 workspace member로 confirm RPC를 transaction 안에서 호출해 decision·reason 저장 결과를 확인하고 rollback했다. 이후 49개 전부 pending, reviewed 0개를 재확인했다.
- 로그인된 실제 앱에서 Replacement Lines `검토 0/49`, Review `0/49`, 첫 pair의 실제 Item 이미지·취득일·공통 Line과 선택 전 저장 비활성 상태를 desktop·390px에서 확인했다.

## Decisions - Phase 4 P4-2B production 적용

- CLI access token이 로컬에 없어 `db push`는 사용하지 못했지만, 원격 migration 이력을 기록하는 연결된 Supabase migration 도구를 사용하고 생성된 version으로 로컬 filename을 맞춰 이력 불일치를 남기지 않았다.
- security advisor가 authenticated용 confirm RPC를 SECURITY DEFINER 함수로 표시하는 것은 의도된 경계다. PUBLIC·anon 실행은 revoke하고, 함수 내부에서 로그인·workspace membership·pending 상태·선택지·필수 이유를 다시 검사한다.
- 화면 조회용 workspace-first index와 FK 검증용 referenced-column-first index는 목적이 다르므로 둘 다 유지한다. 생성 직후의 unused-index INFO는 실제 사용 통계가 쌓이기 전의 예상 상태다.
- 실제 pair의 방향이나 제외 여부는 UI 동작 검증을 위해 임의로 고르지 않는다. RPC 동작 검증은 rollback transaction으로만 수행했다.
- import는 target table만 upsert하고 기존 행 삭제, 검토값 덮어쓰기, 방향 자동 선택을 하지 않는 계약을 유지했다.

## Current Status - Phase 4 P4-2B production 적용

- P4-2A와 P4-2B 체크리스트를 완료했다. production에는 49개 무방향 Legacy Link가 있고 검토 진행률은 0/49다.
- 실제 앱의 Overview와 Review queue read 경로가 동작하며 390px에서 문서 가로 넘침과 console warning·error가 없었다.
- migration 전후 advisor를 실행했다. 새 table의 RLS·grant 문제와 unindexed foreign key는 모두 해소했고, 새 RPC 경고는 의도한 authenticated 호출 권한으로 확인했다. 새 index의 unused INFO와 다른 기존 프로젝트 함수·Auth 설정 경고는 이번 작업 범위에서 변경하지 않았다.
- 실제 Legacy Link review 결과, directed edge, commit·push·앱 배포는 아직 수행하지 않았다.

## Next Steps - Phase 4 P4-2B production 적용

- 앱에서 49개 pair를 실제로 검토하며 방향·동등·제외 여부와 이유를 저장한다.
- P4-2C에서는 검토 결과를 읽어 확인된 A→B·B→A만 directed edge 후보로 변환하고, cycle·self-edge·workspace 불일치를 원자적으로 차단한다.
- 모든 실제 검토가 끝나기 전까지 취득일·Retired 상태로 방향을 자동 확정하지 않는다.

## Work Summary - 기존 착장 수정 기능

Lookbook 상세에서 기존 착장을 복제할 수만 있고 잘못 들어간 구성 Item을 고칠 수 없던 문제를 해결했다. 기존 Outfit ID와 기록을 유지한 채 이름과 구성 Item만 수정하는 흐름을 추가했다.

## Work Completed - 기존 착장 수정 기능

- 착장 상세의 구성 아이템 아래에 `착장 수정`과 `새로 만들기`를 나란히 배치했다.
- 기존 생성 화면을 수정 모드로 재사용해 현재 이름, Item, 이미지 배치를 초깃값으로 불러오고 Item 추가·제거 후 같은 Outfit 상세로 돌아오게 했다.
- Demo와 Supabase 저장소에 Outfit 수정 계약을 추가했다. Supabase에서는 이름 변경과 관계 교체를 단일 RPC transaction으로 처리한다.
- 수정 저장 시 동일 조합 검사는 현재 Outfit 자신을 제외하며, 다른 Outfit과 중복될 때는 기존과 같은 확인 절차를 유지한다.
- 구성 변경 뒤 기존 저장 Preview는 relation trigger를 통해 stale 처리하며, 평가·보관 상태·Wear Log는 변경하지 않는다.
- 상세 진입, 동일 ID 수정, RPC payload, 기존 기록 보존을 다루는 회귀 테스트를 추가했다.

## Decisions - 기존 착장 수정 기능

- 복제와 수정은 목적이 다르므로 두 동작을 모두 유지하되 `착장 수정`을 우선 버튼으로 표시했다.
- 부분적인 relation 쓰기 중 실패해 착장이 불완전해지는 일을 막기 위해 전체 구성을 DB 함수 안에서 한 transaction으로 교체한다.
- 저장 Preview는 현재 UI에서 보류 중이므로 수정 시 새 Preview를 자동 생성하지 않고 기존 cache만 stale 처리한다.

## Current Status - 기존 착장 수정 기능

- 전체 Vitest 56개 파일·245개 테스트와 production build가 통과했다.
- 별도 Demo 서버에서 `착장 수정` 진입, 블랙 팬츠 제거, 같은 ID 저장, 구성 4개에서 3개로 변경, 기존 착용 기록 2개 유지를 실제 화면으로 확인했다.
- 공개 Outfit은 읽기만 했으며 실제 데이터, production schema, Git 원격에는 아직 변경을 적용하지 않았다.
- build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.

## Next Steps - 기존 착장 수정 기능

- production Supabase에 새 migration을 적용한 뒤 함수 권한, workspace 격리, advisor 결과를 확인한다.
- commit·push·GitHub Pages 배포 뒤 공개 Outfit에서 `착장 수정` 버튼과 실제 저장을 확인한다.

## Work Summary - 기존 착장 수정 production 적용

production Supabase에 기존 착장 수정 RPC를 적용하고 실제 authenticated 호출을 rollback transaction으로 검증했다. 이어서 앱 코드를 GitHub Pages에 배포할 준비를 완료했다.

## Work Completed - 기존 착장 수정 production 적용

- healthy `mworkroom` 프로젝트와 적용 전 함수 부재, Outfit 508개 기준선을 확인했다.
- `update_closet_outfit` migration을 원격 version `20260802225908`로 적용하고 로컬 migration filename을 같은 version으로 정렬했다.
- 함수의 `search_path`가 비어 있고 anon 실행은 차단, authenticated 실행만 허용된 것을 권한 query로 확인했다.
- 실제 문제 Outfit의 현재 7개 Item 구성을 그대로 전달해 authenticated RPC를 호출하고 transaction을 rollback했다. 함수가 같은 ID, 이름, 평가, 보관 상태를 반환하는 것을 확인했다.
- rollback 뒤 대상 Outfit의 이름, 평가, 보관 상태와 Item 7개가 그대로인지 재확인했다.
- 적용 후 security·performance advisor를 실행했다.

## Decisions - 기존 착장 수정 production 적용

- 실제 중복 코트 제거는 J가 UI에서 명시적으로 선택할 작업이므로 배포 과정에서 production Outfit 데이터를 임의로 수정하지 않았다.
- 전체 relation 교체는 원자성과 workspace 격리를 위해 SECURITY DEFINER RPC로 유지했다. PUBLIC·anon 실행을 revoke하고 함수 내부에서 로그인과 workspace membership을 검사하며 빈 `search_path`를 고정했다.
- advisor의 authenticated SECURITY DEFINER 경고는 이 RPC가 로그인 앱에서 호출되는 의도된 API 경계이므로 유지한다. 다른 기존 프로젝트 경고는 이번 범위에서 변경하지 않는다.

## Current Status - 기존 착장 수정 production 적용

- production migration과 rollback 호출 검증이 완료됐으며 실제 Outfit 데이터에는 변경이 없다.
- 새 함수와 관련해 anon 노출, mutable search path, 새 table·index 문제는 발견되지 않았다.
- 앱 코드 commit·push·Pages 배포와 공개 화면 확인이 남아 있다.

## Next Steps - 기존 착장 수정 production 적용

- 이번 기능 파일만 stage·commit·push한다.
- Pages 배포 성공 뒤 공개 상세에서 `착장 수정` 진입과 기존 7개 Item 로딩을 확인한다.

## Current Status - 기존 착장 수정 공개 배포 확인

- commit `615190d`를 main에 push했고 GitHub Pages workflow `30771366596`의 build와 deploy가 모두 성공했다.
- 공개 문제 Outfit 상세에서 `착장 수정`과 `새로 만들기` 버튼, 현재 구성 7개가 표시되는 것을 확인했다.
- 공개 수정 화면에서 카멜 코트와 블랙 코트를 포함한 7개 Item이 선택된 상태로 로딩되고 `변경 저장` 버튼이 제공되는 것을 확인했다.
- 실제 중복 코트 제거와 저장은 수행하지 않아 production Outfit 데이터는 그대로 유지했다.

## Next Steps - 기존 착장 수정 공개 배포 확인

- J가 공개 수정 화면에서 잘못 등록된 코트 하나를 선택 해제하고 `변경 저장`을 누른다.

## Work Summary - Phase 4 P4-2C 재검토 이력 기반

J가 production Legacy Link 49개를 모두 검토한 뒤에도 판단을 나중에 바꿀 수 있도록, 완료 목록에서 관계를 다시 열어 수정하고 이전 판단을 보존하는 P4-2C 기반을 구현했다. directed edge 자동 변환보다 수정 가능성과 audit trail을 먼저 확정했다.

## Work Completed - Phase 4 P4-2C 재검토 이력 기반

- production을 읽기 전용으로 확인해 49/49 완료, A→B 8개·B→A 37개·동등·병렬 1개·대체 관계 아님 3개, 이유 누락 0개를 확인했다.
- 48쌍은 공통 Line이 하나였고 `무탠다드 슬리브리스 — 퍼스트클로 슬리브리스` 1쌍만 두 Line을 공유하는 것을 확인했다.
- 검토 완료 화면에 49개 관계 목록과 `다시 검토` 동작을 추가했다. 기존 방향과 이유를 초깃값으로 복원하고 실제 변경이 있을 때만 preview와 저장을 열었다.
- `closet_replacement_legacy_link_revisions` append-only 이력 table, workspace RLS, 최소 grant, FK covering index와 `revise_closet_replacement_legacy_link` RPC를 migration `20260802233848`로 production에 적용했다.
- 하위 호환용 최초 검토 RPC도 row 조회 전 로그인·workspace membership을 먼저 검사하도록 migration `20260802234752`로 보강했다.
- RPC가 row lock과 `expected_updated_at`을 사용해 오래 열린 화면의 stale overwrite를 차단하고, 현재 snapshot 변경과 revision 추가를 한 transaction에서 처리하도록 했다.
- 기존 confirm RPC도 최초 검토를 revision으로 남기도록 연결하고, 이미 저장된 49개 결과를 revision 1로 backfill했다.
- production rollback 검증에서 revision 50·최대 revision 2가 생성되는 것을 확인한 뒤 rollback 후 49개로 복원되는 것을 확인했다.
- desktop과 390px 로그인 앱에서 49/49 완료 목록, 기존 판단·이유 복원, 변경 없음 저장 차단, 목록 복귀, 복수 공통 Line 경고, 가로 overflow 부재와 console warning·error 0개를 확인했다.
- 전체 Vitest 56개 파일·246개 테스트와 production build가 통과했다.

## Decisions - Phase 4 P4-2C 재검토 이력 기반

- 사용자가 판단을 바꿀 수 있어야 하지만 이전 판단을 잃어서는 안 되므로 현재 결과 snapshot과 append-only revision 이력을 함께 유지한다.
- frontend에는 두 table의 직접 쓰기 권한을 열지 않고 authenticated RPC만 사용한다. PUBLIC·anon 실행은 차단하고 함수 내부에서 로그인과 workspace membership을 다시 검사한다.
- 두 Line을 공유하는 1쌍은 어느 Line의 edge인지 데이터만으로 정할 수 없어 자동 변환하지 않는다. directed edge 단계에서 Line을 명시적으로 선택한다.
- production pgTAP extension은 설치돼 있지 않아 원격 pgTAP 파일 실행은 하지 않았다. 대신 전체 migration rollback 적용, 실제 49-row backfill 대조, 권한 query, authenticated revise rollback, Advisor로 같은 계약을 검증했다.
- `scripts/*.test.mjs`를 Node 내장 runner로 실행한 시도는 Vitest context 오류로 실패했다. 올바른 `npm test`가 해당 파일을 포함해 전체 통과했으므로 코드 결함으로 보지 않는다.

## Current Status - Phase 4 P4-2C 재검토 이력 기반

- production의 현재 49개 판단과 revision 49개가 1:1로 일치하며 decision·reason·reviewer 불일치는 0개다.
- authenticated는 revision SELECT와 revise RPC만 사용할 수 있고 revision INSERT·UPDATE·DELETE 및 anon RPC는 차단됐다.
- Advisor에는 의도된 authenticated SECURITY DEFINER RPC와 생성 직후 unused index INFO만 추가됐다. missing RLS·anon 실행·unindexed foreign key 문제는 발견되지 않았다.
- production schema는 적용됐고 로컬 앱은 새 UI와 RPC를 사용한다. 앱 코드 commit·push·공개 배포는 수행하지 않았다.

## Next Steps - Phase 4 P4-2C 재검토 이력 기반

- A→B 8개와 B→A 37개를 directed edge 후보로 읽되 동등·병렬 1개와 대체 관계 아님 3개는 edge로 만들지 않는 schema 계약을 작성한다.
- 두 Line을 공유하는 1쌍의 Line 선택을 명시적으로 받는 preview·confirm 흐름을 포함한다.
- self-edge, cycle, workspace 불일치와 stale overwrite를 원자적으로 차단하는 directed edge RPC와 pgTAP fixture를 구현한다.
- commit·push·공개 배포는 J가 요청할 때 별도 범위 확인 후 진행한다.

## Work Summary - Phase 4 P4-2C directed edge 기반과 후보 preview

49개 Legacy Link 검토 결과를 곧바로 계보로 저장하지 않고, 먼저 실제 directed edge 후보와 예외를 확인할 수 있는 preview를 만들었다. 동시에 이후 확정 저장을 열어도 잘못된 방향·Line·workspace·cycle이 들어가지 않도록 production DB 계약을 준비했다.

## Work Completed - Phase 4 P4-2C directed edge 기반과 후보 preview

- production 검토 결과를 읽기 전용으로 계산해 방향 관계 45개 중 공통 Line이 하나인 저장 준비 후보 44개, 두 Line 중 선택이 필요한 후보 1개, 동등·병렬 또는 대체 관계 아님으로 edge에서 제외할 관계 4개를 확인했다.
- 후보 graph의 self-edge 0개, 중복 edge 0개, cycle 0개와 실제 가지 1곳·합류 1곳을 확인하고 preview 화면에 구조 점검, Line 선택 대상, 실제 가지·합류, 준비 후보와 제외 관계를 분리해 표시했다.
- Replacement Line Overview와 49/49 검토 완료 화면에서 preview로 진입할 수 있게 했으며, preview에는 저장 버튼을 두지 않았다.
- `closet_replacement_line_edges` table, workspace member SELECT RLS, 최소 grant, Line membership composite FK, 출처 Legacy Link FK, 방향 중복·self-edge 제약, 상태·가지 이름·선택 이유 필드를 migration `20260803000733`으로 production에 적용했다.
- authenticated confirm RPC가 Legacy Link의 현재 검토 결과에서 predecessor·successor를 다시 계산하고 `expected_updated_at`을 확인하도록 했다. transaction advisory lock과 recursive cycle trigger로 동시에 들어오는 edge도 같은 Line 안에서 직렬화해 검사한다.
- Legacy Link 판단이나 이유가 바뀌면 이미 확인된 edge를 `needs_review`로 되돌리는 trigger를 추가했다.
- Advisor가 지적한 successor membership과 source Legacy Link FK index 열 순서를 migration `20260803001136`으로 보완했다. 재검사 후 새 unindexed foreign key는 0건이다.
- 실제 production 후보 1개를 authenticated RPC로 저장하는 rollback 검증과 A→B→C 뒤 C→A 차단, 출처 재검토 뒤 `needs_review` 전환 fixture를 transaction 안에서 확인했다. 모든 검증 뒤 production edge는 0개다.
- 전체 Vitest 58개 파일·249개 테스트와 production build를 통과했다. 실제 production 데이터가 연결된 localhost에서 desktop과 390px UI, Overview 진입 링크, 준비 후보 펼치기, 가로 overflow 부재와 console warning·error 0개를 확인했다.

## Decisions - Phase 4 P4-2C directed edge 기반과 후보 preview

- 44개 후보가 구조적으로 유효해도 사람의 최종 저장 승인을 대신하지 않으므로 이번 단계에서는 production edge를 만들지 않는다.
- 두 Line을 공유하는 `무탠다드 슬리브리스 — 퍼스트클로 슬리브리스` 관계는 데이터만으로 Line을 정할 수 없어 두 선택지를 그대로 노출하고 자동 귀속하지 않는다.
- 방향은 frontend payload를 신뢰하지 않고 출처 Legacy Link의 검토 결과에서 DB가 다시 계산한다. 두 Item의 workspace와 선택 Line membership은 composite FK가 최종 차단한다.
- cycle 검사는 단순 사전 조회만으로 끝내지 않고 Line별 transaction lock을 잡은 trigger에서 실행해 동시 저장 race를 막는다.
- Advisor의 authenticated SECURITY DEFINER 경고는 로그인 앱에서만 호출하는 의도된 RPC 경계다. PUBLIC·anon 실행을 차단하고 함수 안에서 로그인·workspace membership을 다시 검사하므로 유지한다.

## Current Status - Phase 4 P4-2C directed edge 기반과 후보 preview

- production에는 directed edge schema와 안전한 confirm RPC가 적용됐고 실제 edge row는 0개다.
- preview는 현재 49개 검토 결과를 44개 준비, 1개 Line 선택, 4개 제외로 정확히 보여 주며 self-edge·중복·cycle은 모두 0으로 표시한다.
- DB rollback fixture에서 정상 DAG 저장, cycle 차단, source revision에 따른 `needs_review`, rollback 뒤 row 0개를 확인했다.
- build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 앱 코드 commit·push·공개 배포는 수행하지 않았다.

## Next Steps - Phase 4 P4-2C directed edge 기반과 후보 preview

- 두 Line을 공유하는 1쌍에 대해 edge가 속할 Line을 J가 명시적으로 선택하는 UI를 연다.
- 44개 단일-Line 후보와 선택을 마친 1개 후보의 실제 저장 대상을 한 번 더 확인하는 preview·confirm 흐름을 구현한다.
- 확인된 edge에서 시작점과 graph depth를 계산하고, 실제 가지·합류를 같은 크기 Item thumbnail의 세로형 Lineage UI로 렌더링한다.
- commit·push·공개 배포는 J가 요청할 때 별도 범위 확인 후 진행한다.

## Work Summary - Replacement Lines 상위 메뉴 이동과 계보 UI 기준 명시

Replacement Lines가 Item Statistics의 하위 기능처럼 보이던 정보 구조를 바로잡아 More의 독립 상위 메뉴로 이동했다. 또한 이전에 확정한 계보도 시안이 계획서의 짧은 원칙만으로는 정확히 복원되지 않는 문제를 확인하고 세대 카드·상태·가지 연결선의 구체적인 UI 계약을 문서에 남겼다.

## Work Completed - Replacement Lines 상위 메뉴 이동과 계보 UI 기준 명시

- More 메뉴에 `Replacement Lines`를 Statistics와 같은 레벨로 추가하고 기본 경로를 `/replacement-lines`로 변경했다.
- Review와 edge preview도 각각 `/replacement-lines/review`, `/replacement-lines/edges/preview`로 옮기고 모든 내부 링크와 route 테스트를 갱신했다.
- 기존 `/statistics/replacement-lines…` 세 경로는 새 경로로 `replace` redirect해 이전 북마크와 열려 있던 링크가 깨지지 않게 했다.
- Statistics 화면 안의 중복 Replacement Lines 진입 카드를 제거하고 사용하지 않게 된 전용 CSS를 정리했다.
- Phase 4 계획서의 메뉴 흐름을 `MORE → Replacement Lines`로 바꾸고, 확정 시안의 화면 머리, G0·G1·G2 묶음 카드, 동일 thumbnail, 상태 badge, 이유, predecessor별 branch connector와 모바일 한 열 규칙을 명시했다.
- More 전용 테스트를 추가하고 기존 navigation 순서 회귀 테스트를 새 네 개 메뉴 계약에 맞췄다.
- localhost desktop과 390px에서 More 메뉴, 새 경로 진입, 옛 preview URL redirect, Statistics의 중복 카드 제거, 가로 overflow 부재와 console warning·error 0개를 확인했다.

## Decisions - Replacement Lines 상위 메뉴 이동과 계보 UI 기준 명시

- Statistics와 Replacement Lineage는 Phase 4라는 개발 단계는 공유하지만 제품 탐색에서는 서로 독립된 목적이므로 More의 형제 메뉴로 둔다.
- route 변경이 기존 링크를 불필요하게 깨뜨리지 않도록 옛 경로를 즉시 삭제하지 않고 명시적 redirect를 유지한다.
- 기존 계획서의 `세로형`, `같은 크기 thumbnail`, `가지 표시`만으로는 확정 시안의 세대별 그룹과 바깥 branch connector를 알 수 없다. 이후 구현자가 이미지를 기억한다고 가정하지 않고 텍스트 wireframe과 acceptance rule을 source of truth로 추가했다.
- 시안의 `1벌`처럼 현재 원본에 없는 값은 화면 모양을 맞추기 위해 추정하지 않는다.

## Current Status - Replacement Lines 상위 메뉴 이동과 계보 UI 기준 명시

- More에서 Favorite, Statistics, Replacement Lines, Settings가 순서대로 보이며 Replacement Lines는 새 상위 경로로 열린다.
- 옛 Statistics 하위 URL은 새 경로로 정상 redirect된다.
- 전체 Vitest 59개 파일·250개 테스트와 production build가 통과했다. 첫 전체 실행에서는 More 메뉴를 3개로 고정한 기존 assertion이 실패했으며, 새 네 번째 메뉴를 의도한 순서로 반영한 뒤 전체 통과했다.
- build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. commit·push·공개 배포는 수행하지 않았다.

## Next Steps - Replacement Lines 상위 메뉴 이동과 계보 UI 기준 명시

- directed edge 확정 뒤 계획서의 `ITEM LINEAGE` 기준안대로 Line 상세 계보 화면을 구현한다.
- 두 Line을 공유하는 1쌍의 Line 선택과 edge 최종 preview·confirm 흐름을 먼저 완료한다.
- 공개 배포 시 옛 URL redirect와 More의 새 메뉴를 실제 GitHub Pages에서도 다시 확인한다.

## Work Summary - Phase 4 P4-2C 원자 일괄 edge 확정 흐름

두 Line을 공유하는 관계를 자동 귀속하지 않고 사람이 명시적으로 선택한 뒤, 44개 자동 후보와 합쳐 45개 전체를 마지막으로 확인하고 한 번에 저장할 수 있는 흐름을 구현했다. 실제 production 저장은 J의 최종 선택 전까지 보류했다.

## Work Completed - Phase 4 P4-2C 원자 일괄 edge 확정 흐름

- Line 선택 대상에 기본값 없는 두 radio 선택지를 제공하고, 선택 전에는 최종 preview를 열 수 없게 했다.
- 선택 뒤 자동 귀속 44개·직접 선택 1개·제외 4개와 선택한 방향·Line, 전체 rollback 조건을 다시 보여 주는 최종 확인 단계를 추가했다.
- 여러 edge를 순차적인 frontend 요청으로 저장하지 않고 `confirm_closet_replacement_line_edges` batch RPC 한 번과 DB transaction 하나로 처리하도록 repository와 DataContext를 확장했다.
- batch RPC는 source 관계를 고정된 순서로 처리하고, 중복 source·잘못된 Line·stale 검토 결과·cycle 등 한 후보의 오류가 앞선 저장까지 전부 rollback되게 했다.
- production migration `20260803010712`를 적용하고 rollback fixture를 실행하는 과정에서 composite 반환값을 UUID 필드로 잘못 대입하던 오류를 발견했다. `SELECT * INTO`로 바로잡은 corrective migration `20260803010955`를 적용했다.
- 첫 fixture의 PostgreSQL `min(uuid)` 미지원도 `array_agg` 기반 선택으로 수정했으며, 이후 첫 후보가 유효하고 두 번째 후보가 잘못된 경우에도 첫 insert까지 rollback되는 것을 production transaction에서 확인했다.
- demo repository에도 같은 원자 저장·중복·membership·stale·cycle 검사를 추가하고, 실제 두 Line 선택을 재현하는 membership fixture를 보강했다.
- production authenticated 실행 권한, anon 실행 차단, 빈 `search_path`와 최종 edge 0개를 확인했다.
- localhost desktop과 390px에서 기본 선택 없음, 선택 전 차단, 최종 preview, rollback 안내, 가로 overflow 부재와 console warning·error 0개를 확인했다. 실제 확정 저장 버튼은 누르지 않았다.
- 첫 전체 테스트에서는 두 Line demo fixture 추가로 membership 합계가 6개에서 7개로 바뀌고 Line 문구가 한 줄로 합쳐져 기존 assertion 2개가 실패했다. 기능 동작과 일치하도록 기대값을 갱신한 뒤 전체 테스트를 다시 통과했다.

## Decisions - Phase 4 P4-2C 원자 일괄 edge 확정 흐름

- 두 공통 Line 중 어느 쪽인지 데이터만으로 정하지 않고 기본 선택을 비워 둔다. 브라우저 QA에서 선택한 값도 화면 확인용 일시 상태일 뿐 저장하지 않는다.
- 45개 중 일부만 저장된 상태는 첫 계보 생성에 부적합하므로 batch 전체를 한 transaction의 성공 또는 전체 rollback으로 처리한다.
- 초기 일괄 확정 화면에서 기존 edge가 일부라도 발견되면 중복·부분 import를 시도하지 않고 차단한다.
- SECURITY DEFINER RPC는 authenticated에만 열고 함수 내부의 로그인·workspace membership·입력 검증과 빈 `search_path`를 유지한다. PUBLIC과 anon 실행은 허용하지 않는다.
- production 데이터 생성은 되돌릴 수 있는 기술 검증 범위를 넘으므로 J의 명시적 Line 선택과 최종 저장 동작 전까지 0개를 유지한다.

## Current Status - Phase 4 P4-2C 원자 일괄 edge 확정 흐름

- production에는 원자 batch RPC와 corrective migration이 적용됐고 rollback 검증 뒤 실제 directed edge는 0개다.
- 선택 UI와 최종 확인 단계, 실제 저장 repository 연결이 완료됐다.
- TypeScript 검사, 전체 Vitest 59개 파일·251개 테스트와 production build가 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.
- commit·push·공개 배포는 수행하지 않았다.

## Next Steps - Phase 4 P4-2C 원자 일괄 edge 확정 흐름

- J가 두 Line 중 하나를 실제로 선택하고 45개 전체 저장을 확정할 때 production edge를 생성한다.
- 저장된 edge를 기준으로 시작점과 graph depth를 계산하고, 확정 시안의 G0·G1·G2 카드와 predecessor별 가지 연결선을 구현한다.
- commit·push·공개 배포는 J가 요청할 때 별도 범위 확인 후 진행한다.

## Work Summary - Phase 4 P4-2C 세대별 Lineage UI

J가 실제로 확정 저장한 production edge 45개를 읽어, 이전에 확정한 `ITEM LINEAGE` 시안과 같은 세대별 Line 상세 화면을 구현했다. 단순 구매일 목록이 아니라 확인된 방향 graph를 G0·G1·G2 이상으로 계산하며 실제 가지와 합류를 데이터대로 표시한다.

## Work Completed - Phase 4 P4-2C 세대별 Lineage UI

- production을 읽기 전용으로 확인해 Legacy Link 49/49 검토와 confirmed edge 45개 저장 완료 상태를 확인했다. 이번 작업에서는 production 데이터를 추가로 변경하지 않았다.
- confirmed edge의 위상 순서와 가장 깊은 predecessor 경로를 기준으로 graph depth를 계산하고 cycle이면 세대를 만들지 않는 순수 계산 모듈을 추가했다.
- 같은 predecessor 집합의 후속 Item은 한 세대 카드의 동일한 행으로 묶고, predecessor가 여러 개인 successor는 합류로 구분했다.
- Line membership에는 있지만 confirmed edge에 참여하지 않은 Item을 임의 G0로 만들지 않고 `계보 연결 전` 영역으로 분리했다.
- `/replacement-lines/:lineId` 상세 route와 Overview의 `계보 보기` 동작을 추가했다.
- 화면 머리의 `ITEM LINEAGE`, Line 이름, 사용 중·Retired 합계와 G0·G1·G2 header, 같은 크기 실제 Item 이미지, 취득 연도, 선택 이유, 상태 badge, 세로 connector를 확정 시안에 맞춰 구현했다.
- 실제 production `Black Hoodie Winter`의 G0→G1→G2, `Navy Blouse Summer`의 한 시작점에서 두 후속 Item으로 이어지는 분기, `Black Long Coat`의 합류 데이터를 확인했다.
- `Ivory Layered`에서는 confirmed edge에 연결된 2개만 G0·G1에 표시하고 나머지 membership 8개가 `계보 연결 전`으로 분리되는 것을 확인했다.
- Overview에서 `Black Hoodie Winter` 카드를 열고 `계보 보기`로 이동한 뒤 Item 상세에 진입하고 뒤로 돌아오는 실제 동작을 확인했다.
- 모바일 첫 QA에서 Active/Retired 합계가 제목과 정렬되지 않고 카드 비율이 시안보다 넓은 차이를 수정했다. DataProvider 로딩 중 잘못된 `Line을 찾을 수 없습니다`가 잠깐 나타나던 조건도 app data가 준비된 뒤에만 판정하도록 고쳤다.
- source 시안과 375px 실제 production 화면을 같은 비교 입력에서 재검사해 P0·P1·P2가 없음을 확인하고 프로젝트 root의 `design-qa.md`를 `passed`로 기록했다.
- 전체 Vitest 61개 파일·254개 테스트와 production build가 통과했다.

## Decisions - Phase 4 P4-2C 세대별 Lineage UI

- graph depth는 구매 연도가 아니라 confirmed edge로만 계산한다. 취득 연도는 카드의 참고 정보로만 표시한다.
- `needs_review`와 `archived` edge는 세대 계산에서 제외하고, 재검토 수가 있으면 별도 상태 문구를 제공한다.
- membership만 있는 Item을 root로 추정하면 확인하지 않은 계보가 생기므로 별도 목록으로 보존한다.
- 시안의 `1벌`과 `이어짐`처럼 현재 데이터 모델에 없는 값을 화면 모양을 맞추기 위해 생성하지 않는다. `이어짐` badge는 향후 planned-successor 상태가 생길 때 추가한다.
- 모든 Item 이미지는 기존 Storage cutout을 재사용하며 세대가 깊어져도 thumbnail slot을 줄이지 않는다.
- 확정 시안은 제품 구조의 source of truth로 사용하되 Item 이름·이미지·상태·이유는 fixture 대신 실제 production 데이터를 우선한다.

## Current Status - Phase 4 P4-2C 세대별 Lineage UI

- production 45개 edge를 읽는 세대별 Lineage 화면과 Overview 진입 동작이 로컬 앱에 구현됐다.
- desktop과 390×844 mobile에서 의미 있는 DOM, framework overlay 부재, 가로 overflow 0, console warning·error 0을 확인했다.
- build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.
- 코드 commit·push·공개 배포는 수행하지 않았다.

## Next Steps - Phase 4 P4-2C 세대별 Lineage UI

- 기존 edge의 Line 귀속, predecessor·successor, 가지 이름과 선택 이유를 안전하게 수정하는 편집 흐름을 구현한다.
- Line 병합·보관·대표 Line과 membership 변경에 따른 `needs_review` 흐름을 구현한다.
- 공개 배포 시 새 Line 상세 route와 실제 production 계보를 GitHub Pages에서 다시 검증한다.
- commit·push·공개 배포는 J가 요청할 때 별도 범위 확인 후 진행한다.
