# 2026-07-31 Closet Index Development Log

## Work Summary

목에 두르는 숄과 이름은 비슷하지만 허리에 묶어 상의·하의 위에 겹치는 페이크숄을 독립적으로 표현하기 위해 `Acc-Waist` 카테고리와 전용 합성 규칙을 추가했다.

## Work Completed

- composition v4에 `main-waist` 슬롯과 `Acc-Waist` category rule을 추가했다.
- 900×1200 canvas에서 허리 액세서리를 600×260, z-index 55로 표시하도록 설정했다.
- 평평하게 펼친 누끼를 허리에 감긴 체감 비율로 보여주기 위해 `Acc-Waist`에만 `fill` 합성 방식을 허용했다.
- 브라우저 합성과 로컬 Sharp preview 합성이 같은 `visualWidth`·`visualHeight`·`fit` 설정을 사용하도록 확장했다.
- `Acc-Waist`를 Outfit 계절 판정과 최근 구매 대표 Item에서 제외하고 추천 온도 가중치를 보조 액세서리 수준으로 낮췄다.
- composition·계절·추천·로컬 합성 규칙 테스트를 추가했다.
- 첨부된 블랙 페이크숄 누끼로 900×1200 로컬 미리보기를 만들어 허리 체감 비율을 확인했다.
- GitHub Pages에 `Acc-Waist` 지원 코드를 배포하고 공개 JavaScript 번들에서 `Acc-Waist`와 `main-waist` 규칙이 포함된 것을 확인했다.
- production Supabase의 `달뮤트 페이크숄 블랙`, `달뮤트 페이크숄 아이보리` category를 `Acc-Neck`에서 `Acc-Waist`로 변경하고 두 행을 재조회했다.

## Decisions

- category 식별자는 기존 형식과 맞춘 `Acc-Waist`를 사용한다.
- 실제 벨트와 허리에 묶는 숄을 역할이 같은 허리 액세서리로 확장할 수 있도록 제품명 대신 착용 위치를 category 기준으로 삼는다.
- 원본 누끼는 변경하지 않고 합성 단계에서만 비율을 조정한다.
- 레이어는 하의 30·상의 50보다 앞이고 아우터 60·가방 80보다 뒤인 55를 사용한다.
- production의 두 Item을 먼저 변경하면 기존 공개 앱에서 합성 규칙을 찾지 못하므로, 코드 배포와 공개 번들 확인을 마친 뒤 category를 변경했다.

## Current Status

구현과 production 반영을 완료했다. 전체 Vitest 37개 파일·162개 테스트, production build, Pages artifact 검증과 GitHub Pages 배포가 통과했다. 공개 번들은 새 category 규칙을 포함하고, production Supabase에는 `Acc-Waist` Item이 정확히 두 개 있으며 모두 대상 페이크숄이다.

## Next Steps

- 실제 모바일/PWA Lookbook에서 두 페이크숄의 허리 배치를 사용하면서 체감 크기나 위치에 추가 보정이 필요한지 관찰한다.

## Work Summary - Acc-Waist 원본 비율 복원

실사용 화면에서 페이크숄 누끼가 `600×260` 상자를 강제로 채우면서 세로로 눌리고 가로로 늘어나는 문제가 확인됐다. 정확한 크기와 위치를 판단할 수 있도록 강제 변형을 제거했다.

## Work Completed - Acc-Waist 원본 비율 복원

- `Acc-Waist`의 `visualHeight: 260`과 `fit: fill` 규칙을 제거하고 최대 너비 600px만 유지했다.
- 브라우저와 로컬 Sharp 합성 모두 원본 이미지 metadata의 가로세로 비율을 유지하는 `inside` 계산만 사용하도록 통일했다.
- 672×704 누끼가 기본 배치에서 600×628.57로 계산되는 회귀 테스트를 추가했다.
- 이미지 규격과 배치 계획 문서에서 600×260 강제 변형 예외를 삭제했다.
- 미수정 `Acc-Waist`의 placement 기본값을 좌우 -100px·상하 0px·크기 70%로 설정했다.

## Decisions - Acc-Waist 원본 비율 복원

- 착용했을 때의 체감 형태를 코드가 추정해 누끼를 변형하지 않는다.
- 위치와 크기 조정은 원본 비율이 보존된 결과를 기준으로 하고, 필요한 보정은 기존 Outfit별 위치·크기 값으로 수행한다.
- 새 placement 기본값은 저장값이 없는 Outfit에만 적용하고 기존에 저장한 위치·크기는 덮어쓰지 않는다.

## Current Status - Acc-Waist 원본 비율 복원

코드와 문서는 로컬에서 수정했고 전체 Vitest 37개 파일·162개 테스트와 production build가 통과했다. 아직 커밋·푸시·배포하지 않았다.

## Next Steps - Acc-Waist 원본 비율 복원

- 검증 후 실제 Lookbook에서 비율이 복원된 페이크숄의 크기와 위치를 다시 조정한다.

## Work Summary - 상위 카테고리 UI와 상세 카테고리 통계

Notion에서 이전한 28개 상세 카테고리는 품목 종류별 통계를 위한 정보로 보존하되, Closet과 향후 Outfit 만들기 화면에서는 옷을 한눈에 고를 수 있도록 상위 카테고리 그룹으로 단순화했다.

## Work Completed - 상위 카테고리 UI와 상세 카테고리 통계

- production Supabase의 상세 카테고리 28개와 사용 중·Retired 수량을 읽기 전용으로 확인했다.
- 상세 category 문자열을 변경하지 않고 선택 UI에는 `Outer → Top → Bottom → Dress → Shoes → Bag → Acc` 7개 상위 그룹만 표시하는 공통 규칙을 추가했다.
- `Socks`는 독립 그룹 대신 `Acc`에 포함하고, 독립 `Innerwear` Item은 Closet과 향후 Outfit 선택 대상에서 제외했다.
- Closet 카테고리 필터가 상세값 대신 현재 Item에 존재하는 7개 상위 그룹만 지정 순서로 표시하도록 변경했다.
- 기존 상세 카테고리 필터 저장값과 충돌하지 않도록 Closet 필터 저장 키를 v2로 분리했다.
- More의 Statistics 화면에 상위 그룹별 전체·사용 중 수량과 상세 category별 수량을 추가했다.
- 현재 production의 Top 상세 category 7개가 모두 `Top`에 포함되는지, `Socks`가 `Acc`에 포함되는지, 독립 `Innerwear`는 Statistics에만 남는지 검증하는 테스트를 추가했다.
- 375px demo 화면에서 `Top` 필터 결과와 Statistics의 상세 카테고리 목록, 가로 overflow 부재를 확인했다.

## Decisions - 상위 카테고리 UI와 상세 카테고리 통계

- Supabase schema와 `closet_items.category` 값은 변경하지 않는다.
- 상세 category는 composition·검색·통계용 원본 정보이고, 상위 그룹은 탐색과 Item 선택을 단순화하는 파생 UI 값으로 취급한다.
- 독립 `Innerwear`와 미래의 알 수 없는 category는 선택 필터에 추가하지 않되 Statistics에서는 `Innerwear`·`Other`로 집계해 원본 정보를 잃지 않는다.
- `Top-T-shirts-innerwear`는 상세 배치 규칙을 유지하면서 UI에서는 다른 Top 6개와 함께 `Top`에 표시한다.
- 아직 P3-4 Outfit 만들기 화면은 구현되지 않았으므로, 이번 공통 그룹 함수를 해당 화면에서 그대로 재사용하도록 계획 계약을 갱신했다.

## Current Status - 상위 카테고리 UI와 상세 카테고리 통계

로컬 코드·테스트·문서 변경을 완료했고 전체 Vitest 39개 파일·169개 테스트와 production build가 통과했다. 지정한 7개 그룹 순서, `Socks → Acc`, 독립 `Innerwear`의 선택 UI 제외와 Statistics 유지 경계를 테스트로 확인했다. Supabase는 읽기 전용 집계만 수행했으며 데이터와 schema는 변경하지 않았다.

## Next Steps - 상위 카테고리 UI와 상세 카테고리 통계

- J의 요청이 있으면 기존 Acc-Waist 비율·기본값 변경과 함께 배포한다.
- P3-4 Outfit 만들기 화면을 구현할 때 같은 상위 카테고리 그룹 선택지를 연결한다.

## Work Summary - Outfit별 T-shirt 표시 방식

같은 T-shirt도 Outfit에 따라 아우터 안에 겹쳐야 하거나 오른쪽에 분리해 전체 형태를 보여야 해서 category만으로 결정하는 공통 규칙으로는 예외를 표현할 수 없었다. Item category는 유지하고 Outfit–Item relation마다 `자동`, `아우터 안`, `옆에 분리` 표시 방식을 선택·저장하도록 확장했다.

## Work Completed - Outfit별 T-shirt 표시 방식

- Outfit 상세의 `착장 이미지 수정` 편집기에 `Top-T-shirts` 계열용 표시 방식 선택 UI를 추가했다.
- 표시 방식을 바꾸면 실시간 composition 미리보기와 해당 방식의 기본 위치·크기가 함께 갱신되도록 했다.
- `자동`은 기존 category·Outer 존재 규칙, `아우터 안`은 주열의 아우터 아래 레이어, `옆에 분리`는 오른쪽 보조열을 사용한다.
- 저장 입력과 Demo·Supabase repository를 위치 전용에서 placement 전체 저장으로 확장해 `slot`, `position_x`, `position_y`, `scale`, `z_index`를 함께 갱신한다.
- 기존 workspace member UPDATE RLS 아래에서 `slot`·`z_index` column-level 권한만 추가하는 migration과 pgTAP 계약을 작성했다.
- 일부 Item만 누끼가 있는 Outfit도 이미지가 준비된 Item부터 편집할 수 있도록 상세 화면 진입 조건을 완화했다. 완성된 저장 preview와 이미지 없는 relation은 그대로 유지한다.
- composition, 편집기 상호작용, Context 낙관적 갱신, Demo 저장, Supabase update query 테스트를 추가·갱신했다.

## Decisions - Outfit별 T-shirt 표시 방식

- 표시 방식은 Item의 고정 속성이 아니라 같은 Item도 Outfit마다 달라지는 relation 속성이다. category를 추가하거나 변경하지 않는다.
- 새 slot 값을 만들지 않고 기존 허용값을 재사용한다. `옆에 분리`는 `slot = top`, `z_index = 0`, `아우터 안`은 category에 따라 `slot = inner/top`, `z_index = 40/50`, `자동`은 두 값을 `NULL`로 저장한다.
- 사용자가 방식을 바꾸면 이전 방식의 위치·크기를 억지로 유지하지 않고 새 방식의 학습된 기본값으로 재설정한 뒤 4px·5% 단위로 미세 조정하게 한다.
- category 기본 규칙은 새 Outfit의 안전한 초기값으로 계속 유지하고, 명시적으로 저장된 relation placement가 그 기본값보다 우선한다.
- 새 migration은 production에 아직 적용하지 않는다. 앱 배포 전에 migration과 pgTAP 검증을 먼저 완료해야 한다.

## Current Status - Outfit별 T-shirt 표시 방식

로컬 TypeScript 검사와 전체 Vitest 39개 파일·172개 테스트가 통과했다. production build와 GitHub Actions 조건의 `/closet-index/` Pages build·404 fallback 검증도 통과했다. Browser 데모 QA에서 `자동 → 아우터 안 → 옆에 분리` 미리보기 전환, 저장 후 새로고침 유지, 375px 가로 overflow 부재, 콘솔 오류 부재를 확인했다. Docker가 없는 환경이므로 새 pgTAP은 로컬 DB에서 실행하지 않았고 production Supabase 데이터·권한은 변경하지 않았다.

Pages 검증 중 일반 로컬 build는 의도대로 `/` base를 사용해 `/closet-index/` 계약 검사에 실패했다. `GITHUB_ACTIONS=true`로 CI 환경을 재현하고 `index.html`을 `404.html`로 복사한 뒤 검증이 통과했다.

## Next Steps - Outfit별 T-shirt 표시 방식

- 배포할 때 먼저 migration을 격리 검증하고 production에 적용한 뒤 앱을 배포한다.
- P3-4 새 Outfit 만들기 화면에서도 같은 세 가지 표시 방식과 placement 저장 모델을 재사용한다.
- 실제 누끼가 있는 `Top-T-shirts-innerwear`와 일반 `Top-T-shirts` Outfit에서 기본값이 체감상 맞는지 확인하고 필요하면 category 기본 위치만 조정한다.

## Work Summary - 착장 위치 편집기 반응형 배치

PC의 넓은 화면에서는 착장 미리보기 옆의 여백을 조작부에 활용하고, 모바일에서는 방향·크기 버튼을 미리보기와 더 가깝게 유지하도록 편집기 배치와 조작 순서를 조정했다.

## Work Completed - 착장 위치 편집기 반응형 배치

- 761px 이상 화면에서 착장 미리보기는 왼쪽, 선택한 Item의 위치·크기·표시 방식 조작부는 오른쪽에 보이는 2열 Grid를 추가했다.
- 아이템 선택 썸네일은 두 열 아래 전체 폭에 유지해 이름과 누끼가 지나치게 작아지지 않도록 했다.
- 모바일에서는 기존 1열 배치를 유지하면서 `상하좌우·크기 조작 → 표시 방식 → 저장` 순서로 변경했다.
- 표시 방식 선택이 방향 버튼 뒤에 오는 DOM 순서를 회귀 테스트에 추가했다.

## Decisions - 착장 위치 편집기 반응형 배치

- Supabase placement 저장 모델과 상태 처리에는 변화가 없는 순수 반응형 UI 변경으로 제한했다.
- PC 2열 전환 기준은 앱 프레임의 최대 너비가 적용되는 761px로 맞췄다.
- 모바일에서 가장 자주 반복하는 위치·크기 조작을 표시 방식보다 먼저 배치해 미리보기와의 시선 이동 거리를 줄였다.

## Current Status - 착장 위치 편집기 반응형 배치

코드와 회귀 테스트를 완료했다. 전체 Vitest 39개 파일·172개 테스트, TypeScript 검사, production build와 `git diff --check`가 통과했다. 1280×720 데모 브라우저에서 390px 미리보기와 278px 조작부가 나란히 표시되고 가로 overflow가 없음을 확인했다. `아우터 안` 선택 후 위로 4px 이동 시 좌표가 52px에서 48px로 바뀌고 저장 버튼이 활성화됐으며 콘솔 오류는 없었다. 모바일은 기존 1열 DOM 흐름과 761px breakpoint를 유지하며 조작 패드가 표시 방식보다 앞에 오는 순서를 단위 테스트로 확인했다.

## Next Steps - 착장 위치 편집기 반응형 배치

- 실제 모바일/PWA에서 방향·크기 조작부와 표시 방식 사이의 체감 간격을 확인하고 필요하면 작은 화면의 gap만 추가 조정한다.

## Work Summary - Outfit 설정 저장 403 복구

표시 방식 기능이 배포된 뒤 production에서 `이 설정 저장` 요청이 실패했다. 앱은 기존 위치·크기 값에 더해 `slot`과 `z_index`를 함께 갱신하지만, 해당 두 컬럼의 production UPDATE 권한 migration이 적용되지 않은 상태였다.

## Work Completed - Outfit 설정 저장 403 복구

- Supabase API 로그에서 `closet_outfit_items` PATCH 요청이 403으로 실패한 사실을 확인했다.
- production 권한을 조회해 `position_x`·`position_y`·`scale`은 UPDATE 가능하지만 `slot`·`z_index`는 불가능한 상태임을 확인했다.
- `allow_outfit_item_display_mode_updates` migration을 production에 적용해 authenticated 역할에 `slot`·`z_index` 컬럼 UPDATE 권한만 추가했다.
- 적용 후 다섯 placement 컬럼의 UPDATE 권한이 모두 활성화되고 broad table UPDATE는 계속 비활성인 것을 재조회했다.
- `closet_outfit_items` RLS와 workspace member UPDATE policy의 `USING`·`WITH CHECK`가 유지되는지 확인했다.
- 원격 migration version `20260731004807`에 맞춰 로컬 migration 파일명을 정렬했다.
- 적용 후 Security·Performance Advisor를 다시 실행했다.

## Decisions - Outfit 설정 저장 403 복구

- 오류를 우회하는 privileged RPC나 broad table UPDATE를 추가하지 않고, 기존 workspace membership RLS 아래에서 필요한 두 컬럼만 최소 권한으로 허용했다.
- Outfit 데이터는 직접 수정하지 않고 schema 권한만 변경했다.
- Advisor의 기존 다른 프로젝트·기능 경고는 이번 최소 권한 migration과 무관하므로 범위를 넓혀 수정하지 않았다.

## Current Status - Outfit 설정 저장 403 복구

production migration `20260731004807_allow_outfit_item_display_mode_updates` 적용을 완료했다. `slot_update = true`, `z_index_update = true`, `table_update = false`, `rls_enabled = true`를 확인했으며 저장 요청을 막던 403 원인은 제거됐다.

## Next Steps - Outfit 설정 저장 403 복구

- 공개 앱에서 같은 Outfit의 설정 저장을 다시 눌러 성공 메시지와 새로고침 후 값 유지를 확인한다.

## Work Summary - 빈 Outfit 정리와 새 Outfit 만들기

설정 저장 권한 복구가 실사용에서 확인된 뒤 Phase 3의 다음 순서인 P3-4 새 Outfit 만들기를 진행했다. 동시에 Item relation·착용 기록·미리보기가 전혀 없는 production Outfit 한 건의 출처와 안전성을 확인해 정리했다.

## Work Completed - 빈 Outfit 정리와 새 Outfit 만들기

- 빈 Outfit은 앱이 임의 생성한 행이 아니라 `notion_page_id`와 `notion_created_at`이 있는 실제 Notion 원본 행임을 확인했다.
- Outfit–Item relation, Wear Log, preview가 모두 0개이고 대상 Notion page ID가 일치하는 조건을 걸어 해당 production 행 한 건만 삭제한 뒤 별도 조회로 잔존 0건을 확인했다.
- LOOKBOOK 상단에 `새 Outfit` 진입을 추가하고 선택 요약, 실시간 composition, Item 검색·필터·추가·제거, 이름과 저장 검토 화면을 구현했다.
- UI에는 상위 카테고리 7개를 지정 순서로 표시하고 `Socks → Acc`, 독립 `Innerwear` 제외 규칙을 CLOSET과 동일하게 적용했다.
- 이미지가 있는 Item과 없는 Item을 함께 저장할 수 있게 하고, 이미지 Item별 위치·크기와 T-shirt의 `자동`·`아우터 안`·`옆에 분리` 표시 방식을 draft에서 조정하도록 연결했다.
- 같은 Item 조합은 기존 Outfit을 먼저 안내하며 명시적으로 확인한 경우에만 별도 저장하도록 했다.
- 기존의 원자적 `create_closet_outfit` 저장 계약과 클라이언트 생성 UUID를 재사용했으며, 저장 성공 결과를 전체 재조회 없이 Context에 즉시 추가하고 새 상세로 이동하도록 했다.
- 혼합 이미지 저장, 중복 조합 확인, Innerwear·Socks 경계와 새 Outfit의 즉시 Context 반영 테스트를 추가했다.
- 전체 테스트를 병렬 실행할 때 기존 Wear Log 테스트가 초기 effect 완료 전에 온도를 바꿔 간헐적으로 실패한 경로를 확인하고, 예보 기본값 24가 실제 입력에 반영된 뒤 변경하도록 테스트 대기 조건을 명시했다.

## Decisions - 빈 Outfit 정리와 새 Outfit 만들기

- 빈 Notion 원본 Outfit은 연결 데이터가 전혀 없어 앱 기능에는 영향을 주지 않지만, 의미 없는 Lookbook 행을 남기지 않도록 넓은 삭제가 아닌 식별자와 무참조 조건을 모두 만족한 한 건만 정리했다.
- 기존 Outfit의 Item relation은 수정하지 않는다. 구성 변경은 새 UUID의 Outfit 생성으로만 처리하며, 원본 수정 흐름은 다음 P3-5의 복제 기능에서도 같은 원칙을 유지한다.
- 위치 조정은 저장 전 로컬 draft로 유지하고 최종 저장 한 번에 relation 전체를 함께 생성한다. 따라서 중간 편집이 production에 부분적으로 남지 않는다.
- 새 Outfit 생성에는 이미 검증된 database function을 사용하므로 이번 단계에서 schema나 Supabase 권한을 추가로 넓히지 않는다.
- 새 Outfit의 초기 평가는 미입력이고 Wear Log는 실제 착용 기록 시점에만 별도로 생성한다.

## Current Status - 빈 Outfit 정리와 새 Outfit 만들기

production의 빈 Outfit 한 건은 안전 조건을 확인한 뒤 삭제했고 잔존하지 않는다. P3-4 기능은 로컬 코드와 테스트로 구현했으며 전체 Vitest 40개 파일·176개 테스트, production build와 GitHub Pages artifact 검증이 통과했다. PC에서는 미리보기와 조작부가 나란히, 375px 모바일에서는 세로로 배치되고 가로 overflow와 콘솔 오류가 없음을 확인했다. production 앱 배포와 실제 Supabase에서 새 Outfit 한 건을 저장하는 최종 검증은 아직 수행하지 않았다.

## Next Steps - 빈 Outfit 정리와 새 Outfit 만들기

- 공개 반영 전에 변경 파일 범위를 확인하고, J의 요청이 있으면 커밋·푸시·배포한 뒤 실제 계정에서 새 Outfit 생성과 즉시 상세·Lookbook 반영을 검증한다.
- 다음 구현 순서는 P3-5 기존 Outfit 복제와 보관·복원 연결이다.

## Work Summary - Outfit 복제와 보관·복원

새 Outfit 직접 등록이 실사용에서 정상 동작하는 것을 확인한 뒤, 기존 Outfit을 안전한 초안으로 가져와 Item 구성을 바꿔 새 착장으로 저장하는 P3-5를 구현했다. 잘못 만든 Outfit은 삭제하지 않고 기본 노출에서 숨겼다가 복원할 수 있도록 별도 보관 상태를 연결했다.

## Work Completed - Outfit 복제와 보관·복원

- Outfit 상세에 `이 착장으로 새로 만들기` 진입과 착장 관리 영역을 추가했다.
- 원본 Outfit의 Item 순서, 위치·크기, slot·z-index와 T-shirt 표시 방식을 새 Outfit 만들기 화면의 draft 초깃값으로 복사했다.
- 복제 화면에서 Item을 추가·제거하거나 배치를 바꾼 뒤 새 UUID와 기존 원자적 생성 function으로 저장하도록 했다.
- 원본과 Item 조합이 같으면 기존 동일 조합 경고를 그대로 거치며, 원본 링크를 복제 안내에 함께 표시했다.
- Outfit 상세에 별도 확인이 필요한 보관과 즉시 복원을 추가하고, 보관 중에는 `오늘 입기`를 숨겼다.
- 보관 Outfit을 기본 Lookbook과 추천 후보에서 제외하고 `Error·Retired·보관 포함` 필터를 켜면 다시 표시하도록 했다.
- Context는 전체 데이터를 다시 읽지 않고 저장·보관 결과를 즉시 반영하도록 확장했다.
- Supabase repository의 workspace 한정 `archived_at` update, 복제 초안의 placement 복사, 원본 독립성, Wear Log 유지와 추천 제외 테스트를 추가했다.

## Decisions - Outfit 복제와 보관·복원

- 복제 버튼을 누르는 즉시 DB 행을 만들지 않는다. 저장 전 수정과 취소가 production에 빈 복제본을 남기지 않도록 모든 변경을 로컬 draft에 보관하고 마지막에 한 transaction으로 생성한다.
- Item 조합은 Outfit의 동일성이므로 기존 relation은 계속 불변으로 유지한다. 위치·크기 보정만 기존 Outfit relation에서 허용하고 구성 변경은 항상 새 UUID로 저장한다.
- 보관은 평가 `Error`와 분리한다. `Error`는 실제 착용 만족도이고 `archived_at`은 관리 상태이므로 서로 덮어쓰지 않는다.
- 보관된 Outfit의 Wear Log는 통계·달력 기록으로 보존하되 새 추천과 기본 Lookbook에는 사용하지 않는다.
- 현재 production에 이미 적용된 `archived_at`, workspace UPDATE RLS와 원자적 생성 function을 재사용하므로 새 schema나 권한 migration은 만들지 않는다.

## Current Status - Outfit 복제와 보관·복원

P3-5 코드는 로컬에서 구현했다. 전체 Vitest 41개 파일·182개 테스트, TypeScript 검사, production build와 GitHub Pages artifact 검증이 통과했다. Demo Browser에서 상세 → 복제 초안 → Item 제거 → 새 UUID 저장과 보관 → 기본 Lookbook 숨김 → 명시적 포함 → 복원 흐름을 확인했다. PC 1200px·모바일 375px 모두 가로 overflow와 콘솔 오류가 없었다. Browser runtime에는 별도 screenshot API가 없어 DOM snapshot·계산 layout·상호작용 상태로 검증했다.

## Next Steps - Outfit 복제와 보관·복원

- 공개 반영 후 실제 계정에서 기존 Outfit 하나를 복제해 Item을 교체하고 원본 관계·평가·Wear Log가 그대로인지 확인한다.
- 보관·복원을 실제 계정에서 한 번 실행해 기본 Lookbook과 추천 노출 상태를 확인한다.
- 다음 구현 순서는 P3-6 preview 생성·캐시와 관리 상태다.

## Work Summary - 핵심 Item별 시험 착장 온도 근거

미착용 Outfit을 과거 Outfit 한 벌과만 비교하던 기존 방식 때문에, 서로 다른 착장에 나뉘어 있는 상의와 하의의 OK 온도 기록을 새 조합의 근거로 쓰지 못했다. 과거에 같은 Item을 입은 모든 Outfit에서 핵심 Item별 온도를 모아 공통 구간을 만들고 새 조합의 시험 근거로 사용하도록 확장했다.

## Work Completed - 핵심 Item별 시험 착장 온도 근거

- 상의·아우터·하의·원피스·이너 계열을 핵심 온도 Item으로 보고, 미착용 Outfit의 각 Item이 포함된 과거 Wear Log에서 OK 온도를 모으도록 했다.
- Item별 기존 ±2°C OK 범위를 계산한 뒤, 최소 두 핵심 Item의 범위가 겹치면 교집합을 새 조합의 시험 범위로 사용하도록 했다. 핵심 Item이 하나인 Dress형 조합은 해당 한 Item 근거를 허용한다.
- 전체 Outfit 유사도 근거는 유지하되 Item별 공통 구간을 우선 사용하고, 카드와 상세에서 근거 종류·범위·관측 수를 구분해 표시했다.
- 오해를 낳던 설명을 고쳐 상의도 가중치 2로 반영되고 아우터·하의·원피스는 가중치 3이라는 실제 규칙을 명시했다.
- 착용 0회 Outfit은 구매일이 있어도 최근 구매 영역으로 이동시키지 않고 `새 착장 시험해보기`에 유지하도록 분류 우선순위를 정리했다.
- 서로 다른 두 과거 Outfit에서 상의와 하의 근거를 가져와 `23~26°C` 공통 구간을 만드는 회귀 테스트를 추가했다.

## Decisions - 핵심 Item별 시험 착장 온도 근거

- 가방·신발·액세서리는 새 조합의 핵심 체감 온도를 확정하는 Item별 교집합에서 제외하고, 기존 Outfit 유사도 보조 가중치만 유지한다.
- 서로 다른 Item 범위를 단순 합집합으로 넓히지 않고 교집합만 사용한다. 모든 지원 Item이 함께 편안했을 가능성이 있는 온도만 시험 범위로 제시하기 위해서다.
- 이 근거는 직접 입어본 조합의 기록이 아니므로 `추천 높음`으로 승격하지 않고 계속 `시험 착장`과 부분 근거 수준으로 표시한다.
- `OK 2회` 같은 표시는 Wear Log 횟수가 아니라 출발·귀가의 OK 온도 관측 수일 수 있으므로, 시험 근거 화면에서는 `OK 관측 N개`로 표현한다.

## Current Status - 핵심 Item별 시험 착장 온도 근거

전체 Vitest 41개 파일·183개 테스트와 production build가 통과했다. 실데이터를 읽기 전용으로 사용한 Browser QA에서 25°C 입력 시 Outfit `6ded14bc-c26d-4248-9465-ca9b4c9f0c45`가 `적정 온도 미확인`에서 빠지고 `새 착장 시험해보기`에 표시되는 것을 확인했다. 상세에는 달뮤트 요거트 스커트 `19~26°C`, 마하그리드 고양이 티셔츠 `23~27°C`, 공통 시험 범위 `23~26°C`, OK 관측 3개가 표시됐으며 PC와 390×844 모바일에서 overflow나 콘솔 오류가 없었다. production Supabase 데이터와 schema는 변경하지 않았고 코드는 아직 커밋·푸시·배포하지 않았다. build에는 기존 500kB 초과 chunk 경고만 남아 있다.

## Next Steps - 핵심 Item별 시험 착장 온도 근거

- J의 요청이 있으면 변경 파일 범위를 다시 확인한 뒤 커밋·푸시·배포하고 공개 앱에서 같은 25°C 흐름을 재검증한다.
- 실제 착용 후 첫 Wear Log가 생기면 Item별 부분 근거 대신 직접 Outfit 근거로 자동 전환되는지 실사용에서 확인한다.
