# 2026-08-27 Closet Index Development Log

## Work Summary

Legacy Link Wave 3의 production 적용 전 안전망을 완성한 뒤 명시적 승인을 받아 cleanup migration까지 적용했다. 기존 계보 edge 119개의 의미는 그대로 보존하고 과도기 source metadata와 Legacy review subsystem만 제거했으며, 전체 schema rollback과 local-only production data restore는 긴급 복구 자료로 분리해 유지한다.

## Work Completed

- Supabase CLI로 생성한 migration을 remote history version에 맞춰 `20260827122033_remove_replacement_legacy_link_subsystem.sql`로 정렬하고 CI workflow 경로와 rollback 주석도 같은 version으로 맞췄다. 명시적 transaction, 5초 lock timeout, 대상 table lock, drift preflight 본문은 그대로 유지했다.
- migration은 Legacy 출처 edge의 12개 의미 field를 전환 전에 고정하고 `source_kind`·`source_legacy_link_id`만 manual 상태로 바꾼 뒤 row count와 전체 의미가 같은지 재검증한다.
- Legacy invalidation trigger/helper와 review·revise·confirm RPC를 제거하고 edge validator, manual create, connection update, reverse RPC를 Legacy table·source column 없는 구현으로 교체한다. 이후 source FK·constraint·index·column과 revision·Link table을 exact order로 제거한다.
- `remove_replacement_legacy_link_subsystem_rollback.sql`에 Legacy 두 table, constraint·index·RLS·grant·comment, source column, trigger와 source-aware RPC를 복구하는 schema rollback을 작성했다. 기존 post-cleanup edge는 manual로 초기화한 뒤 historic `legacy_link` default를 복원해 data restore 전에도 constraint를 만족하게 했다.
- ignored export 폴더에 Link 49개·revision 51개·source association 18개를 복구하는 local-only SQL과 생성기를 만들었다. 실행 전 빈 대상 table과 18개 edge의 12개 의미 field를 확인하고 source field만 재연결하며, 생성 SQL SHA-256 `21efde1462be54ad224110d1dae87a7f1e5e6565cfd751fa58a32b00144e383d`가 재계산과 일치했다.
- Legacy edge 1개·manual edge 1개 synthetic pre-cleanup fixture와 cleanup 37개 assertion, rollback·synthetic restore 39개 assertion을 작성했다. 삭제된 Legacy table·RPC를 현재 계약으로 요구하던 과거 Phase 4 pgTAP 세 개는 제거하고 적용된 migration 이력은 유지했다.
- 기존 Phase 3 workflow에 별도 cleanup round-trip job을 추가했다. run `32989627364`에서 cleanup 뒤 새 계약과 retained Lineage 6개 파일·101 assertions, rollback·data 재연결 39 assertions가 통과했다. 병렬 baseline job의 8개 파일·137 assertions와 Import Runs rollback 14 assertions도 통과했다.
- 첫 격리 run `32989293888`에서는 cleanup migration과 새 37 assertions가 통과했지만, 기존 decision-reason test가 같은 네 값을 한 줄 함수 정의 문자열로 비교해 여러 줄 포맷을 실패로 판단했다. 의미를 바꾸지 않고 기존 production 한 줄 표현을 유지해 재실행을 통과시켰다.
- 로컬 Vitest는 93개 파일 통과·6개 skip, 638개 test 통과·7개 skip이었다. TypeScript 포함 production build와 workflow 순서의 `404.html` 생성 후 Pages artifact 검증도 통과했다.
- `database-cleanup-plan.md`, `database-map.md`, `replacement-line-repository.md`에 준비·검증 완료와 당시 production 미적용 경계를 먼저 기록하고, 실제 적용 뒤 16-table post-cleanup inventory와 완료 결과로 갱신했다.
- 검증 브랜치의 네 commit을 `main`에 fast-forward하고 commit `9707780`까지 push했다. main 기준 Pages run `32990305262`에서 전체 test, build, SPA fallback, artifact 검증과 deploy가 모두 성공했다.
- production 적용 직전 Link 49개, revision 51개, edge 119개(manual 101개·Legacy 18개), integrity 0건과 전체·Legacy 의미 SHA-256이 export 기준선과 같은지 재확인했다. local-only export 6개, manifest와 restore SQL checksum도 모두 일치했다.
- 첫 적용 요청은 구체적인 운영 삭제 승인이 부족하다는 안전 게이트에서 거부됐다. 거부 직후 migration 이력 부재와 49·51·18행 유지 상태를 재확인하고 우회하지 않았으며, J의 명시적 삭제 승인 뒤 같은 migration을 다시 실행했다.
- remote migration `20260827122033_remove_replacement_legacy_link_subsystem`을 production에 적용했다. transaction 안에서 18개 edge의 source field만 제거하고 12개 의미 field를 재검증한 뒤 Legacy table 두 개, source column·constraint·index, Legacy RPC·trigger/helper를 제거했다.
- 적용 후 edge 119개 의미 SHA-256 `cd7fb6c8edc608828193c9f9d3a4bea1f5814978b1c61b73b3ec8ece52caea83`, Line 66개, membership 194개, start 50개가 유지됐다. Legacy 관련 table·column·함수·trigger·constraint·index와 함수 definition 참조는 모두 0개다.
- 유지한 validator·manual create·connection update·reverse 함수는 Legacy 참조 0건, 빈 `search_path`, anon 실행 불가와 의도한 authenticated·service-role 권한을 확인했다. `public.closet_*` table 16개는 모두 RLS가 켜져 있고 적용 직후 lock wait·blocking session은 0건이었다. Postgres 로그의 적용 이후 8개 항목에는 ERROR·FATAL·PANIC이 0건이고 관련 항목은 실행 SQL을 남긴 정상 `LOG` 1건뿐이었다.
- Security Advisor는 47건에서 43건, Performance Advisor는 33건에서 29건으로 줄었다. 각 4건의 감소는 제거한 Legacy RPC와 table index 관련 항목이며 새 Legacy 관련 항목은 0건이다.

## Decisions

- production cleanup은 edge 전환과 DDL을 하나의 transaction으로 묶는다. 필요한 lock을 5초 안에 확보하지 못하거나 preflight·의미 보존 검증이 하나라도 실패하면 전체 작업을 rollback한다.
- `IF EXISTS`와 `CASCADE`를 사용하지 않는다. 예상 object·dependency가 drift했다면 migration이 조용히 일부만 진행되지 않고 중단돼야 한다.
- source 두 column은 모든 edge가 독립 edge가 된 뒤 역사 보존용 nullable field로 남기지 않는다. 과거 판단은 검증된 local-only export가 보존하고 현재 graph의 source of truth는 `closet_replacement_line_edges` 하나로 유지한다.
- rollback은 schema와 개인 data를 분리한다. 추적 가능한 SQL에는 구조와 권한만 두고 production row는 Git ignore 자료로만 복구한다.
- `SECURITY DEFINER` 함수는 빈 `search_path`를 고정하고 public·anon 권한을 제거하며 authenticated·service_role의 의도한 execute만 명시한다.
- 운영 적용 전 단계에서는 read-only 재감사만 허용하고, 실제 삭제는 대상·행 수·복구 경로를 명시한 승인 뒤에만 수행한다. 격리 검증 성공과 production 적용 완료는 계속 구분한다.

## Current Status

- cleanup migration은 production 적용·검증 완료다. schema rollback, local-only data restore와 synthetic round-trip 자료는 보존 중이다.
- frontend 전체 회귀, TypeScript, production build와 Pages artifact 검증은 통과했다.
- production에는 Legacy Link·revision table, Legacy-aware RPC·trigger와 edge source column이 없다. 계보 edge 119개는 모두 confirmed이며 의미 checksum이 적용 전과 같다.
- cleanup 준비·검증 코드는 `main`에 push되고 Pages 배포도 완료됐다. production remote migration history에도 `20260827122033`이 기록됐다.
- GitHub Actions의 Node 20 deprecation warning은 `actions/checkout@v4`와 `supabase/setup-cli@v1` 실행 환경 안내이며 이번 SQL 계약 실패는 아니다.

## Next Steps

- 긴급 복구가 필요할 때만 schema rollback을 먼저 적용하고, ignored restore SQL checksum을 다시 확인한 뒤 49개 Link·51개 revision·18개 source association을 복구한다.
- 실제 production UI mutation은 새 실데이터를 만들지 않기 위해 이번 적용 검증에서 실행하지 않았다. 다음 일반 사용에서 Lineage create/edit/reverse/disconnect 오류가 관찰될 때만 로그와 optimistic-lock 입력을 함께 진단한다.
- Legacy export와 rollback 자료는 보존하되 현재 schema나 frontend에 dormant Legacy 구조를 다시 추가하지 않는다.
