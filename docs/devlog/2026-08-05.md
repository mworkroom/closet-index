# 2026-08-05 Closet Index Development Log

## Work Summary

계보에 연결하기 전 Item을 잘못 묶인 Replacement Line에서 분리해 기존 Line 또는 새 Line으로 옮길 수 있도록 Line 관리의 첫 단계를 구현했다. 계보 이력을 보존하면서 membership 변경을 안전하게 검토할 수 있는 상태를 만드는 것이 목적이다.

## Work Completed

- `closet_replacement_lines`에 `review_status`, `updated_at`을 추가하고 연결 없는 Item의 membership 이동, 새 Line 생성, 시작점 지정, source·target Line 재검토 전환을 한 transaction에서 수행하는 migration과 RPC를 작성했다.
- React 저장소 계약, Supabase mapping, Demo localStorage 상태를 확장했다.
- `계보 연결 전` Item 카드에 `다른 Line으로 옮기기` 폼을 추가하고 기존 Line 선택과 새 Line 이름·Style Identity 입력을 지원했다.
- 이동 후 대상 Line으로 즉시 전환하고 G0 시작점 및 `재검토 필요` 알림을 표시했다. Line 목록 카드에도 재검토 배지를 연결했다.
- 타입 검사, 4개 집중 테스트 파일의 23개 테스트, 전체 61개 파일의 266개 테스트, production build를 통과했다.
- Demo 모드에서 데스크톱과 390×844 모바일의 이동 폼, 새 Line 생성, URL 전환, G0 표시와 콘솔 무오류를 확인했다.

## Decisions

- predecessor 또는 successor edge가 하나라도 남은 Item은 이동하지 않는다. archived edge까지 포함해 기록을 삭제하거나 다른 Line에 억지로 재배치하지 않기 위한 결정이다.
- 이동한 Item은 대상 Line의 명시적 시작점으로 지정하고 source·target Line 모두 `needs_review`로 되돌린다.
- 낙관적 동시성 검사를 위해 화면이 읽은 source·target `updated_at`을 RPC에서 다시 확인한다.
- production DB 변경은 별도 승인과 검증이 필요한 단계이므로 이번 작업에서는 migration 파일과 로컬 동작까지만 완료했다.

## Current Status

로컬 코드와 UI는 완료됐고 전체 테스트와 build, 데스크톱·모바일 브라우저 QA가 통과했다. production의 마지막 migration은 아직 `20260804010637_edit_replacement_line_connections`이며 새 Line 이동 migration은 적용하지 않았다. 따라서 production 앱에서는 아직 이 기능을 사용할 수 없다.

## Next Steps

- 승인 후 production migration을 적용하고 authenticated RPC 성공·권한 거부·rollback fixture를 검증한다.
- production 전후 Line·membership·edge·start count와 보안·성능 Advisor를 확인한다.
- 다음 Line 관리 단계에서 병합·보관·대표 Line과 재검토 완료 처리를 구현한다.
- Line 색상 category 직접 지정 기능을 이어서 구현한다.

## Work Summary - Line 이동 Production Migration

로컬에서 준비한 Replacement Line Item 이동 구조를 production Supabase에 적용하고, 실제 workspace member 권한에서 데이터 이동과 rollback 안전성을 검증했다. 운영 데이터의 Line·membership·계보 edge·시작점 수량을 바꾸지 않고 backend 계약을 준비했다.

## Work Completed - Line 이동 Production Migration

- production migration `20260804183905_move_replacement_line_items`를 적용하고 로컬 migration 파일명도 원격 버전에 맞췄다.
- `review_status`, `updated_at` column과 `move_closet_replacement_line_item` 함수의 생성 상태를 확인했다.
- 함수가 빈 `search_path`를 사용하는 `SECURITY DEFINER`이며 `authenticated`만 실행 가능하고 `public`·`anon`·`service_role`에는 실행 권한이 없음을 확인했다.
- 실제 workspace member JWT claim을 사용한 transaction fixture에서 기존 Line 이동과 새 Line 생성, membership 이전, 새 시작점 지정, source·target `needs_review` 전환을 검증하고 rollback했다.
- confirmed edge에 연결된 Item은 이동이 거절되는지 확인했다.
- rollback 뒤 production 수량이 `53 Line · 165 membership · 87 confirmed edge · 25 start · needs_review 0`으로 유지되고 Line·membership·edge·start checksum이 적용 전 기준선과 일치하는지 재확인했다.
- 보안·성능 Advisor를 확인했고 새 객체와 관련된 성능 경고는 없었다. 로그인 사용자용 `SECURITY DEFINER` 경고 1건은 앱에서 호출하는 전용 RPC 특성상 의도된 항목으로 검토했다.
- 전체 61개 테스트 파일의 266개 테스트와 production build, `git diff --check`를 통과했다. build의 기존 500 kB 초과 chunk 경고는 남아 있다.

## Decisions - Line 이동 Production Migration

- 쓰기 transaction 전체를 하나의 RPC에서 수행하고 함수 내부에서 `auth.uid()`와 workspace membership을 다시 검사하는 기존 Closet Index 경계를 유지했다.
- Advisor 경고를 없애기 위해 RPC를 공개하거나 invoker 함수로 바꾸지 않았다. 직접 table 쓰기를 열지 않고 최소 실행 권한과 고정 `search_path`를 유지하는 편이 현재 데이터 무결성 요구에 맞기 때문이다.
- 검증 fixture는 모두 transaction 안에서 실행한 뒤 rollback해 실제 Item의 Line 배치와 재검토 상태를 남기지 않았다.

## Current Status - Line 이동 Production Migration

production migration과 인증 RPC 검증이 완료되어 로컬 앱에서 연결 없는 Item을 기존 Line 또는 새 Line으로 옮길 backend 조건이 갖춰졌다. 운영 데이터에는 검증용 Line이나 membership 변경이 남지 않았고 Phase 4 계획서의 해당 `needs_review` production 검증 항목을 완료 처리했다. 프론트엔드 변경은 아직 commit·push·공개 배포하지 않았다.

## Next Steps - Line 이동 Production Migration

- Line 병합·보관·대표 Line과 재검토 완료 흐름을 구현한다.
- 자동 색상 분류보다 우선하는 Line 색상 category 직접 지정 기능을 구현한다.

## Work Summary - Line 병합·보관·대표 Line

Replacement Line을 지우지 않고 보관하거나 다른 Line에 병합해 대표 Line으로 정리할 수 있는 lifecycle 관리 기능을 로컬에 구현했다. 실제 production에는 같은 두 Item을 공유하는 Line pair가 있어, 겹치는 membership과 계보를 손상하지 않는 병합 계약을 먼저 확정했다.

## Work Completed - Line 병합·보관·대표 Line

- `closet_replacement_lines`에 active·archived 상태, 대표 Line 참조와 보관 시각을 추가하는 migration `20260804185930_manage_replacement_line_lifecycle`을 작성했다.
- 독립 보관·복원과 source Line을 target 대표 Line으로 병합하는 원자 RPC를 작성하고, archived Line의 membership·edge·start 직접 변경을 trigger로 차단했다.
- 병합 전에 동일 edge 충돌과 합친 graph의 cycle을 검사하고, 겹치는 membership은 중복 없이 보존하며 source·target Line을 `needs_review`로 전환했다.
- Supabase repository, DataContext와 Demo localStorage 계약에 lifecycle 쓰기와 이전 Demo 상태 정규화를 추가했다.
- Line 상세의 `Line 관리` 패널에 명시적 확인을 거치는 대표 Line 병합, 독립 보관·복원 UI를 추가했다. 병합된 Line은 읽기 전용으로 표시하고 대표 Line 링크를 제공한다.
- Color Index에서는 active Line만 표시하고, 접을 수 있는 관리 현황에서는 보관 Line과 대표 Line 연결을 계속 찾을 수 있게 했다.
- 병합 뒤 대표 Line으로 이동할 때 이전 source의 열린 폼 상태가 남는 브라우저 QA 문제를 발견해 Line id 기준으로 관리 패널을 새로 마운트하도록 수정했다.
- 집중 테스트 5개 파일 35개, 전체 61개 파일 275개 테스트와 production build를 통과했다. Demo에서 데스크톱과 390×844 모바일의 보관·복원·병합·대표 Line 이동, Color Index 제외·관리 목록 표시와 콘솔 무오류를 확인했다.

## Decisions - Line 병합·보관·대표 Line

- 병합은 삭제가 아니라 source를 대표 Line에 연결된 archived 상태로 남긴다. 잘못 묶었던 기록을 다시 찾을 수 있고 대표 계보의 출처도 잃지 않기 위한 결정이다.
- 이미 다른 archived Line의 대표인 Line을 독립 보관하지 못하게 하고, 병합 시 기존 대표 참조를 새 target으로 한 단계에 다시 연결해 대표 chain이 길어지지 않게 했다.
- 독립 보관 Line만 직접 복원할 수 있고, 병합된 Line은 자동 분할 복원을 제공하지 않는다. membership·edge·start를 안전하게 역분배할 근거가 없기 때문이다.
- RPC는 `SECURITY DEFINER`의 빈 `search_path`, fully qualified relation, 함수 내부 workspace membership 검사와 `authenticated` 최소 실행 권한을 사용했다.
- production 변경은 별도 승인과 rollback 검증이 필요한 경계이므로 이번 작업에서는 read-only 데이터 감사와 migration·UI의 로컬 구현까지만 진행했다.

## Current Status - Line 병합·보관·대표 Line

로컬 데이터 계약, Demo 동작, React UI와 회귀 검증은 완료됐다. production read-only 감사에서는 복수 Line 소속 Item 2개, 두 Item을 공유하는 Line pair 1개, cross-Line edge 충돌 0건을 확인했다. 로컬 Docker가 실행 중이지 않아 migration SQL을 로컬 Postgres에 직접 적용하지 못했고, 새 lifecycle migration도 production에는 적용하지 않았다. build의 기존 500 kB 초과 chunk 경고는 남아 있다. commit·push·공개 배포는 진행하지 않았다.

## Next Steps - Line 병합·보관·대표 Line

- 승인 후 lifecycle migration을 production에 적용하고 authenticated RPC 성공·권한 거부·동시성 충돌·cycle 또는 edge 충돌 rollback을 검증한다.
- production 전후 Line·membership·edge·start 수량과 checksum, 보안·성능 Advisor를 확인한다.
- 검증이 끝나면 Phase 4 완료 조건의 `Line 병합·보관·대표 Line과 재검토 상태` 항목을 완료 처리한다.
- 자동 분류보다 우선하는 Line 색상 category 직접 지정 기능을 이어서 구현한다.

## Work Summary - Line Lifecycle Production Migration

로컬에서 준비한 Replacement Line 보관·복원·대표 Line 병합 구조를 production Supabase에 적용했다. 검증용 병합과 보관은 모두 transaction 안에서 실행하고 rollback해 실제 Line 배치와 계보 판단을 바꾸지 않았다.

## Work Completed - Line Lifecycle Production Migration

- production migration `20260804193058_manage_replacement_line_lifecycle`을 적용하고 로컬 migration 파일명도 원격 history에 맞췄다.
- active·archived lifecycle column, 대표 Line composite foreign key, archive contract, 두 index와 membership·edge·start의 archived read-only trigger 생성 상태를 확인했다.
- 두 앱 RPC와 내부 trigger 함수가 모두 빈 `search_path`를 사용하고, 앱 RPC는 `authenticated`만 실행 가능하며 `anon`·`PUBLIC`·`service_role`에는 실행 권한이 없음을 catalog에서 확인했다.
- 실제 workspace member claim의 transaction fixture에서 독립 Line 보관·복원, stale timestamp와 비회원 거절, archived Line child 수정 차단을 검증했다.
- 동일 edge를 가진 fixture Line 병합은 `23505`, 각각은 정상이나 합치면 cycle이 되는 fixture Line 병합은 `23514`로 거절되고 source가 active 상태로 유지되는지 확인했다.
- 실제 두 Item을 공유하는 `Ivory Layered Summer → Ivory Layered Sleeveless`를 rollback 안에서 병합해 membership이 `4 + 2 - 공유 2 = 4`로 중복 제거되고 edge 2개가 보존되며 source 보관·대표 참조와 양쪽 `needs_review` 전환이 함께 일어나는지 확인했다.
- 병합된 source를 직접 복원하는 요청이 `23514`로 거절되는지 확인한 뒤 transaction을 rollback했다.
- rollback 후 `53 Line · active 53 · archived 0 · 대표 참조 0 · 165 membership · 87 confirmed edge · 25 start · needs_review 0`과 Line·membership·edge·start checksum이 적용 전 기준선과 일치하고 fixture row가 0건인지 재확인했다.
- Security·Performance Advisor를 다시 확인했다.
- 로그인된 localhost에서 production Supabase를 다시 불러와 Color Index `12 Colors`, Line 관리 현황 `53 사용 중 · 0 보관`과 Ivory 7개 Line 필터가 정상 렌더링되고 콘솔 warning·error가 0건인지 확인했다.

## Decisions - Line Lifecycle Production Migration

- 실제 겹침 pair를 사용해 병합 계약을 검증하되 transaction rollback으로 J가 정한 Line 분류에는 변화를 남기지 않았다.
- 병합은 여러 table을 함께 변경하고 archived 상태에서의 child 쓰기를 우회해야 하므로 `SECURITY DEFINER`를 유지했다. 대신 함수 내부 `auth.uid()`·workspace membership 검사, 빈 `search_path`, fully qualified relation과 `authenticated` 최소 실행 권한을 사용했다.
- Advisor의 signed-in `SECURITY DEFINER` 경고 두 건은 의도된 앱 RPC 표면으로 검토했다. `anon` 실행이나 mutable search path 경고는 없으며, 새 lifecycle index 미사용 안내는 적용 직후 데이터가 모두 active인 상태에서 예상되는 정보성 항목이다.

## Current Status - Line Lifecycle Production Migration

production schema와 인증 RPC 검증이 완료됐고 실제 운영 Line·membership·edge·start 데이터는 적용 전과 동일하다. 앱 코드의 전체 회귀 테스트와 production build도 통과했으며 commit·push·공개 배포는 아직 진행하지 않았다.

## Next Steps - Line Lifecycle Production Migration

- 자동 분류보다 우선하는 Line 색상 category 직접 지정 기능을 구현한다.
- 남아 있는 미연결 Item의 시작점 또는 incoming edge 판단을 J가 앱에서 차례로 저장한다.
- Phase 4 전체 통합 QA와 공개 배포 전 체크를 이어서 진행한다.

## Work Summary - Closet·Lookbook 24개씩 더 보기

전체 계절 범위에서 Closet과 Lookbook이 모든 카드를 한 번에 생성해 긴 스크롤과 이미지 egress를 유발하던 구조를 개선했다. 한 화면에 12개가 들어가는 현재 grid 밀도를 기준으로 버튼 사용 횟수와 초기 로드량의 균형을 맞춰 24개 단위로 표시하도록 결정했다.

## Work Completed - Closet·Lookbook 24개씩 더 보기

- Closet과 Lookbook의 첫 목록을 24개로 제한하고 `더 보기`를 누를 때마다 24개씩 추가했다.
- 검색, 계절, 정렬과 각 페이지 필터가 바뀌면 표시 수를 다시 첫 24개로 초기화했다.
- Lookbook의 합성 착장 레이어 이미지에도 lazy loading을 연결해 현재 표시 범위 밖 이미지 요청을 줄였다. 편집 화면에서 직접 사용하는 레이어 컴포넌트는 기존 eager 기본값을 유지했다.
- 24개·48개 표시, 필터 변경 후 24개 복귀와 레이어 이미지 loading 속성을 검증하는 회귀 테스트를 추가했다.
- 전체 62개 테스트 파일의 277개 테스트, production build와 `git diff --check`를 통과했다.
- 로그인된 로컬 앱에서 실제 production 데이터로 Lookbook `24/250`, Closet `24/355`, 더 보기 후 48개, Lookbook 검색 후 `24/152` 복귀와 브라우저 warning·error 0건을 확인했다.

## Decisions - Closet·Lookbook 24개씩 더 보기

- 서버 페이지네이션은 적용하지 않고 이미 불러온 snapshot에서 카드 DOM만 24개씩 생성한다. 기존 전체 데이터 기반 필터·통계를 유지하면서 초기 이미지 전송과 렌더링 부하를 낮추기 위한 결정이다.
- 목록 전체 개수는 계속 표시하고 버튼에는 `현재 표시 수/전체 수`를 함께 보여 진행 상태를 알 수 있게 했다.
- 전체 회귀 테스트에서 저장 Preview가 lazy여야 한다는 기존 계약 실패를 한 번 발견해 `OutfitVisual` 기본값을 lazy로 복구했다. 직접 편집용 `LayeredOutfitPreview`만 eager 기본값을 유지해 화면별 로딩 목적을 분리했다.

## Current Status - Closet·Lookbook 24개씩 더 보기

로컬 코드, 자동 테스트, production build와 로그인 브라우저 QA가 완료됐다. 첫 진입과 필터 변경 시 24개만 생성되고 사용자가 요청할 때만 다음 24개가 추가된다. 공개 배포는 진행하지 않았다. 기존 production bundle의 500 kB 초과 경고는 남아 있다.

## Next Steps - Closet·Lookbook 24개씩 더 보기

- 실제 사용량은 다음 Supabase 월간 주기에서 Storage egress 변화로 비교한다.
- 필요하면 이후 snapshot 전체 갱신과 signed image URL 재발급 범위를 별도 최적화한다.

## Work Summary - Production Database Read-only Audit

Phase 4에서 늘어난 table과 RPC를 J가 SQL로 이해하고 이후 cleanup 범위를 직접 판단할 수 있도록 production Closet schema를 읽기 전용으로 감사했다. 자동 migration을 영구 기능으로 남기기보다 핵심 원본, 현재 지원 구조, 일회성 Legacy 구조를 구분하는 것이 목적이다.

## Work Completed - Production Database Read-only Audit

- production의 모든 `public.closet_*` table 17개에 대해 역할, lifecycle, source of truth, row count, total size, 주요 column, FK, 읽기·쓰기 코드 경로, trigger와 제거 dependency를 `docs/database-map.md`에 기록했다.
- public Closet RPC 25개의 production signature, 호출 repository·UI, 참조 table, transaction 이유와 cleanup 판정을 기록했다.
- production migration 48개와 local/Git migration을 대조해 Closet 관련 28개가 이름 기준으로 모두 존재함을 확인하고, timestamp 불일치 4쌍과 production 적용 후 Git 미추적 상태인 최신 migration 2개를 구분했다.
- 초기 `DORMANT_REVIEW` 후보였던 Outfit Preview가 실제 Outfit 저장·snapshot·Lookbook·stale trigger에서 사용 중인 `LIVE_SUPPORT`임을 확인했다.
- import run, Outfit Preview, Legacy Link를 순서대로 검토하는 3개 cleanup wave와 export·checksum·중단 조건을 `docs/database-cleanup-plan.md`에 작성했다.
- table, 주요 column, public RPC의 역할·source of truth·lifecycle을 기록하는 COMMENT 전용 migration을 작성했다.

## Decisions - Production Database Read-only Audit

- 상태를 `LIVE_CORE 5`, `LIVE_SUPPORT 9`, `DORMANT 0`, `LEGACY_DROP_CANDIDATE 3`으로 분류했다. 행 수가 적어도 현재 UI가 읽고 쓰는 Outfit Preview는 휴면으로 보지 않았다.
- 즉시 제거 가능한 object는 0개로 판정했다. 가장 독립적인 `closet_import_runs`도 2행 export와 import script 수정 뒤 제거하도록 했다.
- 현재 frontend 자동 색상 분류는 초기 제안/fallback으로만 사용하고, `closet_replacement_lines.color_category`처럼 사람이 바로 읽는 직접 지정 값을 source of truth로 만든 뒤 제거하는 방향을 권장했다. 기존 팔레트 22행과 현재 Line category 12개가 1:1이 아니므로 억지로 FK를 연결하거나 별도 자동 분류 history·confidence table을 만들지 않는다.
- COMMENT migration은 schema 동작을 바꾸지 않으며 production에는 적용하지 않았다. cleanup migration, data export, backfill, DROP·DELETE·UPDATE도 수행하지 않았다.

## Current Status - Production Database Read-only Audit

production read-only catalog 감사와 문서 작성은 완료됐다. 현재 Legacy Link 49개는 모두 reviewed이고 revision은 51개이며, 87개 edge 중 25개가 아직 Legacy FK를 유지한다. Outfit Preview는 ready 2행이지만 current call path와 trigger가 남아 있다. 이번 작업으로 production 데이터와 schema 동작은 변경되지 않았다.

## Next Steps - Production Database Read-only Audit

- Line 색상 직접 지정은 `closet_replacement_lines`의 사람이 읽을 수 있는 nullable category 하나로 설계하고, 기존 팔레트 HEX는 제안·표시에만 활용하며 53개 Line을 채운 뒤 자동 fallback 제거 조건을 검증한다.
- cleanup은 Wave 1부터 별도 승인 하에 export와 checksum을 먼저 만들고 wave별 migration·rollback 검증을 진행한다.
- COMMENT 전용 migration의 SQL 검증이 끝난 뒤 production 적용 여부를 별도로 결정한다.

## Work Summary - Outfit Preview Subsystem Local Cleanup

저장 Outfit Preview는 큰 공유 이미지나 캘린더 thumbnail 원본으로 사용하지 않기로 J가 확정했다. 파생 이미지 cache를 계속 유지하면 table·Storage·RPC·trigger·업로드 Function 전체를 이해해야 하므로, production을 건드리지 않고 local 차기 버전에서 subsystem 단위로 제거했다.

## Work Completed - Outfit Preview Subsystem Local Cleanup

- snapshot, repository, DataContext, Outfit 저장·표시·필터에서 Preview type·상태·업로드 경로를 제거하고 현재 Item cutout의 즉시 합성 또는 색상 swatch만 사용하도록 통일했다.
- Outfit 삭제를 Preview 업로드 Function에서 분리한 `closet-outfit-delete` Function으로 옮기고 workspace membership과 Wear Log 삭제 차단 RPC를 유지했다.
- Preview 전용 Function, 합성·감사·업로드 script와 테스트를 제거했다.
- stale trigger·private helper·preview RPC 세 개·Storage policy dependency·`closet_outfit_previews` table을 함께 제거하는 migration `20260804213423_remove_outfit_preview_subsystem.sql`을 작성했다.
- Preview가 없어야 한다는 pgTAP 계약과 CI 목록을 추가하고 기존 Phase 1·1b·3 schema 계약을 새 구조에 맞췄다.
- database map과 cleanup plan에 production 기준선, local 제거 상태와 안전한 적용 순서를 기록했다.

## Decisions - Outfit Preview Subsystem Local Cleanup

- table 하나만 DROP하지 않고 frontend 상태, upload Function, RPC, trigger와 Storage dependency를 한 subsystem으로 제거한다.
- Outfit 삭제의 권한·Wear Log 보호는 Preview 기능과 무관한 핵심 계약이므로 전용 Function으로 보존한다.
- production 공개 앱이 아직 Preview 구조를 사용하므로 destructive migration을 먼저 적용하지 않는다. 새 삭제 Function과 Preview-free frontend를 선배포한 뒤 pending migration을 적용한다.

## Current Status - Outfit Preview Subsystem Local Cleanup

local code와 cleanup migration은 완료됐고 production table·RPC·trigger·Storage object 2개와 기존 Function은 그대로다. 전체 58개 테스트 파일의 263개 테스트, TypeScript와 production build가 통과했다. 로컬 Docker가 실행 중이지 않아 새 pgTAP을 실제 local DB에서 실행하지는 못했으며 CI 실행 목록에는 연결했다.

## Next Steps - Outfit Preview Subsystem Local Cleanup

- `closet-outfit-delete` Function과 새 frontend를 먼저 배포해 Outfit 삭제 성공·Wear Log 차단을 확인한다.
- pending migration 적용 뒤 Preview table·RPC·trigger·policy dependency가 0인지 확인한다.
- production Storage object 2개와 기존 `closet-outfit-preview` Function을 정확히 확인한 뒤 제거한다.

## Work Summary - Replacement Line 대표 색상 직접 지정

자동 색상 분류를 영구 subsystem으로 키우지 않고, J가 Line마다 대표 category 하나를 직접 선택하고 나중에 계속 바꿀 수 있는 최소 구조를 구현했다. 새 table 대신 핵심 Line row의 nullable column 하나를 source of truth로 사용한다.

## Work Completed - Replacement Line 대표 색상 직접 지정

- `closet_replacement_lines.color_category`와 길이·공백 constraint를 추가하는 migration `20260804213528_add_replacement_line_color_category.sql`을 작성했다.
- workspace membership, Line row lock과 `updated_at` optimistic concurrency를 다시 검사하는 authenticated 전용 RPC를 작성했다.
- Line 관리 화면에 현재 대표 색상, `색상 수정`, 20개 category select, 저장·취소와 `자동 제안 사용` 초기화를 추가했다.
- 직접 지정 값이 있으면 Line 이름과 Item 팔레트 기반 자동 제안보다 우선하도록 Color Index 계산을 변경했다.
- migration 전 schema에서도 새 frontend가 Line 목록을 읽을 수 있도록 `color_category` column 부재 오류에 한해서만 기존 SELECT로 재시도하는 배포 전환 fallback과 회귀 테스트를 추가했다.
- repository, Demo persistence, overview 우선순위와 Line 관리 저장 동작을 자동 테스트로 검증하고 Phase 4 체크리스트를 local 구현 완료로 갱신했다.

## Decisions - Replacement Line 대표 색상 직접 지정

- 별도 category table, 자동 분류 history·confidence·migration run table은 만들지 않는다. SQL에서 `closet_replacement_lines.color_category` 하나만 읽으면 사람이 정한 값을 알 수 있게 한다.
- category를 null로 되돌리면 기존 자동 제안을 다시 사용한다. 따라서 직접 값은 언제든 수정하거나 초기화할 수 있다.
- 기존 53개 active Line을 J가 천천히 채우는 동안에만 자동 제안을 fallback으로 남기고, 직접 값 100%와 화면 QA 뒤 자동 분류 helper 제거를 다시 판단한다.

## Current Status - Replacement Line 대표 색상 직접 지정

local migration·repository·UI·테스트와 문서 갱신은 완료됐다. 전체 58개 테스트 파일의 263개 테스트와 production build, `git diff --check`가 통과했다. GitHub Actions 조건을 재현한 Pages artifact도 fallback 파일과 `/closet-index/` base path 계약을 통과했다. 첫 Pages 검증은 일반 local build에 실행해 `404.html`과 Pages base path가 없어 실패했고, CI와 동일하게 `GITHUB_ACTIONS=true` build와 `index.html → 404.html` 복사를 적용해 정상 통과했다. production migration·공개 배포·실데이터 category 입력은 아직 수행하지 않았다.

## Next Steps - Replacement Line 대표 색상 직접 지정

- Preview cleanup의 안전 순서에 맞춰 새 Function과 frontend를 선배포한 뒤 pending migration들을 적용한다.
- 로그인된 production 화면에서 색상 저장·수정·자동 제안 초기화, stale timestamp 충돌과 비회원 거절을 검증한다.
- J가 active Line의 category를 채운 뒤 미지정 수와 자동 fallback 제거 조건을 다시 확인한다.

## Work Summary - Preview Cleanup·Line Color Production Rollout

Preview-free frontend와 Outfit 삭제 전용 Function을 먼저 공개한 뒤 저장 Preview DB subsystem을 제거하고, 같은 rollout에서 Replacement Line 대표 색상 직접 지정과 DB COMMENT 문서를 production에 적용했다. 앱의 source of truth를 단순하게 유지하면서 destructive schema 변경 전에 새 코드가 실제 공개 환경에서 동작하는지 확인하는 순서로 진행했다.

## Work Completed - Preview Cleanup·Line Color Production Rollout

- `closet-outfit-delete` Edge Function을 배포하고 비인증 요청 차단과 GitHub Pages origin의 CORS preflight를 확인했다.
- Preview-free frontend를 commit `749949b`로 `main`에 push하고 GitHub Pages 배포 성공, 공개 asset의 새 삭제 Function·색상 UI 포함과 구 Preview 호출 제거를 확인했다.
- production migration `20260804213423_remove_outfit_preview_subsystem`, `20260804213528_add_replacement_line_color_category`, `20260804214150_document_closet_database_objects`를 순서대로 적용했다.
- `closet_outfit_previews`, Preview RPC 세 개, stale trigger 두 개와 관련 private helper가 제거되고 Storage SELECT policy가 ready Item image만 허용하는지 catalog로 검증했다.
- 실제 workspace claim의 transaction fixture에서 착용 기록이 있는 Outfit 삭제 차단, 미착용 Outfit 삭제 성공과 rollback 뒤 507개 복원을 확인했다.
- 공개 앱에서 `Royal Blue Summer Top`을 Blue로 직접 저장한 뒤 `자동 제안 사용`으로 초기화해 수정·초기화 경로와 최종 null 값을 확인했다.
- production Closet table 16개, migration history 51개 중 Closet 31개, Line 53개·membership 165개·edge 87개·start 25개와 핵심 row count 불변을 확인했다.
- migration 파일 timestamp를 Supabase가 실제 기록한 production version과 맞추고 database map, cleanup plan과 Phase 4 체크리스트를 현재 상태로 갱신했다.

## Decisions - Preview Cleanup·Line Color Production Rollout

- Preview table을 먼저 지우지 않고 새 삭제 Function과 Preview-free 공개 앱을 선배포해 schema 전환 중 구 frontend가 깨지지 않게 했다.
- Line 색상은 새 table 없이 `closet_replacement_lines.color_category` nullable column 하나만 source of truth로 사용한다. null은 자동 제안을 뜻하며 J가 직접 지정한 값이 항상 우선한다.
- 기존 Preview Storage object 2개와 구 `closet-outfit-preview` Function은 정확한 대상이 확인됐지만, service-role credential을 로컬에 만들지 않고 J의 Dashboard 로그인 뒤 제거한다.
- Phase 4 체크리스트는 실제 완료 증거가 있는 항목만 체크했다. 로컬 Docker에서 실행하지 못한 격리 pgTAP이 포함된 schema 통합 항목은 미완료로 유지했다.

## Current Status - Preview Cleanup·Line Color Production Rollout

production DB와 공개 frontend는 저장 Preview dependency 없이 동작하고 Line 대표 색상을 저장·수정·자동 제안으로 초기화할 수 있다. 전체 58개 테스트 파일의 263개 테스트, TypeScript, production build와 Pages 배포가 통과했으며 공개 앱 console error·warning은 없었다. Preview Storage object 2개는 새 SELECT policy로 앱에서 읽을 수 없는 orphan 상태이고 구 Function은 공개 bundle에서 호출하지 않지만, 두 운영 잔여물은 아직 존재한다.

## Next Steps - Preview Cleanup·Line Color Production Rollout

- J가 열린 Supabase Dashboard에 로그인하면 정확히 확인한 Preview Storage object 2개를 삭제하고 object count 0을 재확인한다.
- 이어서 사용하지 않는 production `closet-outfit-preview` Edge Function을 제거하고 Function 목록과 Edge log를 다시 확인한다.
- J가 53개 Line의 대표 색상을 천천히 직접 지정한 뒤 null count와 자동 fallback 제거 조건을 다시 검토한다.

## Work Summary - Preview 운영 잔여물 제거

J의 Supabase Dashboard 로그인 뒤 Preview subsystem에서 마지막으로 남아 있던 Storage file과 구 Edge Function을 제거했다. 삭제 성공 문구만으로 완료 처리하지 않고 Storage object catalog와 Function API 목록을 다시 조회해 실제 0개 상태를 확인했다.

## Work Completed - Preview 운영 잔여물 제거

- `closet-images` bucket의 두 Preview file 경로와 크기를 SQL로 다시 확인한 뒤 `v1.webp` 2개만 삭제했다.
- Storage 로그에서 두 file 모두 `ObjectRemoved:Delete`와 HTTP 200으로 처리된 것을 확인했다.
- Dashboard가 빈 폴더를 표시하기 위해 만든 `.emptyFolderPlaceholder`가 단계별로 남는 것을 발견해, 실제 file이 없음을 SQL로 확인한 뒤 빈 `preview`·`outfits` 폴더까지 제거했다.
- 최종 SQL에서 `closet-images`의 `/outfits/` 경로 object count가 0임을 확인했다.
- active Function 4개 중 `closet-outfit-preview`를 삭제하고 API와 Dashboard에서 `closet-item-image`, `closet-outfit-delete`, `closet-weather-forecast` 3개만 남았는지 확인했다.

## Decisions - Preview 운영 잔여물 제거

- Storage table row를 SQL로 직접 지우지 않고 Dashboard Storage API를 사용해 실제 binary와 metadata lifecycle을 함께 정리했다.
- `outfits` 폴더를 제거하기 전 SQL로 그 아래에 실제 file이 없고 0-byte placeholder 두 개만 남았음을 확인해 Item 이미지 경로와 삭제 범위를 분리했다.
- 새 Outfit 삭제 계약을 담당하는 `closet-outfit-delete` Function은 유지하고 이름이 비슷한 구 Preview Function만 제거했다.

## Current Status - Preview 운영 잔여물 제거

Outfit Preview subsystem은 frontend, repository, DB table·RPC·trigger·policy dependency, Storage object와 구 Edge Function까지 production에서 완전히 제거됐다. `/outfits/` Storage object는 0개이고 active Edge Function은 3개다.

## Next Steps - Preview 운영 잔여물 제거

- J가 53개 Line의 대표 색상을 천천히 직접 지정한 뒤 null count와 자동 fallback 제거 조건을 다시 검토한다.
- 다음 DB cleanup 후보인 `closet_import_runs`는 별도 승인과 local-only export·checksum을 먼저 준비한 뒤 Wave 1으로 진행한다.

## Work Summary - Replacement Line 재검토와 기본 관리 보강

Item을 다른 Line으로 옮긴 뒤 `needs_review` 안내만 남고 완료할 방법이 없던 흐름을 보완했다. 실제 사용 중 함께 발견된 Line 이름·Style Identity 수정과 완전히 빈 Line 삭제도 기존 Line table 안에서 처리해 관리 기능을 이해하기 쉽게 완성했다.

## Work Completed - Replacement Line 재검토와 기본 관리 보강

- pending edge가 없을 때 membership 재검토를 `ready`로 완료하는 버튼과 인증 RPC를 추가했다.
- Line 이름과 선택적인 Style Identity를 수정·추가·제거하는 폼과 optimistic update RPC를 추가했다.
- Item membership, edge, 시작점, 병합 대표 참조가 모두 없는 active standalone Line만 완전히 삭제하는 확인 UI와 RPC를 추가했다.
- Supabase repository, DataContext와 Demo localStorage 저장 경로를 연결하고 세 RPC의 익명 접근 차단·workspace membership·row lock·stale edit 차단을 확인하는 pgTAP 계약을 CI 목록에 추가했다.
- 집중 테스트 31개, 전체 58개 파일 266개 테스트, TypeScript 검사와 production build를 통과했다.

## Decisions - Replacement Line 재검토와 기본 관리 보강

- 재검토 완료는 모든 Item을 강제로 연결하는 의미가 아니라 현재 membership과 계보 상태를 사람이 확인했다는 표시로 사용한다. 단, `needs_review` edge가 남아 있으면 먼저 해당 연결을 검토하도록 차단한다.
- Line metadata와 review 상태는 기존 `closet_replacement_lines` row를 source of truth로 유지하고 새 table이나 수정 이력 subsystem은 만들지 않는다.
- 빈 Line 삭제는 화면의 Item 수만 신뢰하지 않고 DB에서 edge·시작점·대표 Line 참조까지 다시 확인한다. 데이터가 하나라도 남은 Line은 기존 보관 또는 병합 기능을 사용한다.

## Current Status - Replacement Line 재검토와 기본 관리 보강

local 코드, migration, UI와 자동 테스트는 완료됐다. Demo 전용 로컬 서버에서 Item을 새 Line으로 옮겨 재검토 경고와 완료 버튼을 확인하고, 이름 수정·Style Identity 제거와 원본 빈 Line의 삭제 확인·목록 복귀까지 실제 브라우저로 검증했다. console warning·error는 없었다. 로컬 Docker가 실행 중이지 않아 새 pgTAP을 실제 local DB에서 실행하지는 못했으며 CI 실행 목록에는 연결했다. production DB migration, commit·push와 공개 배포는 진행하지 않았다.

## Next Steps - Replacement Line 재검토와 기본 관리 보강

- 별도 production 적용 단계에서 migration, 실제 Brown Line 재검토 완료, stale·비회원 차단과 공개 화면을 검증한다.
- 모바일 실기기 또는 조절 가능한 viewport에서 관리 카드의 버튼 줄바꿈을 추가 확인한다.

## Work Summary - Replacement Line 관리 RPC Production 적용

Line 이름 수정 UI는 로컬 코드에 있었지만 production DB에 대응 RPC가 없어 저장이 실패했다. 화면 오류를 재현한 뒤 함수 catalog와 migration history를 읽기 전용으로 확인해 코드와 DB 배포 순서 불일치를 원인으로 확정하고, 준비된 migration을 production에 적용했다.

## Work Completed - Replacement Line 관리 RPC Production 적용

- production migration `20260804230012_manage_replacement_line_review_and_metadata`를 적용해 재검토 완료, 이름·Style Identity 수정, 완전 빈 Line 삭제 RPC 세 개를 추가했다.
- 세 함수 모두 빈 `search_path`, workspace membership 검사, row lock과 `updated_at` optimistic concurrency를 유지하는지 확인했다.
- `authenticated`만 실행 가능하고 `anon`·`public`에는 실행 권한이 없는지 catalog로 검증했다.
- 실제 workspace member claim과 `Black Flat Shoes` row를 사용해 `Black Shoes Flat` 수정 RPC를 transaction 안에서 실행하고 반환 이름·Style Identity·timestamp 전진을 확인한 뒤 rollback했다.
- rollback 뒤 원본 이름과 Style Identity, `56 Line · 165 membership · 90 edge · 32 start`가 그대로인지 재확인했다.
- production이 기록한 version과 일치하도록 로컬 migration 파일명을 `20260804230012_manage_replacement_line_review_and_metadata.sql`로 맞췄다.

## Decisions - Replacement Line 관리 RPC Production 적용

- 사용자가 의도한 이름을 대신 확정 저장하지 않고 transaction rollback fixture로 저장 가능성만 검증했다. 실제 최종 이름은 UI에서 다시 저장한다.
- 새 Advisor의 signed-in `SECURITY DEFINER` 안내 세 건은 authenticated 앱 RPC로 의도된 항목이다. 각 함수가 workspace membership과 입력·dependency를 내부에서 재검사하고 `anon`·`public` 실행을 차단하므로 현재 구조를 유지한다.

## Current Status - Replacement Line 관리 RPC Production 적용

production DB와 현재 frontend의 RPC 계약이 일치한다. Line 이름과 Style Identity 저장을 다시 시도할 수 있으며 기존 production 데이터는 변경되지 않았다. commit·push는 아직 진행하지 않았다.

## Next Steps - Replacement Line 관리 RPC Production 적용

- 앱에서 `Black Shoes Flat` 등 원하는 최종 이름을 다시 저장해 실제 UI round-trip을 확인한다.
- Brown Line들의 `needs_review` 안내는 각 계보를 확인한 뒤 `재검토 완료`로 처리한다.

## Work Summary - Replacement Line Item 추가와 전체 제외

기존 Notion migration에 포함되지 않았거나 처음부터 Line을 지정하지 않은 Closet Item을 나중에 Line에 넣을 수 없었고, 실수로 넣은 Item은 다른 Line으로 옮길 수만 있을 뿐 아무 Line에도 속하지 않게 뺄 수 없었다. Item 자체 삭제와 membership 제거를 분리해 Line 관리 흐름을 완성했다.

## Work Completed - Replacement Line Item 추가와 전체 제외

- Line 관리에 Line이 하나도 없는 Item만 이름·category·색상으로 검색하고 thumbnail을 확인해 현재 Line에 추가하는 picker를 구현했다.
- 추가한 Item을 명시적 시작점으로 만들고 Line을 `needs_review`로 전환하는 repository·Demo 저장·Supabase RPC 계약을 연결했다.
- 계보 Item마다 `Line에서 빼기`를 추가하고, Closet Item과 이미지는 유지한 채 해당 Item의 모든 Line membership과 시작점을 제거하도록 구현했다.
- predecessor 또는 successor edge가 남은 Item은 버튼에서 연결 해제를 먼저 안내하고 DB에서도 전체 제외를 거부하도록 이중 차단했다.
- 새 table이나 column 없이 인증 RPC 두 개만 담은 migration `20260804233509_manage_replacement_line_item_membership.sql`과 18개 pgTAP 계약 검사를 추가했다.
- focused 34개와 전체 58개 파일 269개 테스트, TypeScript, production build, desktop·390px Demo browser 왕복 흐름과 console 상태를 검증했다.

## Decisions - Replacement Line Item 추가와 전체 제외

- `Line에서 빼기`는 `계보에서 빼기`와 다른 동작이다. 전자는 Item의 모든 Replacement Line 소속을 없애고 후자는 부모–자식 edge만 해제한다.
- 새 Item 추가 대상은 현재 어느 Line에도 속하지 않은 Item으로 제한한다. 이미 소속된 Item은 기존 `다른 Line으로 옮기기`를 사용해 중복 membership을 만들지 않는다.
- 전체 제외는 예상 밖의 복수 Line membership도 남기지 않도록 해당 Item의 모든 membership과 start를 한 transaction에서 제거한다.
- edge가 하나라도 남은 경우 과거 계보를 암묵적으로 삭제하지 않고 먼저 명시적으로 연결을 해제하게 한다.

## Current Status - Replacement Line Item 추가와 전체 제외

local 코드·migration·UI·자동 테스트와 실제 Demo browser 검증은 완료됐다. 추가한 Item이 즉시 G0 시작점으로 표시되고, 전체 제외 뒤 Closet Item은 보존된 채 다시 `Line 없는 Item` 후보로 나타나는 것을 확인했다. 로컬 Docker가 꺼져 있어 새 pgTAP과 migration을 local DB에서 실행하지는 못했다. production DB migration, commit·push와 공개 배포는 진행하지 않았다.

## Next Steps - Replacement Line Item 추가와 전체 제외

- 별도 승인 뒤 production migration을 적용하고 실제 workspace member rollback fixture, 익명·비회원·stale·edge 차단과 Advisor를 검증한다.
- production frontend를 배포한 뒤 실제 Item 하나로 추가·전체 제외 round-trip을 확인한다.

## Work Summary - Replacement Line Item Membership RPC Production 적용

로컬에서 완성한 Line 미소속 Item 추가와 모든 Line 제외 기능을 실제 앱에서 사용할 수 있도록 production DB 계약을 적용하고, 운영 데이터를 남기지 않는 rollback 방식으로 권한과 원자성을 검증했다.

## Work Completed - Replacement Line Item Membership RPC Production 적용

- production migration `20260804233509_manage_replacement_line_item_membership`을 적용해 `add_closet_replacement_line_item`과 `remove_closet_replacement_line_item` RPC를 추가했다.
- production이 기록한 version과 일치하도록 로컬 migration 파일명을 `20260804233509_manage_replacement_line_item_membership.sql`로 맞췄다.
- 두 함수의 빈 `search_path`, row lock, 내부 workspace membership 검사, `authenticated` 단독 실행 권한과 anon·public·service_role 차단을 catalog에서 확인했다.
- 실제 workspace member claim과 production의 active Line·Line 미소속 Item을 transaction에서 사용해 membership과 시작점 추가, `needs_review` 전환, 모든 membership·start 제외와 Closet Item 보존을 확인한 뒤 rollback했다.
- 같은 fixture에서 stale timestamp, 이미 Line에 속한 Item 중복 추가, edge가 남은 Item 제외와 비회원 호출이 각각 거절되는 것을 확인했다.
- rollback 뒤 `57 Line · 165 membership · 92 edge · 35 start · needs_review 1`과 최근 5분 membership·start 0개를 확인해 검증 데이터가 남지 않았음을 확인했다.
- Security·Performance Advisor를 다시 실행해 새 performance 경고가 없고 새 security 항목은 signed-in 전용 `SECURITY DEFINER` 안내 두 건뿐임을 확인했다.

## Decisions - Replacement Line Item Membership RPC Production 적용

- 두 동작은 membership·start·Line review 상태를 함께 바꾸므로 partial write를 피하기 위해 transaction RPC를 유지한다.
- Advisor의 signed-in `SECURITY DEFINER` 안내는 앱이 REST RPC로 호출해야 하는 의도된 경계다. 함수 내부 authorization, 빈 `search_path`, 최소 권한을 모두 확인했으므로 `SECURITY INVOKER`로 바꾸지 않는다.
- production 검증은 실제 Item 이름이나 J의 Line 구성을 확정 변경하지 않고 전부 rollback한다.

## Current Status - Replacement Line Item Membership RPC Production 적용

production DB와 로컬 repository의 RPC 계약이 일치하고 실제 member 권한 검증이 통과했다. 운영 데이터에는 검증용 membership이나 시작점이 남지 않았다. frontend 코드의 commit·push·공개 배포는 아직 진행하지 않았다.

## Next Steps - Replacement Line Item Membership RPC Production 적용

- frontend 변경을 commit·push하고 공개 배포한 뒤 실제 앱에서 Item 추가·전체 제외 UI를 검증한다.

## Work Summary - 복수 Line 소속 정정 범위 교정

`퍼스트클로 슬리브리스`와 `무탠다드 슬리브리스`가 정상적인 `Ivory Layered Sleeveless` 계보에 연결된 상태로 `Ivory Layered Summer`에도 중복 소속되어 있었다. 기존 제거 RPC가 Item의 모든 Line edge와 membership을 한꺼번에 검사·제거하도록 설계되어, edge가 없는 Summer 쪽 중복만 안전하게 정리할 수 없는 문제를 교정했다.

## Work Completed - 복수 Line 소속 정정 범위 교정

- 운영 데이터를 읽기 전용으로 확인해 두 Item 모두 `Ivory Layered Sleeveless`에는 edge가 있고 `Ivory Layered Summer`에는 membership만 있음을 확정했다.
- `remove_closet_replacement_line_item`이 source Line의 edge만 검사하고 source Line의 membership·start만 제거하도록 forward migration `20260805000354_remove_item_from_single_replacement_line.sql`을 작성해 production에 적용했다.
- 제거 확인창에 현재 Line 이름과 보존되는 다른 Line 이름을 표시하고, `현재 Line에서 빼기`로 결과 범위를 명확히 했다.
- 다른 Line 이동 선택지에서는 Item이 이미 소속된 Line을 제외하고, 중복 정정에는 현재 Line 제거를 사용하라는 안내를 추가했다.
- 화면 상태에서도 source Line membership·start만 제거해 다른 Line의 로컬 상태가 사라지지 않도록 수정했다.
- DB 지도와 Phase 4 계획서의 기존 “전체 Line 제외” 설명을 현재 Line 전용 의미로 교정했다.
- targeted 36개, 전체 58개 파일 271개 테스트, TypeScript 검사와 production build를 통과했다. 첫 전체 검증은 도구의 짧은 기본 제한으로 중단되어 제한 시간을 늘려 같은 명령을 다시 실행했다.
- 실제 Demo 화면에서 이미 소속된 Line 안내, 현재 Line 전용 확인문, 제거 뒤 source Item 수 감소와 다른 Line membership 보존을 확인했고 console warning·error가 없었다.

## Decisions - 복수 Line 소속 정정 범위 교정

- 복수 Line 소속은 migration 과정에서 실제로 존재할 수 있으므로 “모든 소속을 일괄 삭제”보다 사용자가 보고 있는 source Line만 수정하는 동작을 기본으로 삼는다.
- 정상 Line에 edge가 있어도 source Line에 edge가 없다면 source membership 제거를 허용한다. 정상 Line의 membership·edge·review timestamp는 변경하지 않는다.
- 이미 소속된 Line으로 “이동”하면 같은 membership을 중복 생성하게 되므로 이동 대상에서는 제외하고, source Line에서 빼는 정정 흐름으로 안내한다.
- J님의 실제 두 중복 membership은 자동 삭제하지 않았다. production 검증은 전부 transaction rollback으로 수행해 최종 판단과 실행을 UI에 남겼다.

## Current Status - 복수 Line 소속 정정 범위 교정

production RPC는 source Line 전용 제거 계약으로 교체됐다. 실제 member claim rollback에서 Summer 중복 membership은 `2 → 0`, Sleeveless 정상 membership은 `2 → 2`, 정상 edge는 `1 → 1`이었고 rollback 뒤 양쪽 membership이 다시 `2 · 2`로 유지됨을 확인했다. 함수는 빈 `search_path`, 내부 workspace membership 검사와 authenticated 단독 실행 권한을 유지한다. Advisor의 signed-in `SECURITY DEFINER` 경고는 이 사용자 쓰기 RPC에 대해 의도된 항목이며 신규 performance 문제는 없다. frontend 코드는 아직 commit·push·공개 배포하지 않았다.

## Next Steps - 복수 Line 소속 정정 범위 교정

- `Ivory Layered Summer` 화면에서 두 Item의 `Line에서 빼기`를 각각 실행해 실제 중복 membership을 정리한다.
- frontend 변경을 commit·push하고 공개 배포한 뒤 production UI에서 같은 흐름을 재확인한다.

## Work Summary - Replacement Lines 상단 빈 Line 생성

Item을 먼저 선택해 새 Line으로 옮기는 경로만 있던 상태에서, Replacement Lines 목록에서 Line을 먼저 만든 뒤 상세 화면에서 Item을 추가하는 반대 방향을 열었다. Line 생성과 Item 편집의 진입점을 양쪽에 두되 데이터 구조는 늘리지 않았다.

## Work Completed - Replacement Lines 상단 빈 Line 생성

- 상단 소개 카드에 `새 Line 추가` 버튼과 이름, 선택적인 Style Identity, 필수 대표 색상 category 입력 폼을 추가했다.
- 생성 성공 직후 새 Line 상세 화면으로 이동해 기존 `Item 추가` 기능을 바로 사용할 수 있게 했다.
- Demo localStorage, repository, DataContext, Supabase RPC 매핑과 UI navigation 테스트를 추가했다.
- 기존 `closet_replacement_lines`에 active·ready 상태의 빈 row만 생성하는 migration `20260805002809_create_empty_replacement_line.sql`을 production에 적용했다.
- 실제 workspace member claim의 transaction rollback에서 입력 공백 정규화, 대표 색상 저장, membership 0개와 검증 데이터 미잔존을 확인했다.
- 집중 테스트 3개 파일 30개와 전체 58개 파일 274개 테스트, TypeScript 검사와 production build를 통과했다.
- 별도 Demo 서버에서 상단 폼 입력, Brown category 저장, 빈 상세 화면 이동과 `Item 추가` 버튼 노출을 실제 브라우저로 확인하고 검증용 Line을 다시 삭제했다. console warning·error는 없었다.

## Decisions - Replacement Lines 상단 빈 Line 생성

- 수동으로 만드는 Line은 Color Index에서 즉시 찾을 수 있어야 하므로 대표 색상 category를 필수로 받는다. 이후 상세 화면에서 기존 색상 수정 기능으로 바꿀 수 있다.
- 빈 Line은 연결 검토 대상이 없으므로 `review_status = ready`로 시작하며, 첫 Item 추가 시 기존 계약에 따라 `needs_review`로 전환한다.
- 새 table, 임시 membership 또는 migration 이력을 만들지 않고 Line source of truth 한 row만 생성한다.
- `SECURITY DEFINER` RPC는 table에 SELECT 전용 RLS만 있는 현재 구조에서 의도된 쓰기 경계로 유지하고, 빈 `search_path`, 내부 workspace membership 검사와 authenticated 단독 실행 권한을 적용했다.

## Current Status - Replacement Lines 상단 빈 Line 생성

production DB RPC와 로컬 frontend 계약이 일치하며 rollback 검증 데이터는 남지 않았다. 자동 테스트, build와 Demo 브라우저 왕복 검증이 통과했다. frontend commit·push·공개 배포와 실제 production UI 검증은 아직 진행하지 않았다.

## Next Steps - Replacement Lines 상단 빈 Line 생성

- frontend를 공개 배포한 뒤 실제 계정으로 새 Line 생성과 Item 추가 왕복을 확인한다.

## Work Summary - Phase 4 접근성·성능·DB 마감 QA

Phase 4의 기능 구현을 일단락하기 전에 키보드 접근성, 초기 렌더 비용과 production schema 보안 경계를 근거로 다시 점검했다. J가 공개 앱의 상단 Line 생성이 정상 동작한다고 확인한 결과도 계획서에 반영했다.

## Work Completed - Phase 4 접근성·성능·DB 마감 QA

- 계보 전체를 Line 이름으로 식별되는 `region`으로 노출하고, 시각적 connector는 스크린리더에서 숨겨진 장식 요소인지 확인했다.
- Line 생성 버튼의 Enter 키 실행과 이름 필드 자동 초점, Item 상세 링크의 Enter 키 이동을 자동 테스트로 고정했다.
- 실제 브라우저에서 focus outline, label과 heading 구조, blocking overlay, console warning·error와 390px 계보 가로 넘침을 확인했다.
- 로컬 Vite Demo warm reload 3회로 Color 첫 화면 149ms, 2개 Item 계보 211ms, Statistics 236ms 중앙값을 기록했다. Color 첫 화면은 image 요청 0개, 계보는 실제 image asset 1개였다.
- page 전용 Supabase 조회가 Color 첫 화면 3회, 계보 4회이며 각각 병렬로 실행되는 것을 repository 경계에서 대조했다.
- production의 4개 Lineage table RLS·policy·grant와 17개 Lineage RPC의 실행 권한·빈 `search_path`를 읽기 전용으로 확인하고 Security·Performance Advisor를 재검토했다.
- Git과 production의 Closet migration 35개가 최신 `20260805002809`까지 일치하는 것을 확인했다.
- 전체 58개 파일 274개 테스트와 production build를 통과했다.

## Decisions - Phase 4 접근성·성능·DB 마감 QA

- 계보는 ARIA tree로 흉내 내지 않고 일반 링크·버튼의 기본 키보드 동작을 유지한 이름 있는 region과 세대별 heading으로 제공한다. DAG에 tree 전용 방향키 규칙을 억지로 적용하지 않기 위해서다.
- 초기 성능 수치는 production 절대 속도가 아니라 동일 기기·Demo에서 페이지 간 요청량과 warm render 비용을 비교하는 회귀 기준으로 기록한다.
- Advisor의 authenticated `SECURITY DEFINER` 경고는 내부 workspace 회원 검사, 빈 `search_path`, anon/public 실행 차단이 함께 확인된 의도된 RPC 경계로 유지한다.
- start table의 FK 보조 index 권고는 현재 작은 데이터와 실제 query 경로에서 기능 차단이 아니므로 이번 마감에서 새 schema 변경을 추가하지 않고 후속 성능 관찰 대상으로 남긴다.

## Current Status - Phase 4 접근성·성능·DB 마감 QA

접근성과 초기 성능 비교는 완료됐고 자동 회귀 테스트와 build가 통과했다. production RLS·grant·Advisor와 migration 일치도 확인됐다. J의 승인 뒤 GitHub Actions run `30967739713`에서 현재 `main`을 새 Supabase DB에 처음부터 적용했으며, 5개 pgTAP 파일·92개 test와 컨테이너 정리가 모두 통과했다. Node 20 기반 action의 Node 24 강제 실행 deprecation 안내만 남았고 DB contract 실패나 test warning은 없다.

## Next Steps - Phase 4 접근성·성능·DB 마감 QA

- GitHub Actions의 `actions/checkout@v4`, `supabase/setup-cli@v1`이 Node 24 native release를 제공하면 workflow action version을 갱신한다.
- Advisor의 `closet_replacement_line_starts` FK 보조 index 권고는 실제 row 증가와 query plan을 관찰한 뒤 필요할 때 별도 migration으로 처리한다.

## Work Summary - Replacement Line 리팩터링 Phase 0-1 기준선

승인된 중간 규모 리팩터링을 시작하기 전에 현재 동작과 실패 경계를 characterization test로 고정했다. production 구현은 변경하지 않고 이후 public port와 feature hook 변경이 비교할 수 있는 기준선을 만들었다.

## Work Completed - Replacement Line 리팩터링 Phase 0-1 기준선

- 기존 Replacement frontend 12개 파일·63개 테스트와 TypeScript 검사를 먼저 통과시켜 작업 시작점을 확인했다.
- addItem 성공 시 상태 갱신은 기존 테스트를 유지하고, RPC 실패 시 membership을 낙관적으로 적용하지 않는 동작을 추가로 고정했다.
- optimistic timestamp 충돌 시 로드한 `updatedAt`을 RPC에 전달하고 현재 화면과 membership을 유지하는 동작을 고정했다.
- 같은 add action을 첫 요청 처리 중 연속 실행해도 repository 호출은 1회만 발생하는 현재 동작을 고정했다.
- 기존 move·merge·archive 및 edge·start·remove·delete·details·color·review 테스트를 그대로 회귀 기준에 포함했다.

## Decisions - Replacement Line 리팩터링 Phase 0-1 기준선

- feature hook으로 mutation state를 옮기기 전에 중복 실행 방지와 오류 표시를 UI 관찰 가능 결과로 먼저 잠근다.
- 로컬 환경에는 Docker 실행 파일이 없어 기존 pgTAP을 실행할 수 없었다. DB migration·RPC·SQL contract를 수정하지 않는 이번 frontend refactor에서는 pgTAP을 frontend gate에서 제외하고, 최근 main의 GitHub Actions pgTAP 92개 통과 기록을 DB 기준선으로 사용한다.

## Current Status - Replacement Line 리팩터링 Phase 0-1 기준선

production 코드는 아직 변경하지 않았다. 새 characterization test를 포함한 Replacement 집중 테스트와 TypeScript 검사를 통과한 뒤 다음 phase에서 repository public port를 변경할 수 있는 상태다.

## Next Steps - Replacement Line 리팩터링 Phase 0-1 기준선

- `ReplacementLineRepository`를 `ClosetRepository`의 필수 port로 만들고 flat Replacement API를 제거한다.
- Demo Replacement 구현을 별도 파일로 기계적으로 이동하되 validation과 localStorage 형식을 유지한다.

## Work Summary - Replacement Line 리팩터링 Phase 2 public port 정리

Replacement 기능의 선택적 flat method가 `ClosetRepository`, `DataContext`, Demo와 Supabase façade에 반복되던 구조를 필수 `replacementLines` port 하나로 정리했다. Demo 구현은 선택 사항으로 남기지 않고 별도 파일로 실제 이동했다.

## Work Completed - Replacement Line 리팩터링 Phase 2 public port 정리

- `ReplacementLineRepository`를 추가하고 `ClosetRepository.replacementLines`를 필수 port로 지정했다.
- DataContext의 Replacement pass-through callback과 환경별 미지원 분기를 제거하고 port 객체만 노출했다.
- Supabase façade의 flat forwarding method를 제거하고 adapter가 새 port를 직접 구현하도록 했다.
- Demo의 Replacement 상수, localStorage reader/writer, validation과 mutation 구현을 `src/data/demo/replacement-lines.ts`로 기계 이동했다.
- 네 production page와 관련 테스트를 port의 짧은 method 이름으로 전환했다.
- frontend caller가 없는 `revise_closet_replacement_line_edge_details` method를 public port, DataContext, Demo API와 Supabase frontend adapter에서 제거했다. 기존 DB RPC와 migration은 변경하지 않았다.
- 기존 63개를 포함한 Replacement 66개 테스트, 전체 58개 파일·277개 테스트, TypeScript 검사와 production build를 통과했다.

## Decisions - Replacement Line 리팩터링 Phase 2 public port 정리

- 개인용 single-user 앱에서 런타임 optional method 확인은 실제로 사용되지 않으므로 compile-time 필수 port로 바꾼다.
- Demo는 앱의 환경변수·Supabase 설정 부재 시 실제 선택되는 실행 경로이므로 유지하되, 일반 DemoRepository와 Replacement 구현의 변경 범위를 파일 단위로 분리한다.
- Demo의 다섯 localStorage key, JSON 형식, validation 문구와 mutation 순서는 변경하지 않는다.
- UI caller가 없는 edge details RPC는 DB 호환성을 위해 SQL에 보존하지만 frontend public API에는 노출하지 않는다.

## Current Status - Replacement Line 리팩터링 Phase 2 public port 정리

frontend public surface는 `ClosetRepository → replacementLines → adapter` 한 경로로 정리됐다. 전체 테스트와 build가 통과했고 DB schema, migration, RPC 이름·인자·반환형은 변경되지 않았다.

## Next Steps - Replacement Line 리팩터링 Phase 2 public port 정리

- snapshot 적용 규칙을 순수 helper로 추출하고 characterization test를 직접 연결한다.
- page의 load·mutation state와 navigation 책임을 feature hook으로 옮기되 표현 컴포넌트의 UI 문구와 동작은 유지한다.
