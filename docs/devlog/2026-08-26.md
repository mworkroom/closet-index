# 2026-08-26 Closet Index Development Log

## Work Summary

production schema에는 반영됐지만 remote migration history에 없는 Place HVAC migration 2개의 적용 경로와 현재 일치 여부를 읽기 전용으로 감사했다. 두 파일 모두 2026-08-10에 `execute_sql` transaction으로 직접 적용돼 history row가 생성되지 않았음을 원본 실행 기록으로 확정했고, 현재 schema·권한·데이터가 두 migration의 최종 합성 결과와 일치하는지 검증했다.

## Work Completed

- 최신 Supabase changelog와 Database Migrations·migration repair 공식 문서를 확인했다. remote 직접 SQL은 history를 우회하며, schema가 이미 정확히 적용된 경우 `migration repair --status applied <timestamp>`로 tracking row만 보정할 수 있음을 확인했다.
- Git history와 2026-08-10 개발 로그, 당시 원본 실행 기록을 대조했다. `20260809200608_p5b_place_profile_hvac.sql`과 `20260809211440_remove_wear_log_hvac_memo.sql`은 각각 파일 본문을 읽어 `execute_sql`의 명시적 transaction으로 적용됐고 `apply_migration`은 호출되지 않았다.
- production catalog에서 Wear Log HVAC mode·intensity column과 CHECK 3개, Place kind column·CHECK, Place HVAC Profile의 9개 column·9개 constraint·2개 index·RLS·policy 3개·table/column grant·COMMENT를 migration 본문과 대조했다. 후속 제거 대상 `observed_hvac_memo` column과 CHECK는 모두 부재했다.
- Wear Log 808행, Place 25행, Profile 10행의 SHA-256 기준선을 읽었다. HVAC mode·intensity 일관성 위반, Place kind 위반, Profile season·mode·intensity·source·memo 위반, 고아 Profile과 generic Place Profile은 모두 0건이었다.
- local 46개와 remote 66개 migration을 이름과 timestamp로 다시 계산했다. 정확히 일치하는 Closet migration은 36개, 같은 이름·다른 timestamp는 기존 문서의 4쌍이 아니라 8쌍, local-only 이름은 감사 대상 2개, 다른 앱의 remote-only migration은 22개다.
- `database-map.md`의 누락된 P6 timestamp 불일치 4쌍을 보충하고, 두 migration의 적용 경로·schema 감사·repair 의미와 전체 CLI sync 경계를 기록했다.

## Decisions

- audit 단계에서는 production migration history를 쓰지 않는다. 이번 작업은 catalog·data·Git·과거 실행 기록 조회와 문서 정정만 수행한다.
- 두 migration은 공식 `--status applied` repair 조건을 충족한다. 다만 repair의 목적은 이미 적용된 사실을 tracking table에 기록하는 것으로 한정하고, SQL 적용이나 전체 `db push` 호환 복구로 해석하지 않는다.
- 두 row만 repair해도 shared Supabase project의 remote-only 22개와 timestamp 불일치 8쌍이 남는다. 전체 CLI 동기화는 다른 저장소 migration 통합과 각 timestamp 쌍의 본문 증거가 필요한 별도 아키텍처 작업으로 분리한다.
- current schema가 local migration 두 개의 최종 결과와 이미 일치하므로 중복 `db pull` migration이나 schema 보정 migration은 만들지 않는다.

## Current Status

- production schema와 업무 데이터는 변경하지 않았다.
- 두 history row가 없는 원인은 확정됐고, 현재 schema·권한·데이터는 targeted repair 전제에 맞는다.
- local migration SHA-256은 P5B `c5d5d427bc662b09a170095ac8f752ccee3636524e83f52b8296c6aa739c7a3a`, memo cleanup `f4288bcf1712c00d3cf6118a81eba1d670e7916ffe331eaa2cd82b30a1628834`다.

## Next Steps

- 다음 승인 단계에서 두 timestamp의 현재 history 부재와 다른 migration activity 0건을 다시 확인하고, 공식 migration repair로 `applied` tracking row만 추가한다.
- repair 뒤 세 대상 table의 row count·SHA-256, catalog·권한·RLS와 전체 remote migration 목록을 재조회해 schema·data 불변과 history row 2개 추가만 검증한다.
- 전체 `db push` 호환이 실제로 필요해질 때 shared project의 migration 소유권과 8개 timestamp 불일치 쌍을 별도 계획으로 감사한다.

## Work Summary - Migration history repair

읽기 전용 감사에서 적용 사실이 확정된 Place HVAC migration 2개를 production migration history에 `applied`로 복구했다. migration 본문은 다시 실행하지 않았고, tracking metadata 두 row만 한 transaction으로 추가했다.

## Work Completed - Migration history repair

- Supabase CLI 2.109.1의 `migration repair` 도움말과 공식 구현을 확인했다. `applied` repair가 local 파일을 statement 단위로 읽어 `version`, `name`, `statements`를 한 transaction에서 기록하는 동작임을 대조했다.
- 작업 환경에는 CLI access token과 DB password가 없어 CLI를 production에 직접 연결하지 않았다. 이미 인증된 Supabase 연결에서 CLI 2.109.1과 같은 row 형식을 만들되, history table 잠금 뒤 전체 66개 row fingerprint와 대상 부재를 다시 확인해야만 실행되는 transaction으로 제한했다.
- `20260809200608_p5b_place_profile_hvac`에는 분리된 statement 25개, `20260809211440_remove_wear_log_hvac_memo`에는 statement 1개를 기록했다. `created_by`, `idempotency_key`, `rollback`은 CLI repair와 같이 별도 값을 만들지 않았다.
- repair 전후를 독립 재조회했다. 기존 66개 history fingerprint `e388af2a1ece2ff19f39604fe9cc55d3`는 그대로이고, 전체 history는 68개가 됐다. 두 target의 DB statements가 local 파일에서 분리한 payload와 정확히 같음을 확인했다.
- Wear Log 808행(`449cea35f864748c8e0f09eb014bfab7`), Place 25행(`64bf00887159ccb7d5ebb42b8568853b`), Place HVAC Profile 10행(`32f86659a5c4420e2dc324c4156b249b`)의 row fingerprint가 전후 동일했다.
- 관련 constraint·index·policy catalog fingerprint `8050c94365a67f0a25221342b0be8c0d`가 동일하고, 값 위반·고아 Profile·generic Place Profile은 모두 0건이었다. Security Advisor 47건과 Performance Advisor 33건의 경고 key 집합도 전후 동일했다.
- `database-map.md`의 production history 수와 local/remote 일치 수를 현재 상태로 갱신하고, 이번 repair가 전체 CLI 동기화는 아니라는 경계를 기록했다.
- 전체 Vitest는 645개 통과·7개 skip, production build는 성공했다. `verify:pages`를 일반 local build 직후 실행했을 때는 workflow가 만드는 `404.html`과 `GITHUB_ACTIONS=true` base path가 없어 각각 실패했고, 실제 Pages workflow와 같은 환경으로 재빌드한 뒤 `index.html` fallback을 추가해 artifact 검증이 통과했다.

## Decisions - Migration history repair

- schema가 이미 두 migration의 최종 합성 결과와 정확히 일치하므로 migration SQL, `db pull`, 보정 migration은 실행하거나 만들지 않는다.
- CLI 인증 부재를 우회해 광범위한 DB 작업을 하지 않는다. 공식 CLI의 history DML만 재현하고, table lock·기준선 조건·transaction·즉시 후검증으로 대상 두 row 외 변경을 차단한다.
- 이번 작업은 적용 사실의 tracking metadata 복구다. 다른 앱의 remote migration 22개와 같은 이름·다른 timestamp 8쌍은 별도 문제로 유지한다.

## Current Status - Migration history repair

- production migration history repair는 완료됐다. local-only migration 이름은 0개이고, 이름과 timestamp가 모두 일치하는 Closet migration은 38개다.
- production schema와 업무 데이터는 변경되지 않았다. 바뀐 production 상태는 `supabase_migrations.schema_migrations`의 두 row뿐이다.
- 기존 history·관련 데이터·catalog·Advisors 불변 검증이 모두 통과했다.

## Next Steps - Migration history repair

- 전체 `db push` 호환이 실제로 필요해질 때 shared project의 migration 소유권과 timestamp 불일치 8쌍을 별도 감사한다.
- 앞으로 migration SQL은 가능한 한 timestamp를 보존하는 공식 CLI 경로로 적용하고, 직접 SQL 적용이 불가피하면 같은 작업 안에서 history 등록까지 검증한다.

## Work Summary - Timestamp 불일치 migration 본문 감사

local/Git과 production에서 논리 이름은 같지만 timestamp가 다른 migration 8쌍의 실제 SQL 본문을 읽기 전용으로 감사했다. 이름 일치만으로 같은 migration이라고 추정하지 않고 production history에 저장된 `statements`와 local 파일을 직접 비교해, 8쌍 모두 본문이 같은 timestamp-only drift임을 확정했다.

## Work Completed - Timestamp 불일치 migration 본문 감사

- 최신 Supabase changelog와 Database Migrations·migration repair 공식 문서를 다시 확인하고, 이번 단계는 history와 파일 조회만 수행했다.
- production history 68개 기준선에서 대상 8개 논리 이름이 각각 정확히 1행이고, 각 행의 `statements` cardinality가 1임을 확인했다. 8개 local version은 모두 부재하고 기록된 production version만 존재한다.
- local SQL 원문과 production `statements`를 UTF-8 BOM 제거, CRLF·CR의 LF 통일, 파일 경계 공백 제거만 적용해 문자 단위로 비교했다. 8쌍 모두 일치했고 raw 차이는 줄바꿈 형식과 파일 끝 newline으로 한정됐다.
- 같은 정규화 본문을 local Node와 production PostgreSQL `pgcrypto`에서 각각 SHA-256으로 계산해 8쌍의 digest가 모두 일치하는지 독립 확인했다.
- Git 최초 추가 commit과 2026-07-29·08-01·08-02·08-06 개발 로그를 대조해 local 파일 버전과 별도 production 적용 버전이 당시부터 각각 기록돼 있음을 확인했다.
- `database-map.md`의 8쌍 표에 본문 감사 결과와 비교 규칙, production 불변 경계, 다음 repair의 기준선을 추가했다.

## Decisions - Timestamp 불일치 migration 본문 감사

- migration 이름과 현재 schema가 비슷하다는 사실만으로 본문 동일성을 판정하지 않는다. production history에 보존된 실제 SQL을 local 원문과 대조한 결과만 증거로 사용한다.
- 줄바꿈과 파일 끝 newline은 SQL 의미를 바꾸지 않는 텍스트 운반 형식 차이로 분류한다. 주석·statement 순서·SQL token을 제거하거나 재배열하는 의미 기반 정규화는 사용하지 않았다.
- audit 단계이므로 production SQL과 migration history를 쓰지 않는다. 이번 결과는 targeted repair의 전제일 뿐 repair 실행 승인은 아니다.
- 후속 history 정렬은 같은 이름의 row를 추가해 중복시키지 않고, 정확한 8쌍과 전체 68행을 transaction guard로 고정해 version을 교체하는 방향으로 설계한다.

## Current Status - Timestamp 불일치 migration 본문 감사

- 8쌍은 모두 SQL 본문이 같은 timestamp-only history drift로 확정됐다.
- production migration history, schema, 업무 데이터는 변경하지 않았다. history는 68행이고 Closet 논리 이름 46개 중 timestamp까지 일치하는 것은 여전히 38개다.
- shared project의 다른 앱 migration 22개는 이 저장소 범위 밖에 그대로 있으므로 전체 `db push` 호환 상태는 아니다.

## Next Steps - Timestamp 불일치 migration 본문 감사

- 다음 승인 단계에서 8쌍의 history version을 local/Git timestamp로 정렬하는 targeted repair의 transaction, 실패 조건, 전후 fingerprint를 확정한다.
- repair를 실행한다면 전체 68행 유지, 각 논리 이름 1행 유지, 기존 SQL payload·schema·업무 데이터 불변을 독립 재조회한다.
- shared project 전체 CLI 동기화가 필요할 때는 다른 앱 migration 22개의 소유권과 저장소 통합 정책을 별도 결정한다.
