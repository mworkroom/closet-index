# 2026-08-07 Closet Index Development Log

## Work Summary

Phase 5 P5A-1의 Transport-conditioned thermal Policy B를 production 동작에 영향을 주지 않는 development 전용 flag 뒤에 연결했다. 통합 전에 기존 Replenishment 삭제 문구 테스트와 CSS 끝 공백을 정리해 전체 baseline을 green으로 복구했고, 합성 fixture와 production read-only matrix로 Policy B의 실제 이동 폭을 확인했다.

## Work Completed

- Replenishment 삭제 확인 문구 테스트를 현재 UI 문구와 일치시켰다.
- `src/styles.css`의 기존 trailing whitespace 2개를 제거했다.
- `recommendOutfits`가 기존 comparator 결과를 먼저 만든 뒤 Policy B가 켜진 경우에만 같은 level 내부를 재정렬하도록 구현했다.
- `VITE_P5A_TRANSPORT_POLICY_B`를 development 전용·기본 false flag로 HOME 계산 경로에 연결했다.
- feature-off 전체 결과·이유·경고·group deep equality와 feature-on 33°C Walk/Car, evidence 0, Transport null, level 경계 회귀 테스트를 추가했다.
- exactContext 2건 이상 근거를 cross-Place Transport보다 높은 confidence로 보존하되 지원 후보를 승격하지 않도록 했다.
- production 6개 table을 고정 SELECT로 읽는 explicit-gate 비교 테스트를 추가했다. tracked report에는 익명 수치만 기록했다.
- 전체 계절 scope 9-input matrix에서 5개 입력, 532개 position 이동과 직접 감점 13개를 확인했다.
- authenticated local HOME에서 같은 origin의 feature-off/on server를 번갈아 실행해 33°C Walk·Car, Place null, Transport null과 `3개 더 보기`를 실제 카드로 비교했다.
- 33°C Walk에서 목표 후보가 첫 일반 추천 화면에서 내려가는 동시에 구매일 tie 때문에 `최근 구매 착장` 세 번째 후보도 바뀌는 partition 경계를 발견했다.
- Place null은 첫 3장에는 변화가 없지만 첫 pagination의 5번째 후보부터 차이가 노출됨을 확인했다.

## Decisions

- 0건 unknown, 1건 weak, 2건 이상 strong이라는 Policy B threshold를 채택했다.
- overall이 target을 지원하고 current Transport가 지원하지 않는 borrowed-only 경우에만 감점한다.
- feature-off에서는 level·warning·reason·group·candidate membership과 baseline comparator를 모두 보존한다. Feature-on에서도 level·warning·reason·전체 candidate membership은 유지하지만 최근 구매 tie 때문에 group membership은 바뀔 수 있다.
- production build에서는 환경값이 있어도 flag가 켜지지 않도록 `import.meta.env.DEV`를 함께 요구한다.
- weak 1건과 Place null 사례에서 이동 폭이 지나치게 커질 수 있으므로 flag는 비활성으로 유지한다.
- 최근 구매 group에 Policy B가 영향을 줘도 되는지는 별도 제품 결정이 필요하므로 이번 단계에서 comparator를 추가 수정하지 않는다.

## Current Status

통합 전 baseline은 76개 test file·396개 test가 통과했다. 최종 검증은 77개 test file·402개 test 통과, production comparison gate 1개 기본 skip, TypeScript 검사와 production build 통과로 완료했다. build에는 기존 500kB 초과 chunk 경고만 남았다. production 비교는 처음 sandbox network EACCES로 실패한 뒤 승인된 network 경로에서 동일한 read-only SELECT로 성공했다. DB, migration, HOME UI와 추천 문구는 변경하지 않았다.

Policy B는 33°C Walk 실패 패턴을 실제 HOME 카드에서도 개선하고 Car·Transport null에서는 baseline을 복구했다. 그러나 weak evidence·Place null·deep-list churn과 최근 구매 group membership 변화의 보수성이 충분하지 않아 production 활성화 준비가 되지 않았다. Browser console error·warning은 없었고 desktop 카드 렌더링과 pagination은 정상 동작했다. Browser viewport override가 390px로 적용되지 않아 mobile-specific breakpoint 검증은 미완료다.

## Next Steps

- Policy B를 최근 구매 partition 전후 어느 위치에 적용할지 결정한다.
- weak evidence의 최대 하락 폭 또는 tie 범위를 제한할지 검토한다.
- inferred return endpoint가 감점에 미치는 영향을 별도 confidence 규칙으로 제한할지 결정한다.
- P5A-2, HVAC, Place Profile, Item observation은 이번 범위에서 계속 보류한다.
## Work Summary - Transport Taxonomy Validation

기존 `도보` 기록이 짧은 근거리 이동과 지속적·빠른 걷기를 한 bucket으로 취급해 thermal evidence를 오염시킬 수 있는지 확인했다. production 데이터는 SELECT로만 감사하고, 앱에 연결되지 않는 test-only 분리 시뮬레이터로 의미 분리와 cold start를 검증했다.

## Work Completed - Transport Taxonomy Validation

- active Transport별 distinct Wear Log 수와 역사적 Walk 207건의 Place·월·계절·온도·체감·반복 분포를 읽기 전용으로 집계했다.
- 가까운 카페 후보 3곳과 영화관 후보의 여름 Transport 분포를 확인하고, 개인 Place·Outfit 이름은 tracked 문서에서 익명화했다.
- `walk_short`, `walk_sustained`, `walk_unclassified`, Car, other, null을 분리하는 순수 시뮬레이터를 추가했다.
- short/sustained 상호 배제, cold start, Car 보존, Place/Transport null, historical null Transport, inferred return, 입력 순서, Wear Log ID dedup fixture를 추가했다.
- Model 0 baseline, Model 1 unsplit Policy B, Model 2 strict split을 production SELECT snapshot에서 비교했다.

## Decisions - Transport Taxonomy Validation

- 역사적 Walk row는 Place 이름이나 날짜만으로 short/sustained를 자동 분류하지 않는다.
- confirmed `walk_short` 기록이 0건이므로 분리가 증거 오염은 막아도 추천 prior를 만들지는 않는다고 판단했다.
- `longWalkCondition`은 신발 적합성 조건으로 유지하고 Transport duration이나 thermal activity의 대용값으로 사용하지 않는다.
- 다음 경계는 UI label과 입력 의미를 확정하는 Transport option 설계이며, historical backfill·HVAC·Place Profile·HOME comparator는 포함하지 않는다.

## Current Status - Transport Taxonomy Validation

생산 Transport row, Wear Log, HOME UI, comparator, feature flag는 변경하지 않았다. Policy B는 계속 비활성이다. 합성 fixture와 익명화 production 비교에서 분리 bucket 간 증거 오염이 없었고, Car 영화관과 Transport null 순서는 보존됐다.

## Next Steps - Transport Taxonomy Validation

- `walk_short`와 `walk_sustained`의 실제 UI label·설명 확정
- 10~20분 이동의 입력 지침 결정
- 207개 역사적 Walk row의 수동 검토 방식과 backfill 여부를 별도 결정
- 새 short-Walk 데이터가 쌓인 뒤 prior 필요성을 재평가
## Work Summary - Human-reviewed Walk Replay Preparation

Transport taxonomy 분리 검증 다음 단계로, 실제 nearby Place와 short Walk 여부를 J가 직접 확인한 최소 행만 test-only replay에 사용할 수 있도록 검토 자료와 실행 gate를 준비했다. 분류나 replay 자체는 수행하지 않았다.

## Work Completed - Human-reviewed Walk Replay Preparation

- 세 café 후보별 첫·마지막 착용일, Walk 수, 여름·고온 기록, distinct Outfit, exact 반복 threshold를 SELECT-only로 집계했다.
- June~August 또는 `temp_out >= 28°C`인 Walk 16건을 실제 라벨이 포함된 비공개 검토표로 출력하고 decision을 비워 두었다.
- simulator가 `walk_short`·`walk_sustained`로 명시 확인된 Wear Log ID만 remap하고, ambiguous·not relevant·미검토 행은 legacy/unclassified로 유지하도록 보강했다.
- J의 exact Place label과 decision map 파일이 있어야만 실행되는 production read-only replay gate를 준비했다.

## Decisions - Human-reviewed Walk Replay Preparation

- 첫 replay는 207건 전체 backfill을 요구하지 않고 nearby 후보의 hot-weather 행만 사용한다.
- Place 이름·날짜·온도로 short/sustained를 자동 추정하지 않는다.
- 기존 Walk는 backfill policy 승인 전까지 legacy/unclassified로 취급한다.
- 실제 Place 선택과 decision map이 없으므로 Model 2 replay를 실행하지 않았다.

## Current Status - Human-reviewed Walk Replay Preparation

검토 대상은 16건이며 decision은 모두 미입력이다. 생산 Transport·Wear Log·HOME·추천·migration·feature flag에는 변경이 없고 Policy B는 disabled 상태다.

## Next Steps - Human-reviewed Walk Replay Preparation

- J가 실제 nearby Place 한 곳을 식별
- 16개 행에 `walk_short`, `walk_sustained`, `ambiguous`, `not relevant` 중 하나를 반환
- 반환 map으로만 28°C·30°C·33°C short Walk와 primary cinema Car를 read-only replay

## Work Summary - Human-reviewed Walk Replay

J가 가까운 두 Place의 검토 행은 short Walk이고 나머지 한 Place의 검토 행은 sustained Walk라고 확인했다. 단일 nearby Place만 받던 test-only gate를 복수 Place 입력으로 확장하고, 확인된 16개 ID만 사용해 production SELECT-only replay를 실행했다.

## Work Completed - Human-reviewed Walk Replay

- `nearbyPlaceLabels` 배열을 받아 각 Place의 exact context를 합치지 않고 별도 시나리오로 실행하도록 replay gate를 확장했다.
- 단수형 입력은 이전 로컬 파일과의 호환을 위해 한 요소 배열로 정규화하되, 빈 값과 중복 Place는 거부하도록 검증했다.
- 기존 boolean 검증 helper가 `false`를 거부하지 못하던 문제를 발견해 replay config와 production project ref 안전 검증을 명시적 assertion으로 교체했다.
- J가 확인한 15건의 `walk_short`와 1건의 `walk_sustained`만 remap하고, 나머지 역사적 Walk는 `walk_unclassified`로 유지했다.
- 두 nearby Place 각각의 28°C·30°C·33°C short Walk와 primary cinema Car를 비교하고, 실제 라벨·이동 방향·근거 count·range·inferred-return sensitivity를 private 출력으로 확인했다.

## Decisions - Human-reviewed Walk Replay

- 두 nearby Place는 같은 short bucket을 사용하지만 exact Place + Transport evidence는 서로 합치지 않는다.
- 33°C 목표 Outfit 복구는 taxonomy split의 유효성을 지지한다.
- weak evidence 1건이 같은 recommendation level 안에서도 7~31계단 하락을 만들 수 있어 현재 Policy B를 production 또는 HOME UI에 노출하지 않는다.
- nearby Place의 Car 기록이 없으므로 의미 없는 합성 Car 시나리오는 추가하지 않았다.

## Current Status - Human-reviewed Walk Replay

리플레이 7개 시나리오 중 nearby short Walk 6개에서 baseline 대비 순서 변화가 있었고 cinema Car는 완전히 동일했다. 33°C 목표 Outfit은 두 nearby Place 모두 2위로 복귀했다. Supabase write, migration, Transport row, Wear Log, HOME UI, recommendation wording, feature flag는 변경하지 않았으며 Policy B는 계속 disabled 상태다.

## Next Steps - Human-reviewed Walk Replay

- weak 1건 증거의 허용 하락 폭 또는 적용 조건을 더 보수적으로 조정할지 별도 fixture로 검토
- taxonomy UI·production data backfill은 별도 승인 전까지 보류
- Policy B는 비활성 상태를 유지하고 현재 단계에서는 local manual QA로 넘기지 않음
## Work Summary - Wear Log Editor V1

Wear Log Editor V1 계획에 따라 기존 `closet_wear_logs`를 직접 조회·편집하는 내부 유지보수 페이지를 구현했다. Phase 5의 역사적 Walk 기록을 한 화면에서 찾고 안전하게 정비할 수 있도록 `/tools/wear-log` 경로를 추가했으며, 새 테이블·RPC·마이그레이션은 만들지 않았다.

## Work Completed - Wear Log Editor V1

- 실제 스키마 감사 결과에 맞춰 Walk를 별도 필드가 아니라 기존 `transport_mode_id` 관계로 표시하고, 날짜·Outfit·장소·Transport·온도·체감·비·장거리 걷기·메모를 표에 배치했다.
- 날짜 범위, Transport, Walk/도보, Transport 미지정, 장소, 텍스트 검색, 최신/과거순 정렬과 25/50/100건 페이지네이션을 구현했다. Supabase snapshot의 Wear Log 조회도 1,000건 단위 페이지 수집으로 보강했다.
- 중앙 컬럼 설정을 기준으로 inline 편집과 행 선택·일괄 편집을 구현했다. 변경은 로컬 pending 상태로 표시되며 변경 취소와 저장 버튼을 제공한다.
- `WearLogPatch`와 partial update 경로를 추가해 저장 시 변경된 필드와 `updated_at`만 갱신하고, 저장 결과를 Context에 반영한 뒤 전체 데이터를 한 번 재조회하도록 구성했다. 실패한 행의 pending 변경은 유지한다.
- `/tools/wear-log`는 일반 하단 내비게이션에 노출하지 않고 넓은 데스크톱 표 레이아웃을 사용하며, 모바일에서는 표 가로 스크롤을 유지한다.
- 필터·pending·취소·일괄 편집·변경 필드만 저장·실패 보존에 대한 유틸리티 및 화면 테스트를 추가했다.

## Decisions - Wear Log Editor V1

- 기존 `closet_wear_logs`, Outfit/Place/Transport 관계, workspace RLS를 재사용했다. 이번 요구는 기존 구조로 충분하므로 새 schema object를 추가하지 않았다.
- 기존 Wear Log 편집 API가 전체 입력을 저장하므로 editor 전용 partial update를 추가했다. 이로써 사용자가 건드리지 않은 필드는 덮어쓰지 않는다.
- bulk 저장은 독립적인 행별 update를 병렬 실행하고 마지막에 한 번만 재조회한다. 반환된 갱신 행도 즉시 Context에 반영해 선택 수가 늘어도 불필요한 전체 snapshot waterfall을 피한다.
- 현재 스키마에는 별도 Walk 분류 컬럼이 없으므로 `Transport 미지정`을 missing Walk classification 필터로 명시했다. 자동 분류나 데이터 backfill은 구현하지 않았다.

## Current Status - Wear Log Editor V1

기능 구현과 로컬 검증을 완료했다. `npm run typecheck`, 전체 Vitest 81개 파일(420개 테스트) 통과, `npm run build` 성공을 확인했다. Build에는 기존 단일 chunk 500kB 초과 경고만 남았다. VITE_DEMO_MODE로 `/tools/wear-log`를 데스크톱·390×844 모바일에서 렌더링했고, Walk 필터·inline pending·변경 취소·콘솔 오류/경고 없음과 Supabase 읽기 전용 schema/count/policy 확인을 완료했다. production Wear Log 데이터에는 쓰기를 수행하지 않았다.

## Next Steps - Wear Log Editor V1

- 실제 인증 세션에서 J님이 `/tools/wear-log`를 열어 Walk 기록을 확인하고, 필요할 때만 partial update 저장을 수행한다.
- 향후 Walk taxonomy가 별도 persisted field로 확정될 경우, 이 편집기의 중앙 컬럼 설정과 필터에 그 필드를 추가할지 별도 결정한다.

## Work Summary - Approved Walk Transport Taxonomy

미래 Wear Log에서 짧은 도보와 지속 도보의 열부하 근거가 섞이지 않도록 기존 `transport_mode_id` 관계만 재사용하는 입력 UI를 준비했다. production SQL이나 과거 데이터는 변경하지 않고, 기존 Walk ID를 보존하는 수동 전환 runbook과 전환 전 legacy 편집 안전장치를 구현했다.

## Work Completed - Approved Walk Transport Taxonomy

- Transport 선택지를 `도보 · 근거리`, `도보 · 지속`, 차, 지하철, 버스 순서로 정렬하는 공용 helper를 추가했다.
- 새 Wear Log에서는 전환 전의 raw `도보`를 숨기고 navigation state가 legacy ID를 전달해도 자동 선택하지 않도록 했다.
- 기존 legacy Walk 기록 편집에서는 같은 ID를 `도보 · 기존 기록`으로만 표시해 다른 필드 수정이 Transport 변환을 강제하지 않도록 했다.
- 일반 Wear Log 편집과 내부 `/tools/wear-log` 모두 short/sustained 변환 시 선택한 ID를 그대로 create, full update, partial update payload에 전달하도록 검증했다.
- 10~20분 이동은 시간만이 아니라 실제 체열 증가를 기준으로 short/sustained를 고르도록 UI 도움말과 계획 문서에 기록했다.
- 기존 Walk row rename, 새 short row 1개 insert, Wear Log/reference count 검증과 conditional rollback을 포함한 수동 SQL runbook을 추가했다.
- 수동 전환 후에도 read-only audit가 `도보 · 근거리`와 `도보 · 지속`을 모두 Walk로 인식하고 short distinct count를 별도로 보고하도록 감사 도구를 확장했다.

## Decisions - Approved Walk Transport Taxonomy

- 새 column, relation, legacy row, `walk_unclassified` production option을 만들지 않고 기존 `closet_transport_modes`와 `closet_wear_logs.transport_mode_id`를 재사용한다.
- production 전환은 기존 Walk row를 같은 ID의 `도보 · 지속`으로 rename하고 stable ID의 `도보 · 근거리` row만 하나 추가한다.
- 초기 수동 backfill은 J가 확인한 16건으로 제한한다. 15건은 short로 재지정하고 sustained 1건은 기존 ID가 유지되므로 불필요한 update를 하지 않는다.
- Policy B는 계속 disabled이며 다음 ranking 실험에서는 1건을 informational-only, 2건 이상을 ranking 영향 후보로 본다.

## Current Status - Approved Walk Transport Taxonomy

코드·문서·수동 runbook 준비와 집중 회귀 테스트를 완료했다. Supabase SQL, migration, production Transport row, Wear Log 데이터, HOME ranking과 feature flag는 변경하지 않았다. 전환 전 legacy 기록과 전환 후 두 Walk option을 모두 처리할 수 있다.

## Next Steps - Approved Walk Transport Taxonomy

- J가 runbook의 SELECT preflight를 실행하고 workspace·기존 Walk ID·reference count를 확인
- J 승인 후에만 기존 row rename과 short row insert를 수동 적용
- editor에서 15건 short를 재지정하고 sustained 1건의 기존 ID 표시를 확인
- read-only audit를 다시 실행해 Transport별 distinct Wear Log count를 검증

## Work Summary - Wear Log Editor Wide Table Layout

Wear Log Editor에서 열이 늘어날 때 화면 양쪽의 사용하지 않는 폭과 표 가로 탐색 문제가 확인되어, 편집기 전용 프레임과 표 스크롤 동작을 보강했다.

## Work Completed - Wear Log Editor Wide Table Layout

- 편집기 전용 `wide` 프레임을 고정 최대 폭 대신 뷰포트 전체 폭으로 확장했다.
- 표 컨테이너에 좌우 스크롤, 터치 스크롤, 스크롤바 공간 예약을 적용하고 표는 내용 기반 폭을 유지하도록 변경했다.
- 표 스크롤 영역에 키보드 포커스와 접근성 라벨을 추가하고, 추가 열을 확인할 수 있다는 안내 문구를 표시했다.
- `npm run typecheck`, 전체 Vitest 81개 파일(420개 테스트), `npm run build`를 다시 실행했다. 데모 화면은 1280×720과 390×844에서 확인했다.

## Decisions - Wear Log Editor Wide Table Layout

- 일반 앱 화면의 모바일형 프레임은 유지하고, 열이 많은 내부 편집기만 전체 폭을 사용하도록 `app-frame--wide`에 범위를 한정했다.
- 페이지 전체에 가로 오버플로를 허용하지 않고 표 래퍼에만 스크롤을 두어 모바일에서 필터와 헤더가 옆으로 밀리지 않게 했다.
- 고정된 열 수를 전제로 폭을 계속 수정하지 않도록 `max-content`와 최소 폭을 조합해 향후 컬럼 추가에도 동일한 스크롤 구조를 유지한다.

## Current Status - Wear Log Editor Wide Table Layout

데스크톱에서 프레임이 화면 폭을 사용하고 표의 가로 스크롤이 동작한다. 모바일에서는 문서 전체 가로 오버플로 없이 표 영역만 스크롤되며, 브라우저 콘솔 오류와 경고는 확인되지 않았다.

## Next Steps - Wear Log Editor Wide Table Layout

- 실제 인증 세션에서 향후 추가될 컬럼이 표 래퍼 안에서 동일하게 탐색되는지만 확인한다.
