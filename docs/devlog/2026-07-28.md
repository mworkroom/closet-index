# 2026-07-28 Closet Index Development Log

## Work Summary

J가 과거 아이폰 앱 화면을 정사각형으로 캡처해 만든 누끼의 용량이 약 100~1000KB로 다양하다는 점을 확인했다. 새 Batch 업로드 전에 입력 파일 크기를 사람이 맞추지 않아도 앱 표시용 결과만 일정한 범위로 정규화하고, 고해상도 원본이 Supabase Storage에 계속 쌓이지 않도록 이미지 준비·업로드 계약을 구현 정책과 일치시켰다.

## Work Completed

- 공통 WebP 인코더를 추가해 목표 용량을 넘으면 다음 quality·해상도 후보로 자동 재인코딩하도록 했다.
- Item cutout은 1600px·quality 90부터 시작해 500KB 이하를 목표로 압축한다. 마지막 후보에서도 700KB를 넘으면 업로드 준비를 차단한다.
- 로컬 입력 원본과 정규화 original PNG를 원격 asset plan·metadata에서 제외하고, Item마다 안정적인 UUID와 경로의 cutout WebP 하나만 업로드 대상으로 만들었다.
- Outfit preview는 900×1200px를 유지하면서 quality 90부터 42까지 단계적으로 낮춰 350KB 이하로 만들며, 목표를 충족하지 못하면 업로드 준비를 차단하도록 했다.
- 인코딩 report에 선택된 quality·최대 크기·실제 용량과 전체 시도 이력을 기록하도록 했다.
- 고용량 노이즈 fixture가 첫 후보를 넘었을 때 더 작은 후보로 내려가는 테스트와, 로컬 원본을 업로드 자산으로 만들지 않는 테스트를 추가했다.
- Batch 0 실제 누끼 7개를 dry-run해 원격 후보가 cutout 7개뿐이며 29~437KB, 총 약 1.07MB, 경고 0건인지 확인했다.
- composition v2를 다시 합성해 900×1200 preview가 quality 90 첫 후보에서 약 157KB로 목표를 충족하는지 확인했다.
- 전체 Vitest 17개 파일·65개 테스트, 이미지 스크립트 구문 검사와 production build를 수행했다.

## Decisions

- 아이폰 캡처나 누끼 입력 파일이 100~1000KB로 다양해도 입력 단계에서는 허용한다. alpha trim·2.5% 안전 여백 뒤의 앱용 WebP 결과만 용량 기준으로 판정한다.
- 기존 결과와 재실행 호환성을 위해 첫 후보는 기존과 같은 1600px·quality 90·alpha quality 100을 유지한다. 목표를 넘는 파일에만 더 강한 압축을 적용한다.
- 500KB는 일반 목표이고 700KB는 안전 상한이다. 500~700KB 결과는 report 경고와 함께 허용하지만 700KB 초과 결과는 업로드하지 않는다.
- 누끼 생성 자체는 이번 작업 범위에 포함하지 않는다. 입력은 기존처럼 배경이 투명하고 Item 전체가 보이는 이미지여야 하며, 정사각형 프레임과 원본 해상도는 자동 trim 이후 배치 기준에 영향을 주지 않는다.
- 기존 원격 Batch 0 original 7개는 삭제하지 않는다. 새 도구로 cutout·preview만 사용하는 흐름을 먼저 검증한 뒤 별도 정리 단계에서 삭제 여부를 결정한다.

## Current Status

문서에만 있던 original 미보관 정책과 용량 자동 조정이 코드와 테스트에 반영됐다. Batch 0 dry-run과 v2 합성은 통과했으며 Supabase DB·Storage의 기존 객체나 metadata는 변경하지 않았다. production build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있고 이미지 파이프라인 실패는 아니다. 변경은 로컬 working tree에 있으며 아직 커밋·푸시·배포하지 않았다.

## Next Steps

- J가 준비 중인 새 누끼를 local-only manifest에 연결하고 dry-run report에서 선택 quality·해상도·용량을 검토한다.
- 결과가 적절하면 cutout만 `--apply` 업로드하고 새 Outfit preview를 별도 composition version으로 생성한다.
- 새 정책의 원격 표시와 재실행을 확인한 뒤 기존 Batch 0 original 7개의 안전한 정리 여부를 결정한다.

## Work Summary - Batch 2 Cutout과 실시간 Thumbnail

J가 준비한 투명 PNG 12개를 기존 Item·Outfit 관계에 연결해 private Supabase Storage에 cutout만 업로드했다. 상세 화면에서 조정한 composition v2 비율과 위치가 메인 목록의 구형 저장 preview에는 반영되지 않던 문제를 해결하기 위해, 모든 Item cutout이 준비된 Outfit은 목록에서도 현재 합성 규칙을 우선 사용하도록 변경했다.

## Work Completed - Batch 2 Cutout과 실시간 Thumbnail

- 12개 파일의 실제 DB Item UUID·이름·카테고리와 지정한 pilot Outfit 6개의 relation을 원격에서 읽기 전용으로 대조했다.
- 모든 입력이 alpha 채널과 투명 영역을 가진 PNG인지 확인하고 `assets/private/phase-1b/batch-2/` 아래 local-only 원본·manifest·준비 결과로 보관했다.
- 한 Item 묶음이 여러 Outfit에 재사용되는 batch를 지원하도록 준비 도구가 `outfits` UUID 목록의 relation 합집합을 검증하게 확장했다. 기존 단일 `outfit` manifest도 계속 지원한다.
- 드라이런 결과 12개 cutout이 12~121KB, 총 약 532KB, 경고 0건으로 준비됐다. 원본 12개는 원격 asset plan에 포함하지 않았다.
- private Storage에 cutout 12개와 `ready` metadata 12개를 업로드했다. 첫 적용 시 sandbox 네트워크가 `EACCES`로 차단됐지만 relation 검증 단계에서 멈춰 원격 쓰기는 일어나지 않았고, 승인된 네트워크 경로로 재실행해 완료했다.
- `OutfitVisual`이 Outfit의 모든 Item cutout을 찾으면 저장 preview보다 `LayeredOutfitPreview`를 우선 렌더링하도록 변경했다. 일부 이미지가 없으면 저장 preview, 그것도 없거나 실패하면 기존 스와치 fallback을 유지한다.
- 로그인된 로컬 LOOKBOOK에서 지정 pilot 6개와 Batch 0 카드가 실시간 합성을 사용하고, 필터 결과 6개에서 이미지 29개가 모두 로드되는지 확인했다. 전체 데이터에서는 18개 Outfit이 완전한 cutout 세트를 갖췄다.
- 전체 Vitest 17개 파일·67개 테스트, 이미지 도구 3개의 구문 검사, production build를 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남았다.

## Decisions - Batch 2 Cutout과 실시간 Thumbnail

- 새 Outfit 행을 임의로 만들지 않고 기존 507개 Outfit의 Item 재사용 관계를 활용한다. 이번 12개만으로 지정 pilot 6개와 추가 기존 조합까지 즉시 시각화할 수 있기 때문이다.
- 목록 thumbnail은 완전한 cutout 세트가 있을 때 composition v2를 실시간 합성한다. 별도 preview 재생성 없이 상세 위치 보정이 즉시 목록에도 반영되며, 저장 preview는 불완전한 이미지 세트의 안전한 fallback으로 남긴다.
- J가 부른 Batch 2 명칭은 local-only 작업 폴더에서 유지하되, 기존 계획 문서의 누끼 준비 순서에서는 같은 12개가 Batch 1이므로 두 명칭을 함께 기록한다.

## Current Status - Batch 2 Cutout과 실시간 Thumbnail

원격에는 ready cutout 19개가 있고 Storage 객체는 27개다. 이번 대상 12개 metadata와 객체가 모두 존재하며 지정 pilot 6개는 각 Outfit의 전체 Item 수와 ready cutout 수가 일치한다. 브라우저 콘솔 오류는 없었고 전체 테스트·build가 통과했다. J의 요청에 따라 검증된 코드와 문서를 main에 커밋·푸시하고 GitHub Pages 배포를 확인하는 단계로 전환했다.

## Next Steps - Batch 2 Cutout과 실시간 Thumbnail

- J가 iPhone에서 18개 합성 thumbnail을 확인할 수 있도록 요청 시 커밋·푸시한 뒤 Pages 배포 결과를 검증한다.
- 실사용에서 어긋나는 Outfit만 4px 화살표 보정으로 수정하고, 반복되는 오차는 카테고리 slot 기본값 문제인지 구분한다.

## Work Summary - composition v3 종횡비 정규화와 하단 간격

Batch 2의 세로형 후드 재킷과 일반 상의가 Batch 0의 가로형 가디건보다 지나치게 크게 보이는 현상을 조사했다. 기존 composition v2가 카테고리마다 같은 비례 상자를 제공했지만, alpha trim 이후의 실제 종횡비에 따라 보이는 높이가 크게 달라지는 것이 원인이었다. J의 `lookbook3.png`처럼 하의와 신발 사이에도 기본 공간이 필요하다는 실사용 기준을 함께 반영했다.

## Work Completed - composition v3 종횡비 정규화와 하단 간격

- 기존 v2를 보존하고 `outfit-composition.v3.json`을 추가했다.
- 브라우저 합성이 cutout metadata의 실제 너비·높이로 비율 유지 fit 결과를 계산하도록 변경했다.
- 아우터는 최대 표시 높이 500px, 아우터가 없는 일반 상의는 440px로 제한했다. 가로형 Batch 0 가디건의 승인된 너비와 높이는 유지된다.
- 하의·원피스 끝과 신발 시작점에 32px 기본 간격을 적용했다. 기본 관계를 먼저 계산한 뒤 저장된 위치 보정을 적용해 4px 화살표 조정과 원위치 기능을 유지했다.
- 로컬 Sharp 합성 도구에도 같은 최대 높이와 하단 간격 계산을 적용하고 composition fingerprint에 새 규칙을 포함했다.
- Batch 2의 문제 화면 3개 조합과 Batch 0을 local-only 원본으로 재합성해 시각 검증했다. 개인 이미지와 생성 결과는 `assets/private/` 안에만 유지했다.
- 전체 Vitest 17개 파일·70개 테스트와 production build를 수행했다.

## Decisions - composition v3 종횡비 정규화와 하단 간격

- 아우터 전체 배율을 일괄 축소하면 승인된 가로형 가디건까지 작아지므로, 너비 배율은 유지하고 세로형 Item에만 작동하는 최대 표시 높이를 사용한다.
- 하의와 신발 간격은 특정 Outfit의 저장값이 아니라 900×1200 canvas의 공통 기본값으로 둔다. 양말이 있으면 간격 구간에 양말이 보일 수 있지만 하의와 신발의 기준 관계는 동일하다.
- composition 기본 규칙이 바뀌었으므로 기존 v2를 덮어쓰지 않고 version 3으로 분리한다. 원격 preview와 Storage metadata는 이번 작업에서 변경하지 않는다.

## Current Status - composition v3 종횡비 정규화와 하단 간격

후드 재킷의 표시 높이는 약 692px에서 500px로, 단독 일반 상의는 약 577px에서 440px로 줄었다. 실제 Batch 2 합성에서 하의와 신발 사이 32px 기본 간격이 확인됐고 Batch 0 가디건 크기도 유지됐다. 전체 테스트는 통과했고 production build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 변경은 아직 커밋·푸시·배포하지 않았다.

## Next Steps - composition v3 종횡비 정규화와 하단 간격

- J의 로컬 결과 확인 후 요청이 있으면 커밋·푸시하고 GitHub Pages 배포를 검증한다.
- iPhone 실사용에서 일부 Item만 예외적으로 어긋나면 기존 4px 위치 보정을 사용하고, 같은 카테고리에서 반복되면 v3 기본값을 재조정한다.

## Work Summary - Outfit 상세 요약과 메뉴 레이어

iPhone Safari에서 위치 조정용 착장 이미지가 고정 하단 메뉴 위로 그려지는 문제와, Outfit 상세 상단 카드 오른쪽 공간이 선호도 한 항목만 표시해 비어 보이는 문제를 수정했다. J가 지정한 순서대로 선호도·마지막 착용·착용 횟수를 이미지 옆에서 바로 확인할 수 있게 했다.

## Work Completed - Outfit 상세 요약과 메뉴 레이어

- 착장 미리보기 컨테이너에 독립 stacking context를 적용해 Item별 z-index가 미리보기 내부에서만 작동하도록 했다.
- 하단 메뉴 z-index를 100으로 올려 스크롤 중인 착장 이미지와 위치 조정 미리보기보다 항상 위에 표시되도록 했다.
- Outfit 상세 identity card 오른쪽을 동일 높이의 3행 요약 영역으로 변경했다.
- `품질 상태`를 `선호도`로 바꾸고, 선호도·마지막 착용·착용 횟수를 실제 Outfit과 Wear Log 집계값으로 표시했다.
- 마지막 착용 날짜를 `M/D/YY` 형식으로 표시하는 공통 formatter와 테스트를 추가했다. 날짜가 없으면 `기록 없음`, 착용 기록이 없으면 `0회`를 유지한다.
- 기존 상세 화면 아래의 중복 착용 횟수·마지막 착용 grid를 제거했다.
- 390×844 로컬 데모 화면에서 3행 요약 카드와 하단 메뉴 stacking을 시각 확인했다.

## Decisions - Outfit 상세 요약과 메뉴 레이어

- Item 레이어의 상대 z-index 순서는 유지하되 페이지 전체 UI와 경쟁하지 않게 컨테이너에서 격리한다. 메뉴 숫자만 높이는 것보다 착장 내부와 앱 UI의 stacking 책임을 분리하는 것이 안전하다.
- 요약 숫자는 화면 예시를 하드코딩하지 않고 기존 `getOutfitStats`가 계산한 실제 Wear Log 수와 마지막 날짜를 사용한다.
- 날짜는 J의 예시 `7/18/26`에 맞춰 월·일 앞자리 0을 생략한다.

## Current Status - Outfit 상세 요약과 메뉴 레이어

모바일 상세 카드에서 왼쪽 이미지는 기존 3:4 영역을 유지하고 오른쪽 공간은 선호도·마지막 착용·착용 횟수 세 행으로 채워진다. 로컬 데모에서는 `Favorite`, `5/3/26`, `2회`가 실제 기록에 맞게 표시됐고 하단 메뉴가 콘텐츠보다 위에 유지됐다. 전체 Vitest 17개 파일·72개 테스트와 production build가 통과했으며 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 원격 DB·Storage는 변경하지 않았다.

## Next Steps - Outfit 상세 요약과 메뉴 레이어

- J의 요청이 있으면 앞선 composition v3 변경과 함께 커밋·푸시·배포한다.

## Work Summary - HOME 착장 중심 카드

HOME 추천 카드에서 Outfit 제목과 아이템명 문장이 이미지보다 넓은 공간을 차지하던 구성을 바꿨다. J가 착장 자체를 먼저 보고 오른쪽에서 판단에 필요한 최소 정보만 확인할 수 있도록, 카드 왼쪽 2/3을 착장 이미지에 배정하고 오른쪽 1/3을 요약 정보로 구성했다.

## Work Completed - HOME 착장 중심 카드

- HOME의 최근 구매·추천·시험 착장·온도 근거 미확인 구역에 전용 `home` 카드 레이아웃을 적용했다.
- HOME 카드에서 Outfit 제목, 아이템명 문장, 이동 화살표를 시각적으로 제거했다. 제목 데이터는 상세 이동 링크의 접근성 이름과 이미지가 없는 fallback 설명에 유지했다.
- 오른쪽 요약란에 선호도, 착용 횟수, `M/D/YY` 형식의 최근 착용일, 추천 수준, 적정 온도 범위, OK 횟수를 세로로 표시했다.
- 직접 착용 근거가 없는 Outfit은 `시험 착장`으로 구분하고, 유사 착장의 부분 근거 수준·온도 범위·OK 횟수를 표시하도록 했다.
- 추천 결과에 직접 착용한 OK 관측 횟수를 구조화된 `okObservationCount` 필드로 추가해 화면이 추천 문장 문자열을 해석하지 않도록 했다.
- HOME 카드 전용 렌더링 테스트와 추천 OK 횟수 테스트를 추가했다.
- 390×844 로컬 실제 데이터 화면에서 카드 폭 335px, 이미지 222px, 요약란 111px의 2:1 비율과 텍스트 잘림 여부를 확인했다.
- iPhone Safari에서 시험 착장의 정보 줄 수가 많아지면 왼쪽 3:4 이미지 비율이 grid 열 너비를 역산해 이미지 영역이 약 절반으로 줄어드는 현상을 확인하고, 카드 전체를 9:8 비율로 고정한 뒤 열 너비를 66.6667%·33.3333%로 명시했다.

## Decisions - HOME 착장 중심 카드

- 제목은 데이터에서 삭제하지 않고 HOME에서만 숨긴다. Lookbook과 상세 화면은 이번 변경 범위에서 유지하며, 이미지가 없는 소수의 착장도 기존 색상 fallback을 계속 사용한다.
- 직접 착용 근거와 유사 착장 근거를 같은 `추천 높음`으로 표시하지 않는다. 시험 착장은 직접 검증되지 않았다는 사실을 유지해 추천 신뢰도를 오해하지 않게 한다.
- 오른쪽 1/3의 좁은 폭에서도 판단 정보가 한눈에 들어오도록 문장형 선택 근거 대신 짧은 구조화된 항목을 사용한다.
- 카드 높이는 왼쪽 2/3 영역이 3:4 캔버스가 되는 9:8 비율로 카드 자체에서 결정한다. 이미지 요소의 `aspect-ratio`가 Safari grid 열 계산에 개입하지 않도록 이미지 영역은 카드 높이를 그대로 채운다.

## Current Status - HOME 착장 중심 카드

HOME 카드가 착장 이미지 2/3, 정보 1/3로 표시되며 제목 없이 `Favorite`, `착용 7회`, `최근 10/2/25`, `추천 높음`, `13~26°C 적정 범위`, `OK 8회`처럼 실제 데이터가 표시된다. 390px 화면에서 직접 추천과 정보가 더 많은 시험 착장을 각각 측정했으며 모두 카드 335px, 이미지 222px, 요약란 111px를 유지했다. 전체 Vitest 18개 파일·73개 테스트와 production build가 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 원격 DB·Storage와 배포 상태는 변경하지 않았다.

## Next Steps - HOME 착장 중심 카드

- J가 iPhone 실사용 화면에서 이미지와 정보 비중을 확인한 뒤 필요하면 글자 크기나 오른쪽 여백을 미세 조정한다.
- 요청이 있으면 현재 로컬 변경을 범위별로 확인한 후 커밋·푸시·배포한다.

## Work Summary - Closet·Lookbook 제목 없는 3열 카드

Item 이름은 검색을 위해 계속 관리하되 Closet 목록에서는 이름을 표시하지 않고, 이름이 없는 Outfit도 Lookbook에서 시각 정보 중심으로 탐색할 수 있도록 두 목록을 같은 최소 정보 원칙으로 정리했다. 한 화면에서 더 많은 옷과 착장을 비교할 수 있게 두 화면 모두 한 줄 3개 카드 구성을 사용한다.

## Work Completed - Closet·Lookbook 제목 없는 3열 카드

- Closet의 세로 행 목록을 3열 카드 grid로 변경했다.
- Item 카드는 정사각형 이미지 아래에 `착용 n회`, `최근 M/D/YY` 두 줄만 표시한다.
- Lookbook의 기존 3열·3:4 착장 이미지는 유지하고, 카드 아래의 Outfit 제목·아이템명·선호도 badge를 제거해 같은 두 줄 통계만 표시한다.
- 착용 기록이 없으면 `착용 0회`, `최근 기록 없음`으로 표시한다.
- Item 이름과 Outfit의 내부 label은 삭제하지 않고 검색 조건, 상세 이동 링크의 접근성 이름, 이미지 alt와 fallback 설명에 유지했다.
- Closet과 Lookbook의 제목 비표시·통계 표시·3열 grid를 검증하는 렌더링 테스트를 추가·수정했다.
- 390×844 로컬 실제 데이터 화면에서 Closet 364개 Item과 Lookbook 250개 Outfit의 3열 폭, 이미지 비율, 두 줄 요약 잘림 여부를 확인했다.

## Decisions - Closet·Lookbook 제목 없는 3열 카드

- Item은 이름을 계속 입력하고 검색에 사용하지만 목록 화면의 시각적 식별은 이미지가 담당한다. Outfit은 별도 이름 입력을 전제로 하지 않으며 구성 Item에서 만든 label은 내부 접근성 용도로만 사용한다.
- 정사각형 템플릿으로 준비되는 Item 이미지는 1:1로, 900×1200 합성 규칙을 사용하는 Outfit 이미지는 3:4로 표시한다.
- Retired 여부와 선호도 같은 추가 상태는 이번 목록 카드에 넣지 않는다. 필터와 상세 화면에서 확인할 수 있으므로 목록은 착용 횟수와 최근 날짜만 유지한다.

## Current Status - Closet·Lookbook 제목 없는 3열 카드

390px 화면에서 두 grid 모두 각 열이 약 107px로 동일했다. Closet Item 카드는 이미지 약 106×106px와 46px 요약 영역, Lookbook Outfit 카드는 이미지 약 106×142px와 46px 요약 영역으로 표시됐으며 가로 overflow와 텍스트 잘림이 없었다. 전체 Vitest 18개 파일·74개 테스트와 production build가 통과했고 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 로컬 변경 상태로 유지하며 원격 DB·Storage는 변경하지 않는다.

## Next Steps - Closet·Lookbook 제목 없는 3열 카드

- J가 iPhone에서 실제 cutout이 있는 Item과 합성 Outfit의 가독성을 확인한 뒤 필요하면 카드 간격이나 요약 글자 크기를 미세 조정한다.
- 요청이 있으면 변경 파일을 확인한 후 커밋·푸시·배포한다.

## Work Summary - Outfit Item 크기 조절

위치 화살표만으로는 원본 이미지의 체감 비율 차이를 보정할 수 없어, 선택한 Item을 Outfit 안에서 축소·확대하고 같은 배치값으로 저장할 수 있게 했다. 새 필드를 추가하지 않고 초기 Phase 1 스키마부터 존재하던 `closet_outfit_items.scale`을 기존 위치 편집 흐름에 연결했다.

## Work Completed - Outfit Item 크기 조절

- 위치 조정 card의 화살표 왼쪽과 오른쪽에 `−` 축소, `+` 확대 버튼을 추가했다.
- 크기는 5% 단위로 50~150% 범위에서 조절하며 현재 좌우·상하 좌표와 함께 퍼센트를 표시한다.
- 원위치 버튼이 좌표 `0, 0`과 크기 100%를 함께 복원하도록 변경했다.
- 편집 중인 scale을 browser 합성 미리보기에 즉시 반영하고, 저장 시 `position_x`, `position_y`, `scale`을 함께 갱신하도록 Supabase·demo repository를 확장했다.
- 인증 사용자가 `scale` 열만 추가로 갱신할 수 있도록 `20260727205324_allow_outfit_item_scale_updates.sql` migration을 원격 `ddlwainwollvpaeccpty`에 적용했다.
- 원격 권한을 재조회해 authenticated는 위치와 scale을 수정할 수 있고, `z_index`와 anon scale 수정 권한은 계속 차단됨을 확인했다.
- Supabase schema contract, repository, editor interaction, composition scale 테스트를 갱신했다.
- 390×844 로컬 실제 데이터 화면에서 `− / 화살표 / +` 배치, 105% 즉시 미리보기, 가로 overflow, console 오류를 확인하고 `design-qa.md`에 비교 결과를 기록했다.

## Decisions - Outfit Item 크기 조절

- DB에는 이미 비율형 `scale` 값이 있으므로 별도 `scale_percent` 컬럼을 만들지 않는다. UI에서만 100% 형태로 변환해 표시한다.
- 한 번의 탭으로 차이를 판단하기 쉬우면서 과도하게 튀지 않는 5% 단위를 사용한다.
- Item이 화면에서 사라지거나 Outfit 전체를 지나치게 덮지 않도록 편집 범위를 50~150%로 제한한다.
- 확대·축소는 선택 Item에만 적용하고 기존 카테고리 slot과 layer 순서는 유지한다.
- 실제 데이터 변경을 피하기 위해 browser QA에서는 확대 미리보기까지만 확인하고 저장 버튼은 누르지 않았다. 저장 경로는 repository 단위 테스트와 원격 열 권한 조회로 검증했다.

## Current Status - Outfit Item 크기 조절

모바일 위치 조정 card에서 선택한 Item을 5%씩 축소·확대할 수 있고 위치와 크기를 한 번에 저장할 준비가 됐다. 전체 Vitest 18개 파일·75개 테스트와 production build가 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 원격 DB migration은 적용됐지만 앱 코드 변경은 아직 커밋·푸시·배포하지 않았다.

## Next Steps - Outfit Item 크기 조절

- J가 로컬 또는 다음 배포본에서 실제 Item별 크기 보정을 사용해 보고 5% 단위와 50~150% 범위가 충분한지 확인한다.
- 요청이 있으면 현재 누적 변경 파일을 범위별로 확인한 뒤 커밋·푸시·배포한다.

## Work Summary - composition v4 하단 간격과 가방 위치

크기 조절 기능으로 Item별 예외는 직접 보정할 수 있게 됐지만, 기본 합성에서 신발이 조금 크고 하의와 신발 사이가 넓으며 가방이 주열에서 멀다는 반복 가능한 차이를 확인했다. 기존 v3를 보존하고 신발·하의·가방의 공통 초기값을 v4로 조정했다.

## Work Completed - composition v4 하단 간격과 가방 위치

- 신발 기본 `visualScale`을 `0.28`에서 `0.266`으로 바꿔 v3 크기의 정확히 95%로 축소했다.
- 하의·원피스와 신발 사이 기본 간격을 32px에서 16px로 줄였다.
- 신발 slot을 900×1200 canvas에서 12px 위로 이동했다. 간격 계산과 결합해 하의는 아래로 더 내려오고 신발은 조금 올라가도록 했다.
- 가방 `side-bag` slot을 40px 왼쪽으로 이동했다. 가방의 최상위 z-index 80은 유지해 주열 의상과 일부 겹칠 수 있게 했다.
- 기존 `outfit-composition.v3.json`은 그대로 보존하고 `outfit-composition.v4.json`을 추가했으며 browser 합성이 v4를 사용하도록 변경했다.
- 신발 배율·하단 16px 간격·가방 중심점·최상위 z-index를 검증하는 composition 테스트를 추가했다.
- 실제 `카브라 티 + 플리츠 스커트 퍼플 + 크로스백 블랙 + 크록스 클래식` 착장을 390×844 로컬 화면에서 확인했다.

## Decisions - composition v4 하단 간격과 가방 위치

- 간격 16px을 모두 하의 이동에 쓰지 않고 신발 slot을 12px 위로 함께 옮긴다. 실제 Batch 2 Item 비율에서는 v3 대비 하의 끝이 약 13.9px 내려가고 신발 윗부분이 약 2.1px 올라가 J가 설명한 방향을 모두 만족한다.
- 신발 크기는 별도 예외 목록이 아니라 `Shoes` 카테고리 공통 초기값으로 조정한다. 저장된 Item scale은 이 초기값에 계속 곱해지므로 개별 보정도 유지된다.
- 가방은 주열과 겹치지 않게 분리하는 것보다 Outfit 전체의 시각적 결속을 우선해 40px 왼쪽 이동한다.

## Current Status - composition v4 하단 간격과 가방 위치

실제 대상 착장에서 하의와 신발 사이 계산 간격은 16px, 가방 중심은 canvas x=685로 확인됐다. 모바일 화면에서도 신발이 작아지고 가방이 의상에 가까워진 결과가 표시되며 console 오류가 없다. 전체 Vitest 18개 파일·77개 테스트와 production build가 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 코드 변경은 아직 커밋·푸시·배포하지 않았다.

## Next Steps - composition v4 하단 간격과 가방 위치

- J가 로컬 상세 화면에서 다른 신발·하의 조합도 확인한 뒤 공통 초기값을 확정한다.
- 요청이 있으면 현재 누적 변경을 확인해 커밋·푸시·배포한다.

## Work Summary - 착장 조정 저장 후 되돌아감 수정

착장 상세 화면에서 위치·크기를 저장하면 로딩 표시 뒤 이전 모습으로 돌아오는 현상을 조사했다. Supabase API 로그와 실제 행을 확인한 결과 저장 요청은 `PATCH 200`으로 성공했고 값도 DB에 남아 있었으므로, 저장 실패가 아니라 저장 직후 전체 앱 데이터를 다시 읽는 동안 편집 상태가 오래된 착장 객체로 덮이는 화면 동기화 문제로 판단했다.

## Work Completed - 착장 조정 저장 후 되돌아감 수정

- 착장 Item 위치·크기 저장이 성공한 뒤 전체 `repository.load()`를 다시 실행하지 않고, 저장한 배치값만 현재 `AppData.outfits` 상태에 반영하도록 `DataProvider`를 변경했다.
- 위치 저장 중에는 전역 `loading` 상태가 켜지지 않도록 해 착장 상세 화면 전체가 잠시 사라졌다 다시 나타나는 현상을 제거했다.
- 동시에 실행된 데이터 새로고침 중 오래된 요청이 늦게 끝나 최신 화면 상태를 덮지 못하도록 refresh sequence 방어를 추가했다.
- `OutfitPositionEditor`가 내용은 같은 새 Outfit 객체를 받았다는 이유만으로 저장 전 조정값을 초기화하지 않도록, 실제 저장 배치값의 revision이 바뀔 때만 편집 상태를 동기화하게 했다.
- DataProvider가 저장 뒤 전체 데이터를 재조회하지 않고 현재 위치·크기를 갱신하는지, 동등한 Outfit 재렌더링에도 미저장 조정값이 유지되는지 회귀 테스트를 추가했다.
- 원격 Batch 0을 읽기 전용으로 확인해 양말의 `scale=1.5000`과 스커트·스카프·가방의 기존 위치값이 남아 있음을 확인했다.

## Decisions - 착장 조정 저장 후 되돌아감 수정

- Supabase 저장 함수는 필터 뒤 `.select()`로 갱신 행을 반환받고 행이 없으면 오류를 내므로 기존 성공 판정은 유지한다. 저장 성공 뒤 같은 데이터를 전부 재조회하는 절차만 제거한다.
- 위치·크기 조정은 다른 Item, 착용 기록, signed URL에 영향을 주지 않는 국소 변경이므로 서버 성공 뒤 현재 상태를 국소 갱신하는 방식이 더 안전하고 화면 전환도 적다.
- 외부 새로고침이나 signed URL 갱신은 계속 허용하되, 먼저 시작한 오래된 요청이 나중 요청의 결과를 덮어쓰지 못하게 한다.

## Current Status - 착장 조정 저장 후 되돌아감 수정

Supabase API 로그에서 실제 저장 요청 성공과 DB 지속 상태를 확인했다. 새 로컬 화면에서도 DB에 저장된 양말 크기 150%가 다시 표시된다. 전체 Vitest 19개 파일·79개 테스트와 production build가 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 이번 수정은 로컬 working tree에만 있으며 커밋·푸시·배포하지 않았다.

## Next Steps - 착장 조정 저장 후 되돌아감 수정

- J가 로컬 화면에서 위치 또는 크기를 한 번 더 저장해 로딩 전환과 되돌아감이 사라졌는지 확인한다.
- 확인 후 요청이 있으면 현재 누적 변경 범위를 분리해 커밋·푸시·배포한다.

## Work Summary - 착용 근거 조건 문구 단순화

Outfit 상세의 착용 근거에서 비와 걷기 조건이 `해당 없음/미지정`으로 함께 표시돼 한 기록에 두 상태가 동시에 있는 것처럼 읽히는 문제를 정리했다. J의 실제 사용 기준에 맞춰 화면에서는 `해당`과 `해당 없음`만 사용한다.

## Work Completed - 착용 근거 조건 문구 단순화

- 비 조건이 `yes`이면 `비 해당`, 그 외에는 `비 해당 없음`으로 표시하도록 변경했다.
- 오래 걷기 조건이 `yes`이면 `걷기 해당`, 그 외에는 `걷기 해당 없음`으로 표시하도록 변경했다.
- 기존 `unknown` 원본 데이터는 변경하지 않고 표시 문구에서만 `미지정`을 제거했다.
- Outfit 상세 렌더링 테스트에 해당·해당 없음 조합과 `미지정` 비표시 검증을 추가했다.

## Decisions - 착용 근거 조건 문구 단순화

- 과거 import에서 생긴 `unknown` 값을 일괄 수정하면 원본 기록 의미가 달라질 수 있으므로 DB 값은 보존한다.
- 착용 근거 화면은 J가 실제로 판단하는 두 상태만 보여 주고 내부 데이터의 세부 상태는 노출하지 않는다.

## Current Status - 착용 근거 조건 문구 단순화

착용 근거에는 이제 `비 해당`, `비 해당 없음`, `걷기 해당`, `걷기 해당 없음` 중 해당하는 문구만 표시되며 `/미지정`은 나타나지 않는다. 전체 Vitest 19개 파일·79개 테스트와 production build가 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 변경은 로컬 working tree에만 있으며 커밋·푸시·배포하지 않았다.

## Next Steps - 착용 근거 조건 문구 단순화

- J의 확인 후 필요하면 Wear Log 입력 화면에서도 `미지정` 선택지를 제거하는 별도 정리를 진행한다.

## Work Summary - Phase 1B Batch 3 cutout 30개 업로드

J가 Pilot 잔여 Item 가운데 `달뮤트 페이크숄 블랙`을 제외한 30개 투명 PNG를 준비했다. 파일과 실제 Supabase Item을 번호 기반 local-only manifest로 연결하고, 기존 이미지 준비·업로드 도구를 이용해 cutout 전용으로 최적화한 뒤 private Storage에 반영했다.

## Work Completed - Phase 1B Batch 3 cutout 30개 업로드

- Desktop의 `Batch 3` 폴더에서 002~031번 파일 30개가 빠짐없이 존재하는지 확인했다.
- 30개 모두 실제 PNG이며 투명 alpha 영역을 갖고 있음을 검사했다. `.png.png` 이중 확장자는 실제 형식에 영향이 없어 원본 파일명을 그대로 manifest에 사용했다.
- Pilot Outfit과 현재 ready image를 Supabase에서 대조해 Batch 3 대상 UUID·정확한 Item명·카테고리를 확정했다.
- `assets/private/phase-1b/batch-3/` 아래에 input 복사본, manifest, prepared cutout, 업로드 report, visual review contact sheet를 만들었다. 전체 폴더는 기존 ignore 규칙에 따라 Git에서 제외된다.
- 원본별 alpha trim과 2.5% 안전 여백을 적용하고, 500KB 목표·700KB 상한의 WebP 인코딩을 실행했다.
- dry-run에서 30 Item·30 asset·경고 0개를 확인한 뒤 mworkroom private Storage에 cutout만 업로드하고 `closet_item_images` metadata를 ready로 upsert했다.
- 업로드 후 Pilot Item 50개 중 49개가 ready이고, Pilot Outfit 24개 중 23개가 완성됐음을 원격 DB에서 재검증했다.
- contact sheet로 30개 결과를 확인해 식별 불가, 심한 잘림, 잘못 연결된 Item이 없음을 확인했다.
- 로컬 앱에서 완성된 Pilot #3을 열어 새 cutout 5개가 실시간 합성과 위치·크기 조정 UI에 정상 표시되는지 확인했다.
- 사진이 빠진 Pilot #2는 `달뮤트 페이크숄 블랙`만 기존 색상 fallback으로 남고 위치 조정 UI가 열리지 않아, 불완전 이미지 세트의 안전한 fallback도 유지됨을 확인했다.

## Decisions - Phase 1B Batch 3 cutout 30개 업로드

- J가 구하지 못한 `달뮤트 페이크숄 블랙`은 임의 대체 이미지를 사용하지 않고 미완성으로 남긴다.
- 개인 원본 PNG는 원격에 올리지 않으며, 앱 표시용으로 최적화한 cutout WebP만 저장한다.
- 파일명은 사람이 읽는 이름보다 앞의 3자리 번호를 manifest 연결 기준으로 우선해 이중 확장자나 DB 원본명의 이중 공백에도 안전하게 대응한다.

## Current Status - Phase 1B Batch 3 cutout 30개 업로드

Batch 3의 준비 가능한 30개는 모두 private Storage와 metadata에 반영됐고 업로드 경고는 0개다. Pilot #2만 `달뮤트 페이크숄 블랙`이 없어 미완성이며 나머지 23개 Pilot은 실시간 합성에 필요한 모든 cutout을 갖췄다. 로컬 앱에서도 완성 Pilot과 미완성 Pilot의 합성·fallback 동작을 각각 확인했다. 개인 이미지·manifest·prepared 결과는 Git ignore 상태이고 커밋·푸시·배포는 수행하지 않았다.

## Next Steps - Phase 1B Batch 3 cutout 30개 업로드

- J가 실제 iPhone 화면에서 새로 완성된 Pilot들의 기본 비율과 간격을 확인하고, 반복되는 차이가 있으면 category slot 기본값을 조정한다.
- 추후 `달뮤트 페이크숄 블랙` 이미지를 구하면 001번 한 장만 같은 manifest 흐름으로 추가한다.

## Work Summary - Outfit 상세 정보 흐름 재구성

Outfit 상세에 착장명과 펼쳐진 이미지 조정기가 큰 공간을 차지해, 실제로 자주 확인하는 착장 이미지·구성 아이템·착용 기록까지 도달하는 흐름이 길었다. 착장에 별도 이름이 필요하지 않다는 J의 사용 원칙에 맞춰 제목을 숨기고, 조회 정보는 위로 모으고 이미지 수정 기능은 접힌 상태로 페이지 맨 아래에 배치했다.

## Work Completed - Outfit 상세 정보 흐름 재구성

- 상단의 Item 이름 조합형 Outfit 제목을 화면에서 숨기고 접근성 제목 `착장 상세`만 유지했다.
- 상단 identity card를 착장 이미지 2/3, 선호도·마지막 착용·착용 횟수 요약 1/3 비율로 변경했다.
- 상세 본문 순서를 상단 착장 요약, 구성 아이템, 착용 기록, 오늘 입기, 착장 이미지 수정으로 재배치했다.
- `착용 근거` 제목을 `착용 기록`으로 변경했다.
- 이미지 수정기를 기본적으로 닫힌 `착장 이미지 수정` 영역으로 만들고 필요할 때만 펼치도록 했다.
- 수정기 내부를 설명, 합성 미리보기, 현재 선택 Item 조절 버튼, Item 선택 목록 순서로 변경했다.
- 제목 비노출, `착용 기록` 문구, 미리보기·조절 버튼·Item 선택 목록의 순서를 검증하는 테스트를 보강했다.
- 실제 Pilot #3 로컬 화면에서 긴 제목 비노출, 섹션 순서, 수정기 기본 접힘과 펼친 뒤의 내부 순서를 확인했다.

## Decisions - Outfit 상세 정보 흐름 재구성

- Outfit은 UUID로 식별하고 화면 제목은 만들지 않는다. 이미지의 대체 텍스트에는 구성 Item 기반 설명을 유지해 접근성과 이미지 의미는 보존한다.
- 이미지 수정은 조회보다 사용 빈도가 낮은 관리 기능이므로 기본 정보 뒤에 두고 닫힌 상태를 기본값으로 사용한다.
- 수정기에서는 조정 결과를 보면서 바로 버튼을 누를 수 있어야 하므로 Item 선택 목록보다 조절 버튼을 미리보기 가까이에 둔다.

## Current Status - Outfit 상세 정보 흐름 재구성

Outfit 상세가 이름 없는 이미지 중심 화면으로 정리됐고, 기본 진입 시 긴 수정 UI가 렌더링 영역을 차지하지 않는다. 전체 Vitest 19개 파일·79개 테스트와 production build가 통과했다. build에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다. 변경은 로컬 working tree에 있으며 커밋·푸시·배포하지 않았다.

## Next Steps - Outfit 상세 정보 흐름 재구성

- J가 iPhone에서 상단 2:1 비율과 1/3 요약 영역의 글자 크기를 확인하고 필요하면 모바일 전용 여백만 미세 조정한다.
