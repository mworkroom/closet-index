# 2026-08-02 Closet Index Development Log

## Work Summary - Outfit 기본 배치 보정

착장 이미지에서 양말, 코트, 옆에 분리한 티셔츠의 위치·크기 기본값을 J가 확정한 값으로 조정했다. 조끼는 매번 수동 판단값이 달라지는 문제를 줄이기 위해 실제 저장된 조정값 분포를 읽기 전용으로 확인하고 대표 중앙값을 적용했다.

## Work Completed - Outfit 기본 배치 보정

- 모든 양말의 기본 상하 위치를 28px로 설정하고 기존 150% 배율을 유지했다.
- `Outer-Coat`의 기본 배율을 150%로 설정했다.
- 옆에 분리한 `Top-T-shirts` 계열의 기본값을 좌우 -100px, 상하 64px, 배율 115%로 설정했다.
- `Top-T-shirts-innerwear`도 옆에 분리 모드에서는 같은 분리 티셔츠 규칙을 사용하도록 규칙 선택 경로를 보완했다.
- `Outer-Vest`와 `Outer-Vest-made`의 기본 배율을 75%로 설정했다.
- category별 기본값과 분리 모드 회귀 테스트를 추가했다.

## Decisions - Outfit 기본 배치 보정

- 조끼 대표값은 production의 `Outer-Vest` 20개와 연결된 착장 relation 73개를 읽기 전용으로 확인해 결정했다. 조정 전 기록을 뜻하는 배율 0인 51개는 제외하고, 실제 조정값 22개(60~90%)의 중앙값인 75%를 사용했다.
- 세부 category 규칙을 일반 `Outer` 규칙보다 앞에 두어 코트와 조끼만 독립 기본 배율을 갖게 하고 다른 아우터에는 기존 90%를 유지했다.
- 저장된 착장별 위치·배율은 계속 category 기본값보다 우선한다.

## Current Status - Outfit 기본 배치 보정

설정과 브라우저 합성 규칙, 관련 회귀 테스트를 수정했다. production 데이터 자체는 변경하지 않았으며 새 기본값은 저장된 개별 조정값이 없는 경우에 적용된다.

## Next Steps - Outfit 기본 배치 보정

- 실제 착장 화면에서 코트 150%와 조끼 75%가 서로 다른 이미지 비율에서도 자연스러운지 확인한다.
- 필요하면 조끼의 위치값은 배율과 분리해 별도 분포를 기준으로 조정한다.

## Work Summary - 날씨 한 번 클릭 추천 흐름

HOME에서 `날씨 불러오기` 뒤 `이 날씨로 추천 보기`를 다시 눌러야 했던 두 단계 흐름을 한 번의 명시적 동작으로 줄였다. 날씨 조회 자체가 추천 의도를 충분히 나타내므로 별도 확인 단계를 없애고, 조회 성공 즉시 예보값 입력과 추천 결과 표시까지 완료하도록 변경했다.

## Work Completed - 날씨 한 번 클릭 추천 흐름

- 첫 버튼을 `날씨로 추천 보기`로 바꾸고 기본 버튼으로 강조했다.
- 날씨 조회 성공 응답을 기존 예보 적용 함수에 직접 전달해 출발·귀가 온도, 비 조건, 추천 결과, weather provenance를 한 번에 갱신하도록 했다.
- 같은 요청의 세션 캐시가 있으면 API를 다시 호출하지 않고 저장된 예보를 바로 추천 조건에 적용하도록 유지했다.
- 예보 결과 카드의 두 번째 `이 날씨로 추천 보기` 버튼을 제거했다.
- HOME과 HOME → 착장 상세 → Wear Log 통합 테스트를 한 번 클릭 흐름에 맞게 갱신했다.
- Phase 2 계획서의 사용자 흐름을 현재 구현과 일치시켰다.

## Decisions - 날씨 한 번 클릭 추천 흐름

- 별도 확인 버튼 없이도 사용자가 `날씨로 추천 보기`를 누른 행위 자체를 날씨 조회와 추천 적용에 대한 명시적 동작으로 본다.
- React 상태 갱신 직후 저장된 예보 상태를 다시 읽지 않고, API 응답 객체를 적용 함수에 직접 전달해 오래된 상태를 참조할 가능성을 없앴다.
- API 실패 시 수동 입력을 유지하고, 적용 후 직접 수정·예보값 복원·Wear Log 출처 기록은 기존 동작을 그대로 보존한다.

## Current Status - 날씨 한 번 클릭 추천 흐름

관련 화면 테스트 5개, 전체 Vitest 45개 파일·199개 테스트, TypeScript 검사, production build가 통과했다. 인앱 브라우저의 로컬 데모 모드에서 버튼 한 번으로 출발 24°C·귀가 20°C 입력과 추천 결과 표시를 확인했고, 기본 화면과 375px 모바일 화면 모두 콘솔 오류와 가로 넘침이 없었다. production 데이터, 원격 함수, 배포 상태는 변경하지 않았다. 빌드에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.

## Next Steps - 날씨 한 번 클릭 추천 흐름

- 실제 공개 앱 반영이 필요하면 변경 파일을 다시 분류한 뒤 별도 승인에 따라 commit·push·배포하고 인증된 실제 기상청 응답으로 최종 확인한다.

## Work Summary - Item 카테고리 드롭다운

신규 Item 등록과 기존 Item 수정에서 카테고리가 자유 입력창처럼 보여 기존 상세 분류를 외워 입력해야 했던 문제를 수정했다. 실제 데이터에 존재하는 카테고리를 명확하게 고를 수 있도록 `datalist` 입력을 표준 드롭다운으로 교체했다.

## Work Completed - Item 카테고리 드롭다운

- 등록·수정 공용 Item 편집 화면의 카테고리 필드를 필수 `select`로 변경했다.
- 새 Item은 `카테고리 선택` 안내 상태에서 시작하고, 현재 Closet 데이터에 존재하는 상세 카테고리를 알파벳순으로 표시한다.
- 기존 Item 수정 화면에서는 저장된 카테고리가 자동 선택되도록 유지했다.
- 등록 중복 경고 테스트를 실제 드롭다운 선택 방식으로 변경하고, 수정 시 선택한 새 카테고리가 저장 입력에 전달되는 검증을 추가했다.
- Phase 3 계획서의 현재 구현 상태를 드롭다운 방식과 일치시켰다.

## Decisions - Item 카테고리 드롭다운

- 카테고리는 합성 규칙·검색·통계에 쓰이는 구조화 값이므로 일반 Item 등록에서는 기존 상세 카테고리만 선택하게 한다.
- 새 카테고리가 필요한 경우 자유 입력으로 조용히 생성하지 않고, 합성·그룹·통계 규칙을 함께 검토하는 별도 변경으로 다룬다.
- 상위 7개 그룹으로 값을 바꾸지 않고 기존 상세 category 문자열을 그대로 저장해 현재 데이터와 이미지 합성 규칙을 보존한다.

## Current Status - Item 카테고리 드롭다운

Item 편집 테스트 3개, 전체 Vitest 45개 파일·199개 테스트, TypeScript 검사, production build가 통과했다. 인앱 브라우저 데모 모드에서 신규 등록의 `Top-Knitwear` 선택과 미리보기 갱신, 기존 Item 수정의 `Outer-Cardigan` 자동 선택을 확인했다. 375px 모바일 화면에는 가로 넘침이 없고 콘솔 오류도 없다. production 데이터와 공개 앱은 변경하지 않았으며, 빌드에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.

## Next Steps - Item 카테고리 드롭다운

- 새 상세 카테고리가 필요해지면 단순 자유 입력을 다시 열지 않고 category group·composition 규칙과 함께 추가한다.
- 공개 앱 반영은 변경 파일을 다시 분류한 뒤 별도 승인에 따라 commit·push·배포한다.

## Work Summary - Item 수정 기능 통합

Item 상세 화면에 흩어져 있던 이미지 관리, 조건 적합성, Retired 전환을 `정보 수정` 흐름으로 통합했다. 상세 화면은 구매일·착용 횟수·마지막 착용과 포함된 Outfit처럼 현재 앱에서 이미 제공하는 조회 정보에 집중하도록 정리했다.

## Work Completed - Item 수정 기능 통합

- Item 상세 화면에서 이미지 편집기, 조건 적합성 편집 패널, 상태 전환 패널을 제거했다.
- 기존 Item의 정보 수정 화면에 이미지 추가·교체 기능을 배치했다.
- 이미 정보 수정 폼에 있던 착용 조건을 유일한 조건 수정 위치로 유지했다.
- 정보 수정 화면 하단에 `상태 관리` 영역을 추가하고 Retired 전환·사용 중 복원 기능과 확인 절차를 옮겼다.
- 신규 Item 등록은 기존처럼 정보를 먼저 저장한 뒤 이미지를 추가하도록 유지했다.
- 수정 화면의 통합 기능과 상세 화면의 조회 전용 구성을 검증하는 회귀 테스트를 추가했다.

## Decisions - Item 수정 기능 통합

- 이번 작업은 현재 앱에 이미 존재하는 기능의 위치만 재구성하고 새 통계, 수량, 세탁 알림 데이터는 추가하지 않았다.
- 기본 정보와 착용 조건은 `변경 저장`으로 함께 저장하되, 이미지 업로드와 Retired 전환은 각 작업의 실패·확인 상태가 다르므로 같은 화면 안에서 독립 동작을 유지했다.
- Retired 전환은 기록을 보존하는 중요한 상태 변경이므로 정보 수정 화면 맨 아래에서 기존 확인 절차를 계속 사용한다.

## Current Status - Item 수정 기능 통합

관련 화면 테스트 4개, 전체 Vitest 45개 파일·200개 테스트, TypeScript 검사, production build가 통과했다. 인앱 브라우저 데모 모드에서 상세 화면에 조회 정보와 포함된 Outfit만 남은 것, `정보 수정` 안에 이미지 관리·착용 조건·상태 관리가 함께 표시되는 것, Retired 전환 확인 절차가 열리는 것을 확인했다. 콘솔 오류는 없었으며 production 데이터와 공개 앱은 변경하지 않았다. 빌드에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.

## Next Steps - Item 수정 기능 통합

- 실제 모바일 화면에서는 정보 수정 섹션 순서와 하단 저장 영역의 사용성을 추가로 확인한다.
- 공개 앱 반영은 변경 파일을 다시 분류한 뒤 별도 승인에 따라 commit·push·배포한다.

## Work Summary - Item·Outfit 삭제와 보관

Item과 Outfit을 앱 안에서 정리할 수 있도록 두 화면의 최하단에 `삭제`와 `보관`을 같은 한 줄로 배치했다. 보관은 기존 기록을 유지하는 기본 선택으로 두고, 삭제는 연결 기록이 없는 경우에만 허용해 과거 착용 기록과 구성 관계가 끊어지지 않도록 했다.

## Work Completed - Item·Outfit 삭제와 보관

- Item 정보 수정 화면의 마지막에 `삭제`·`보관` 버튼을 배치하고, 보관된 Item에는 `복원`을 표시했다.
- 착장 상세 화면의 마지막으로 기존 보관 기능을 옮기고 `삭제`·`보관` 또는 `복원`을 한 줄에 배치했다.
- Outfit에 포함되거나 Replacement Line에 연결된 Item, Wear Log가 있는 Outfit은 삭제할 수 없도록 UI와 저장소 양쪽에서 차단했다.
- 삭제 전 복구 불가 안내와 명시적 확인 절차를 추가했다.
- Supabase에서는 service role 전용 database function이 membership과 참조를 다시 검사해 metadata를 삭제하고, Edge Function이 반환받은 경로의 Storage 객체를 Storage API로 정리하도록 연결했다.
- Demo repository, Supabase repository, Edge Function 계약, Item·Outfit 화면의 차단·삭제·보관·복원 회귀 테스트를 추가했다.
- Phase 3 계획서의 삭제·복구 정책을 현재 결정과 일치시켰다.

## Decisions - Item·Outfit 삭제와 보관

- 보관은 Item의 기존 `retired`, Outfit의 기존 `archived_at`을 그대로 사용해 Outfit 관계·평가·Wear Log를 보존한다.
- Item 삭제는 Outfit 관계와 Replacement Line 관계가 모두 없을 때만, Outfit 삭제는 Wear Log가 없을 때만 허용한다. 관련 기록을 함께 지우는 연쇄 삭제는 제공하지 않는다.
- Storage metadata를 SQL로 직접 지우는 것만으로는 객체가 제거되지 않으므로, database function은 삭제 대상 경로를 반환하고 실제 객체 삭제는 Storage API를 사용하는 기존 Edge Function에서 수행한다.
- 브라우저에 일반 table DELETE 권한을 추가하지 않고, 인증된 Edge Function과 service role 전용 RPC를 통해서만 영구 삭제한다.

## Current Status - Item·Outfit 삭제와 보관

전체 Vitest 45개 파일·208개 테스트, TypeScript 검사, production build가 통과했다. 인앱 브라우저 데모 모드에서 Item과 착장 관리 영역이 각각 페이지의 마지막 요소이며 두 버튼이 동일한 y축과 너비로 한 줄에 배치된 것을 확인했다. 연결된 Item과 Wear Log가 있는 착장의 삭제 비활성화 안내, 보관 확인 절차, 가로 overflow 0, 콘솔 오류 부재도 확인했다. production 데이터, 원격 migration·Edge Function, 공개 앱은 변경하지 않았으며 빌드에는 기존 500kB 초과 JavaScript chunk 경고만 남아 있다.

## Next Steps - Item·Outfit 삭제와 보관

- 실제 서비스에 반영하려면 migration과 두 Edge Function을 적용·배포한 뒤 인증된 계정으로 미연결 Item 1개와 미착용 Outfit 1개의 삭제 및 Storage 객체 제거를 확인한다.
- commit·push·배포는 변경 파일을 다시 분류하고 J님의 별도 요청을 받은 뒤 진행한다.

## Work Summary - Item·Outfit 삭제 원격 적용

J님의 다음 단계 진행 요청에 따라 삭제 안전장치를 실제 Supabase 프로젝트에 적용하고, 공개 앱 배포를 위한 Git 반영 단계로 전환했다.

## Work Completed - Item·Outfit 삭제 원격 적용

- `add_safe_item_outfit_deletion` migration을 원격 프로젝트에 적용했다.
- `closet-item-image`와 `closet-outfit-preview` Edge Function을 각각 version 2로 배포했다.
- 두 Edge Function이 `ACTIVE` 상태이며 JWT 검증이 활성화된 것을 확인했다.
- 실제 배포 endpoint에 인증 없이 POST 요청을 보내 두 함수가 모두 `401`로 차단하는 것을 확인했다.
- 원격 DB에서 두 삭제 함수가 `SECURITY DEFINER`, 빈 `search_path`, `postgres`와 `service_role`만 실행 가능한 권한으로 저장된 것을 확인했다.
- migration 적용 후 Supabase security·performance advisor를 다시 실행했다.

## Decisions - Item·Outfit 삭제 원격 적용

- 운영 데이터에 존재하는 Item이나 Outfit을 배포 검증 목적으로 임의 삭제하지 않는다. 파괴적 검증은 J님이 명확히 정한 테스트 레코드가 있을 때 수행한다.
- 이번에 추가한 삭제 함수는 브라우저 RPC로 직접 호출할 수 없게 유지하고, 인증된 Edge Function만 service role로 호출하게 한다.
- advisor의 기존 공유 프로젝트 경고는 이번 기능 범위를 벗어나므로 함께 수정하지 않되, 새 삭제 함수가 anonymous/authenticated 실행 경고에 포함되지 않는지는 확인한다.

## Current Status - Item·Outfit 삭제 원격 적용

원격 migration `20260802015500 add_safe_item_outfit_deletion`이 적용되었고, 두 Edge Function version 2가 `ACTIVE`·JWT 필수 상태다. 인증 없는 실제 endpoint 요청도 모두 `401`로 차단됐다. 새 삭제 함수의 권한과 `search_path` 설정도 원격 SQL로 확인했다. 기존 advisor 경고는 남아 있으나 이번 두 삭제 함수에서 새로 발생한 security·performance 경고는 없다. 전체 Vitest 45개 파일·208개 테스트, TypeScript 검사, GitHub Actions와 같은 base 환경의 production build 및 Pages artifact 검증도 통과했다.

## Next Steps - Item·Outfit 삭제 원격 적용

- 변경 파일만 선별해 commit·push하고 GitHub Pages workflow와 공개 artifact를 확인한다.
- 실제 영구 삭제의 성공 경로와 Storage 객체 제거는 별도의 폐기 가능한 테스트 Item·Outfit이 준비되었을 때 검증한다.

## Work Completed - Item·Outfit 삭제 공개 배포

- 기능 변경 21개 파일을 commit `36fa5df`로 `origin/main`에 push했다.
- GitHub Pages Actions run `30728117716`의 build·artifact 검증·deploy가 모두 성공한 것을 확인했다.
- 공개 HTML이 새 JavaScript asset `index-bUURVkGN.js`를 제공하고 두 Edge Function 경로를 포함하는 것을 확인했다.
- 로그인된 공개 앱에서 `Scarf No 1` 정보 수정 화면의 카테고리 드롭다운과 최하단 `삭제`·`보관` 버튼을 확인했다.
- 착용 기록 5개가 있는 실제 Outfit에서 `삭제`가 비활성화되고 차단 사유가 표시되며, `보관`은 같은 행에 활성 상태로 표시되는 것을 확인했다.
- 공개 앱 브라우저 콘솔 오류가 없고, 검증 과정에서 실제 Item·Outfit 데이터는 변경하지 않았다.

## Current Status - Item·Outfit 삭제 공개 배포

Supabase migration·Edge Function과 GitHub Pages 프론트엔드가 모두 공개 환경에 반영됐다. 보관과 참조 기록이 있는 대상의 삭제 차단은 실제 로그인 화면에서도 확인했다. 성공적인 영구 삭제와 Storage 객체 정리는 폐기 가능한 검증 레코드가 없어 의도적으로 실행하지 않았다.

## Next Steps - Item·Outfit 삭제 공개 배포

- J님이 폐기 가능한 미연결 Item과 미착용 Outfit을 지정하거나 새 테스트 레코드 생성을 요청하면, 성공 삭제와 Storage 정리까지 실제 endpoint로 검증한다.

## Work Summary - Phase 4 전 구조 리팩토링

Phase 3 종료 뒤 Phase 4 기능이 기존 대형 화면과 저장소에 계속 누적되는 것을 막기 위해, 현재 동작과 공개 계약을 보존한 채 Supabase 저장소·HOME 추천 흐름·Statistics 계산의 책임 경계를 정리했다. 전면 재작성이나 상태관리 교체 없이 다음 기능이 추가될 가능성이 높은 지점만 분리했다.

## Work Completed - Phase 4 전 구조 리팩토링

- `SupabaseRepository`의 공개 메서드와 기존 import 경로를 유지하면서 snapshot, Item, Outfit, Weather, Wear Log 구현을 도메인별 내부 모듈로 분리했다.
- 공통 Supabase row 변환과 1,000행 pagination 로직을 shared 모듈로 이동하고 기존 export를 facade에서 다시 제공해 회귀 테스트 계약을 보존했다.
- HOME의 날짜별 local/session storage 복원, 날씨 요청·적용·복원, 수동 수정 provenance, 추천 입력 검증과 결과 계산을 `useHomeRecommendation` hook으로 옮겼다.
- HOME 화면에는 입력 컨트롤과 결과 렌더링을 남기고 날씨 피드백 초기화 같은 상태 동작은 hook API로 통일했다.
- Statistics의 현재 snapshot 선택과 순위·카테고리 계산을 전용 순수 계산 모듈로 옮기고, 페이지가 전용 data hook을 통해 결과를 받도록 변경했다.
- Statistics 계산 경계의 요약 수·행 수·세부 카테고리 계산을 확인하는 단위 테스트를 추가했다.

## Decisions - Phase 4 전 구조 리팩토링

- Supabase 쿼리·RPC·Storage·Edge Function 호출 형태와 RLS 또는 DB schema는 바꾸지 않고, facade가 도메인 구현에 위임하는 구조만 도입했다.
- HOME 상태는 전역 context나 새 라이브러리로 옮기지 않고 화면 전용 hook에 유지해 독립 상태 경계와 기존 저장 복원 동작을 함께 보존했다.
- Statistics는 현재 authenticated `AppData` snapshot을 사용하는 전용 selector·hook 경계로 시작한다. 이후 실제 성능 측정으로 lazy query나 DB 집계가 필요해지면 페이지를 다시 작성하지 않고 이 경계만 교체한다.
- 파일 크기만을 이유로 추천 알고리즘은 분리하지 않았으며, 순수 함수와 기존 테스트가 안정적인 영역은 이번 범위에서 유지했다.

## Current Status - Phase 4 전 구조 리팩토링

TypeScript 검사, 핵심 회귀 테스트 8개 파일·21개 테스트, 전체 Vitest 46개 파일·209개 테스트, production build가 통과했다. 처음 일반 로컬 build로 Pages 검사를 실행했을 때 CI 전용 `404.html`과 `/closet-index/` base가 없어 실패했으며, GitHub Actions와 같은 `GITHUB_ACTIONS=true` 환경에서 다시 build하고 fallback을 복사한 뒤 Pages artifact 계약까지 통과했다. `git diff --check`에도 오류가 없다. production 데이터, Supabase 원격 설정·함수·migration, 공개 앱은 변경하지 않았다. 빌드에는 기존 500kB 초과 JavaScript chunk 경고가 남아 있다.

## Next Steps - Phase 4 전 구조 리팩토링

- P4-0에서 실제 데이터 분포와 통계 기준을 확정한 뒤 Statistics 전용 계산 경계에 기간·관리 신호를 추가한다.
- 초기 로드나 기간 변경이 실제 모바일에서 느린 경우에만 Statistics data hook 뒤를 lazy query 또는 RLS를 통과하는 DB 집계로 교체한다.
- draft PR을 검토한 뒤 `main`에 merge하면 GitHub Pages workflow와 공개 artifact를 확인한다.

## Work Completed - Phase 4 전 구조 리팩토링 GitHub 게시 준비

- 변경 파일을 다시 분류해 개인 이미지, 비밀 파일, `dist` 같은 build 산출물이 포함되지 않은 것을 확인했다.
- `codex/refactor-phase4-boundaries` 브랜치에 commit `390ca31`로 리팩토링 변경을 저장하고 같은 이름의 원격 브랜치에 push했다.
- `main` 대상 draft PR #2 `Refactor Phase 4 boundaries`를 생성했다.
- 저장소 workflow를 확인해 Pages 배포는 `main` push에서만 실행되고, 현재 PR 브랜치에는 자동 check가 연결되지 않는 것을 확인했다.

## Current Status - Phase 4 전 구조 리팩토링 GitHub 게시 준비

로컬과 원격 PR 브랜치는 최신 head로 일치한다. 리팩토링 코드는 commit `390ca31`에 포함되어 있으며, draft PR은 아직 `main`에 merge되지 않았으므로 공개 앱은 기존 배포 상태다. 이 PR의 검증 근거는 앞서 통과한 전체 Vitest 46개 파일·209개 테스트, TypeScript 검사, production build, Pages artifact 검사다.

## Next Steps - Phase 4 전 구조 리팩토링 GitHub 게시 준비

- draft PR을 ready 상태로 전환하고 `main`에 merge한다.
- `Deploy GitHub Pages` workflow 완료 뒤 공개 HTML과 JavaScript asset이 merge commit을 제공하는지 확인한다.

## Work Summary - Phase 4 전 구조 리팩토링 병합 및 공개 검증

리팩토링 PR을 `main`에 병합하고 GitHub Pages 배포와 로그인된 공개 앱의 핵심 화면을 확인했다. 코드 구조만 바뀌고 기존 사용자 흐름과 실제 집계 결과가 유지되는지를 공개 환경에서 검증했다.

## Work Completed - Phase 4 전 구조 리팩토링 병합 및 공개 검증

- draft PR #2를 ready 상태로 전환한 뒤 squash merge했으며, `main`의 병합 commit은 `0f57df0`이다.
- GitHub Pages Actions run `30729317639`의 build, SPA fallback, Pages artifact 검사, upload, deploy 단계가 모두 성공한 것을 확인했다.
- 로그인된 공개 앱에서 HOME이 정상 렌더링되고 `MORE` 메뉴로 이동하는 것을 확인했다.
- `MORE`에서 `Statistics`로 이동해 착용 기록 784건, Outfit 509개, Item 451개와 카테고리별 보유 집계가 렌더링되는 것을 확인했다.
- HOME, MORE, Statistics 검증 중 브라우저 콘솔 경고·오류가 없었고 실제 데이터를 생성·수정·삭제하지 않았다.
- 로컬 `main`을 `origin/main`으로 fast-forward했으며, 작업 전부터 있던 미추적 기획 문서 2개는 병합·커밋 대상에서 제외하고 그대로 보존했다.

## Decisions - Phase 4 전 구조 리팩토링 병합 및 공개 검증

- 여러 준비 commit은 PR 제목을 기준으로 한 squash merge 하나로 정리해 `main` 이력을 간결하게 유지했다.
- 공개 검증은 데이터 변경이 없는 화면 이동과 집계 렌더링으로 제한했다. 이번 리팩토링의 목적이 동작 변경이 아닌 책임 경계 분리이므로 쓰기 작업은 검증 범위에 넣지 않았다.
- PR workflow가 별도로 실행되지 않는 저장소 구조이므로, 병합 전 로컬 전체 검사와 병합 후 `main` Pages workflow 및 공개 화면 확인을 결합해 배포 기준으로 삼았다.

## Current Status - Phase 4 전 구조 리팩토링 병합 및 공개 검증

PR #2의 리팩토링 코드는 `main`에 병합되어 GitHub Pages 공개 환경에 반영됐다. 전체 자동 검사와 Pages artifact 검사는 통과했으며, 공개 HOME·MORE·Statistics 화면과 집계 데이터도 정상이다. 기능상 새 오류는 확인되지 않았다.

## Next Steps - Phase 4 전 구조 리팩토링 병합 및 공개 검증

- GitHub Actions가 Node.js 20 지원 종료 예정 경고를 표시하므로, 별도 유지보수 작업에서 `checkout`, `setup-node`, Pages 관련 action의 최신 major 지원 상태를 확인한다.
- 기존 500kB 초과 JavaScript chunk 경고는 실제 초기 로드 성능 측정 결과가 나쁠 때 Statistics lazy loading 또는 route 단위 분할과 함께 다룬다.

## Work Summary - Phase 3.5 Calendar 및 전면 내비게이션

기존 Calendar가 날짜별 기록 목록에 가까워 이번 달 착장을 한눈에 보기 어려웠던 문제를 해결했다. Phase 4를 시작하기 전의 독립 작업을 Phase 3.5로 정의하고, Calendar를 전면 탭으로 옮긴 뒤 월요일 시작의 전체 높이 월간 착장 달력으로 구현했다.

## Work Completed - Phase 3.5 Calendar 및 전면 내비게이션

- 하단 탭을 `HOME → CALENDAR → CLOSET → LOOKBOOK → MORE`로 바꾸고 Favorite를 More의 첫 메뉴로 이동했다.
- Calendar를 최소 5주·최대 6주의 월간 격자로 변경하고, 월 제목·요일·월 이동·상태·선택 시트를 영어 UI로 통일했다.
- 하루 한 기록은 Outfit 상세로 바로 연결하고, 두 기록 이상은 첫 착장과 `+N`을 표시한 뒤 하단 선택 시트에서 고르게 했다.
- 앞뒤 달 날짜는 흐린 숫자만 표시하고, 오늘과 URL로 지정한 날짜를 별도로 구분했다.
- Calendar의 수정·삭제 동작을 제거하고 Outfit 상세 착용 이력에 수정·삭제를 배치했다. 삭제는 해당 Wear Log 안에서 다시 확인한 뒤 기존 저장소 계약으로 처리한다.
- 월요일 시작, 윤년·월 경계, 5주·6주 계산, 내비게이션 순서, 복수 착장 선택, Wear Log 삭제 회귀 테스트를 추가했다.
- Phase 3.5 기준 문서, Roadmap, Phase 1 역사 명세 대체 안내, `design-qa.md`를 갱신했다.

## Decisions - Phase 3.5 Calendar 및 전면 내비게이션

- 아래에 선택 착장을 반복 표시하지 않고 달력 자체가 하단 메뉴 위의 높이를 사용하게 했다. 5주 달은 더 큰 셀, 6주 달은 조금 작은 셀로 같은 화면을 채운다.
- 하루 복수 착장은 드문 예외이므로 달력 밀도를 늘리지 않고 `+N` 선택 시트로만 처리했다.
- Calendar의 조작 UI와 접근성 이름은 영어로 고정하되, 착장명·장소명처럼 저장된 사용자 데이터는 번역하지 않는다.
- 모바일 Safari 호환성을 위해 주 수를 CSS 계산식에 맡기지 않고 5주·6주 클래스로 명시했다.
- 새 기록·아이템 단독 기록 기능과 DB schema·Supabase 변경은 이번 범위에 포함하지 않았다.

## Current Status - Phase 3.5 Calendar 및 전면 내비게이션

TypeScript 검사, 전체 Vitest 49개 파일·220개 테스트, production build, GitHub Actions와 같은 base의 Pages artifact 검사가 통과했다. 390×844 브라우저에서 April 2026의 5주 달력과 March 2026의 6주 달력이 스크롤 없이 하단 메뉴까지 들어가는 것을 확인했고, 월 이동, 단일 착장 직접 연결, 복수 착장 선택 시트, console warning·error 부재도 확인했다. 기존 500kB 초과 JavaScript chunk 경고는 남아 있다. 공개 앱, Supabase 원격 환경, 실제 데이터는 변경하지 않았으며 commit·push·배포도 아직 수행하지 않았다.

## Next Steps - Phase 3.5 Calendar 및 전면 내비게이션

- J가 로컬 화면에서 착장 썸네일 밀도와 5주·6주 달력 높이를 확인한다.
- 실제 iPhone Safari/PWA와 공개 환경 검증은 commit·push·배포를 별도로 요청받은 뒤 진행한다.
- Phase 3.5 공개 검증이 끝난 뒤 Phase 4 P4-0 읽기 전용 기준선 확인을 시작한다.

## Work Summary - Phase 3.5 Calendar 날짜 칸 배경 보정

Calendar의 흰 날짜 칸 안에서 착장 합성 영역의 회색 배경이 별도 직사각형처럼 보이던 문제를 보정했다. J가 지정한 `#f0f0f0`을 기존 디자인 토큰으로 적용하고, Lookbook보다 착장이 세로로 길어 보이는 원인도 실제 렌더링 크기로 확인했다.

## Work Completed - Phase 3.5 Calendar 날짜 칸 배경 보정

- 일반 날짜 칸 배경을 `var(--surface)`에서 값이 `#f0f0f0`인 `var(--surface-soft)`로 변경했다.
- 404×894 브라우저에서 날짜 칸과 착장 합성 배경이 모두 `rgb(240, 240, 240)`으로 계산되는 것을 확인했다.
- Calendar와 Lookbook의 합성 영역을 측정해 각각 약 0.52:1과 3:4 비율로 렌더링되는 차이를 확인했다.
- 배경 변경 뒤 `April → May → April` 월 이동과 브라우저 console 상태를 다시 확인했다.

## Decisions - Phase 3.5 Calendar 날짜 칸 배경 보정

- 하드코딩 색을 새로 추가하지 않고 이미 `#f0f0f0`을 소유하는 `--surface-soft` 토큰을 사용해 착장 배경과 날짜 칸이 계속 같은 값을 공유하게 했다.
- 세로로 길어 보이는 현상은 Calendar의 `height: 100%`와 `aspect-ratio: auto` 조합이 Lookbook의 3:4 캔버스 비율을 덮기 때문에 발생한다. J의 이번 요청은 원인 확인이므로 비율 코드는 함께 변경하지 않았다.

## Current Status - Phase 3.5 Calendar 날짜 칸 배경 보정

날짜 칸과 착장 합성 배경의 색상 불일치는 로컬 화면에서 해소됐다. Calendar의 영문 UI, 주 시작 순서, 월 이동과 하단 메뉴에는 회귀가 없고 console warning·error도 없다. TypeScript 검사, Calendar·visual 관련 Vitest 2개 파일·13개 테스트, production build가 통과했으며 기존 500kB 초과 chunk 경고만 남아 있다. Calendar 착장 비율은 기존 동작을 유지한다.

## Next Steps - Phase 3.5 Calendar 날짜 칸 배경 보정

- J가 원하면 Calendar 착장 wrapper도 Lookbook과 같은 3:4 비율로 고정해 세로 늘어짐을 제거한다.

## Work Summary - Phase 3.5 Calendar 착장 3:4 비율 보정

J의 후속 요청에 따라 Calendar 착장 합성 영역을 Lookbook과 같은 3:4 비율로 고정했다. 달력이 하단 메뉴까지 남는 높이를 모두 채우던 방식도 제거해, 5주 달력 아래에 공백이 생기더라도 착장 비율이 우선되게 했다.

## Work Completed - Phase 3.5 Calendar 착장 3:4 비율 보정

- Calendar grid의 flex 확장을 제거하고 5주·6주 날짜 행을 모두 92px로 고정했다.
- 착장 wrapper를 최대 48px 너비와 3:4 비율로 변경하고, 셀 안의 남는 이미지 영역에서는 가운데 정렬되게 유지했다.
- 404×894에서 수정 전 약 0.52:1이던 wrapper가 47.09×61.20px의 3:4로 계산되는 것을 확인했다.
- 390×844에서 5주 달력 아래 131.8px, 6주 달력 아래 39.8px의 공간이 남고 하단 메뉴와 겹치거나 추가 페이지 스크롤이 생기지 않는 것을 확인했다.
- April과 March 사이의 월 이동, 실제 cutout 이미지 로딩, console warning·error 부재를 검증했다.

## Decisions - Phase 3.5 Calendar 착장 3:4 비율 보정

- 달력 높이를 화면에 맞춰 늘리는 것보다 Lookbook과 동일한 합성 좌표계 비율을 우선한다.
- 92px 행은 날짜 영역 27px과 3:4 착장 영역을 함께 수용하면서 390×844의 6주 달력도 하단 메뉴 위에 들어오는 높이로 정했다.
- 5주와 6주의 셀 높이를 다르게 하지 않아 월 이동 때 착장 크기가 갑자기 달라지지 않게 했다.

## Current Status - Phase 3.5 Calendar 착장 3:4 비율 보정

Calendar의 착장 wrapper는 Lookbook과 같은 3:4 비율로 표시되며 세로 늘어짐이 해소됐다. 날짜 칸 `#f0f0f0`, 영문 Calendar UI, 월요일 시작, 복수 착장 `+N`, 하단 메뉴는 그대로 유지된다. TypeScript 검사, Calendar·Lookbook visual 관련 Vitest 2개 파일·13개 테스트, production build가 통과했고 기존 500kB 초과 chunk 경고만 남아 있다. 공개 앱과 Supabase는 변경하지 않았다.

## Next Steps - Phase 3.5 Calendar 착장 3:4 비율 보정

- 실제 iPhone Safari/PWA에서 390px 안팎 화면의 착장 식별성과 5주·6주 하단 여백을 최종 확인한다.
