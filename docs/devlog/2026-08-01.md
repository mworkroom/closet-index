# 2026-08-01 Closet Index Development Log

## Work Summary

오늘 추천 근거가 전혀 없는 미착용 Outfit을 HOME에 계속 노출하는 대신, Closet과 Lookbook에서 필요할 때 찾아볼 수 있도록 짧은 `Unworn` 필터를 추가했다. HOME은 오늘 온도와 비교할 수 있는 직접 또는 부분 근거가 있는 착장만 보여주도록 역할을 좁혔다.

## Work Completed

- Closet 필터에 `Unworn` 체크박스를 추가하고, 해당 Item을 포함한 모든 Outfit의 Wear Log를 확인해 착용 기록이 0회인 Item만 표시하도록 했다.
- Closet의 `Unworn` 상태를 기존 카테고리·색상·Retired·정렬 필터와 함께 localStorage에 저장하고 필터 초기화에 포함했다.
- Lookbook 필터에 `Unworn` 체크박스를 추가하고, 해당 Outfit 자체의 Wear Log가 0회인 착장만 표시하도록 했다.
- 기존 `최근 90일 미착용`은 과거 착용 이력이 있는 Outfit도 포함하는 별도 필터로 유지했다.
- 추천 그룹에서 OK 온도 범위가 없는 미착용 Outfit 반환값을 제거하고 HOME의 `적정 온도 미확인 착장` 영역과 관련 더보기 상태를 삭제했다.
- Item·Outfit Unworn 판정, Closet 필터 상태 복원, 근거 없는 Outfit의 HOME 제외를 회귀 테스트로 고정했다.
- 데모 데이터의 활성 Item은 모두 착용 이력이 있어 Closet Unworn 결과가 빈 상태였으므로, 테스트에는 미착용 활성 Item fixture를 명시적으로 추가했다.

## Decisions

- 두 화면 모두 짧은 표기인 `Unworn`을 사용하되 판정 단위는 화면에 맞춘다. Closet은 Item 전체 이력, Lookbook은 정확한 Outfit 조합의 이력을 기준으로 한다.
- 보관·Error·Retired Outfit에 남아 있는 Wear Log도 실제 과거 착용 이력이므로 Item Unworn 판정에서 삭제하거나 무시하지 않는다.
- `Unworn`과 `최근 90일 미착용`은 의미가 다르다. 전자는 착용 0회이고 후자는 마지막 착용이 90일보다 오래됐거나 착용 기록이 없는 경우다.
- 온도 근거가 없는 Outfit은 추천 화면에서 추측 후보로 보여주지 않는다. 계절 범위와 Unworn 필터가 있는 Lookbook을 탐색 경로로 사용한다.

## Current Status

전체 Vitest 41개 파일·185개 테스트와 production build가 통과했다. 실데이터 Browser QA에서 Closet Unworn은 착용 0회 Item 51개, Lookbook Unworn은 착용 0회 Outfit 4개를 표시했고 Outfit `53e94048-c745-48c4-a07a-3498e300b099`가 Lookbook 결과에 포함됐다. HOME 25°C 조회에는 `적정 온도 미확인 착장` 제목과 해당 Outfit이 나타나지 않았고, 근거가 있는 시험 착장은 유지됐다. PC와 390×844 모바일에서 가로 overflow와 콘솔 오류가 없었다. production Supabase 데이터는 변경하지 않았고 코드는 아직 커밋·푸시·배포하지 않았다. build에는 기존 500kB 초과 chunk 경고만 남아 있다.

## Next Steps

- J의 요청이 있으면 변경 범위를 다시 확인한 뒤 커밋·푸시·배포하고 공개 앱에서 같은 세 흐름을 재검증한다.
- 실제 사용 중 `Unworn`과 `최근 90일 미착용`을 동시에 켰을 때 문맥이 혼란스러운지 관찰하고, 필요하면 착용 상태 단일 선택 필터로 통합한다.

## Work Summary - P3-6 Outfit Preview Cache

P3-5의 Outfit 생성·복제 흐름 다음 단계로, 브라우저에서 현재 착장 composition을 900×1200 WebP로 만들고 Supabase에 교체 가능한 cache로 저장하는 P3-6 로컬 구현을 진행했다. Preview 실패 때문에 저장된 Outfit을 잃거나, 이전 relation의 이미지를 현재 착장으로 잘못 보여 주지 않는 것을 우선 기준으로 삼았다.

## Work Completed - P3-6 Outfit Preview Cache

- composition v4의 Item ID·category·ready image ID와 크기·placement·composition version을 SHA-256 source fingerprint로 만드는 browser 합성 모듈을 추가했다.
- 900×1200 canvas에 현재 layer를 합성하고 700KB 이하 WebP로 단계적으로 최적화하는 preview 생성 경로를 구현했다.
- Supabase CLI로 P3-6 migration을 생성하고 version 발급, pending·ready·error 전환, Storage WebP 검증, cancel과 이전 객체 정리를 담당하는 service-role 전용 RPC를 작성했다.
- `closet-outfit-preview` 인증 Edge Function을 추가해 workspace·Outfit 접근을 먼저 확인하고 signed upload ticket을 발급한 뒤 finalize하도록 했다.
- relation update와 ready cutout 교체 시 관련 preview를 stale 처리하는 trigger를 추가했다. 앱 로드 시에도 source fingerprint가 현재 relation·image와 다르면 preview를 사용하지 않는다.
- 새 Outfit 저장 시 preview를 자동 시도하되 실패는 별도 안내만 남기고 Outfit 저장과 상세 이동은 유지하도록 했다.
- Outfit 상세에 Preview 상태·재생성 조작을, Lookbook에 Preview 없음·오래됨·오류·생성 중·준비됨 관리 필터를 추가했다.
- production을 변경하지 않는 `npm run outfit:preview:audit -- --workspace-id ...` 읽기 전용 점검 도구를 추가해 고아 metadata, 누락 ready object와 고아 Storage object를 분리한다.
- browser fingerprint, Edge handler, repository signed upload, 기존 ready fallback, audit 분류, Lookbook 상태 필터와 전체 회귀 테스트를 추가했다.
- 기존 Phase 3 GitHub Actions 격리 Supabase 워크플로가 모든 `codex/phase3-*` 검증 브랜치에서 P3-1·P3-3·P3-6 pgTAP을 함께 실행하도록 확장했다.
- production `mworkroom` 프로젝트에 `phase3_outfit_preview_cache` migration을 적용하고 source fingerprint, stale trigger 2개, 인증 회원용 SELECT policy, service-role 전용 begin·finalize·cancel RPC를 생성했다.
- `closet-outfit-preview` Edge Function version 1을 `verify_jwt=true`로 배포하고, 무인증 production POST가 401로 거절되는 것과 같은 요청이 Edge 로그에 남는 것을 확인했다.
- 배포 후 읽기 전용 audit로 Preview metadata 1개와 Storage object 1개가 일치하며 고아 metadata·누락 ready object·고아 object가 모두 0개임을 확인했다.
- 검증 브랜치를 main에 fast-forward 통합하고 GitHub Pages 실행 `30669399088`의 build·artifact 검증·deploy가 모두 통과하는 것을 확인했다.
- 실제 공개 HTML이 새 JS `index-BlzTTZra.js`와 CSS `index-80jPsnSb.css`를 가리키고 두 자산이 200을 반환하며, JS에 P3-6 Function slug와 source fingerprint 코드가 포함된 것을 확인했다.

## Decisions - P3-6 Outfit Preview Cache

- Preview는 relation이 아니라 언제든 재생성 가능한 cache다. 따라서 생성 실패를 Outfit 저장 실패로 승격하지 않는다.
- 같은 Storage 경로를 upsert하지 않고 Outfit별 증가 version 경로를 사용한다. 새 object 검증과 ready 전환이 끝난 뒤에만 이전 ready를 비활성화한다.
- Database trigger와 browser fingerprint를 함께 사용한다. trigger는 저장된 relation·cutout 변경을 즉시 표시하고, fingerprint는 앱 composition 규칙 변경까지 포함해 stale cache 노출을 막는다.
- pending 또는 error가 최신 상태여도 마지막 정상 ready preview는 fallback으로 유지한다. 관리 상태와 실제 표시 가능한 cache를 별도로 판단한다.
- authenticated 사용자는 자기 workspace의 preview metadata 상태를 읽을 수 있지만 직접 insert·finalize하지 못한다. 쓰기는 인증 Edge Function과 service-role RPC 경계로 제한한다.
- 기존 수동 Preview 1개에는 새 source fingerprint가 없으므로 억지로 값을 채우지 않는다. 공개 앱이 P3-6 코드로 갱신된 뒤 현재 composition에서 재생성해 정확한 fingerprint를 부여한다.

## Current Status - P3-6 Outfit Preview Cache

TypeScript 검사와 전체 Vitest 44개 파일·195개 테스트, production build, Pages artifact 검사, GitHub Actions 격리 Supabase 실행 [30667343590](https://github.com/mworkroom/closet-index/actions/runs/30667343590)의 pgTAP 3개 파일·70개 계약이 통과했다. production migration version은 `20260731220215`이고 Function은 version 1·`ACTIVE`·`verify_jwt=true`다. 적용 전후 Item 451개, Outfit 509개, relation 2,415개, Wear Log 784개, 이미지 metadata 505개, Preview metadata 1개, Storage object 438개가 유지됐고 Preview pending·error와 세 가지 orphan 지표는 모두 0이다. Advisor에는 P3-6 관련 새 차단 경고가 없다. P3-6 웹 코드는 main과 공개 Pages에 배포됐고 실제 공개 자산도 확인했다. Browser 연결은 코드와 무관한 runtime asset 경로 오류로 시작되지 않아 authenticated signed upload와 화면 상호작용 검증만 남아 있다.

## Next Steps - P3-6 Outfit Preview Cache

- 로그인된 공개 앱에서 실제 Outfit 한 건의 preview 생성·재생성·stale 전환을 확인한다.
- Browser runtime이 정상인 환경에서 Lookbook 상태 필터와 상세 Preview 관리 영역을 PC·모바일로 확인하고, 다시 audit해 세 가지 orphan 지표가 0인지 확인한다.

## Work Summary - HOME 추천 계절 범위와 점진적 더 보기

HOME 추천에서 처음 3개 아래에 전체 후보 수를 한 번에 펼치는 버튼이 표시되고, Lookbook의 현재 계절 Outfit 수보다 추천 후보가 훨씬 많은 문제를 수정했다. 추천 계산이 전역 계절 범위를 사용하지 않아 전체 Outfit을 대상으로 삼고 있었던 것이 원인이었다.

## Work Completed - HOME 추천 계절 범위와 점진적 더 보기

- HOME 추천 입력에 Closet·Lookbook과 동일한 `SeasonScope`를 적용해 현재 선택 계절에 맞는 Outfit만 추천 엔진으로 전달한다.
- 일반 추천과 새 착장 시험 후보를 처음 3개만 표시하고, 버튼을 누를 때마다 최대 3개씩 추가하도록 변경했다.
- 계절 선택이나 추천 조건을 다시 적용하면 표시 개수가 3개로 초기화되도록 했다.
- HOME을 직접 렌더링하는 기존 테스트에 `SeasonScopeProvider`를 연결했다.
- 여름 범위에서 겨울 Outfit이 제외되는 동작과 추천 카드가 3개에서 6개로 늘어나는 동작을 전용 회귀 테스트로 추가했다.

## Decisions - HOME 추천 계절 범위와 점진적 더 보기

- 계절 판정은 별도 추천 규칙을 만들지 않고 Lookbook이 이미 사용하는 `outfitMatchesSeasonScope`를 재사용한다. 따라서 신발·가방·액세서리 등 계절 판단 제외 규칙도 두 화면에서 동일하게 유지된다.
- `나머지 N개 더 보기`로 전체 목록을 즉시 렌더링하지 않고, 실제 다음 표시량인 `3개 더 보기` 또는 마지막 남은 수량을 버튼 문구로 사용한다.
- 최근 구매 추천도 같은 계절 범위 안에서 계산해 HOME의 모든 추천 섹션이 서로 다른 계절 기준을 갖지 않게 한다.

## Current Status - HOME 추천 계절 범위와 점진적 더 보기

관련 HOME·날씨 흐름 테스트의 fixture 조정 과정에서 텍스트 노드 단위 assertion 1건과 잘못된 Outfit fixture 필드 `name`을 발견했다. 접근 가능한 카드 링크 기준과 실제 타입의 `displayName`으로 바로잡은 뒤 전체 Vitest 45개 파일·197개 테스트와 production build가 통과했다. build에는 기존 500kB 초과 chunk 경고만 남아 있다. 변경은 아직 커밋·푸시·공개 배포하지 않았다.

## Next Steps - HOME 추천 계절 범위와 점진적 더 보기

- 전체 회귀 테스트와 production build를 통과시킨 뒤 공개 앱에서 실제 여름 계절 선택 시 후보 수와 3개씩 펼치기를 확인한다.
- 공개 배포 전 변경 파일과 기존 로컬 전용 `.gitignore` 수정은 분리해 검토한다.

## Work Summary - Phase 3 최종 범위 정리와 Calendar thumbnail fallback

J의 실제 사용 목적과 Supabase egress를 다시 검토해 900×1200 Outfit Preview를 모든 목록에 우선 적용하지 않고 선택적 cache로 보류했다. 대신 자주 보는 Calendar가 저장 Preview 유무에 의존하지 않고 현재 Item 누끼 composition을 표시하도록 Phase 3 마지막 UI 경로를 정리했다.

## Work Completed - Phase 3 최종 범위 정리와 Calendar thumbnail fallback

- Calendar의 `outfit.preview` 조건을 제거해 연결된 Outfit이 있으면 `OutfitVisual`을 항상 표시한다.
- 저장 Preview가 없는 demo Outfit의 Wear Log 카드에도 실시간 composition thumbnail이 나타나는 회귀 테스트를 추가했다.
- J가 공개 앱에서 만든 Outfit `34cf66af-29b2-800b-93a4-ed26f7b2dd2a` Preview를 production에서 읽기 전용으로 확인했다.
- 해당 Preview는 900×1200·28,572B WebP, composition version 1, 현재 source fingerprint가 있는 ready 상태이며 Function begin·finalize 로그는 모두 POST 200이었다.
- workspace Preview는 ready 2개, pending·error·stale 0개이고 누락 ready object와 고아 preview object도 0개임을 확인했다.
- Phase 3 계획서와 원격 Runbook에 Preview 선택적 cache 결정과 Calendar fallback을 기록했다.

## Decisions - Phase 3 최종 범위 정리와 Calendar thumbnail fallback

- J는 Outfit 공유·내보내기를 사용하지 않고 iPhone의 작은 thumbnail 품질이면 충분하므로 Preview 일괄 생성과 Lookbook·Calendar 우선 사용은 보류한다.
- 현재 ready Item 누끼 전체가 약 38MB이고 같은 Item이 여러 Outfit에서 재사용되므로, 509개 Outfit별 고해상도 이미지를 중복 저장하는 것보다 기존 누끼와 browser composition을 유지한다.
- Preview schema·Function·stale 판정은 안전한 선택적 cache 기반으로 보존한다. 이미 만든 Preview를 삭제하지 않고, 필요한 Outfit에서만 수동 생성할 수 있다.
- 화면 표시 가능 여부를 Preview metadata에 묶지 않는다. Calendar는 현재 누끼가 있으면 합성하고, 누끼가 부족하면 기존 `OutfitVisual` fallback을 사용한다.

## Current Status - Phase 3 최종 범위 정리와 Calendar thumbnail fallback

Calendar와 HOME 관련 집중 테스트 2개 파일·12개 테스트와 전체 Vitest 45개 파일·197개 테스트가 통과했다. GitHub Actions 환경의 production build와 `dist/404.html` 생성 뒤 Pages artifact 검사도 통과했고 금지 파일은 0개였다. build에는 기존 500kB 초과 chunk 경고만 남아 있다. production Preview 생성·Storage 정합성도 정상이다. commit `e4518c8`을 main에 푸시했고 GitHub Pages 실행 `30672277387`의 build·artifact upload·deploy가 모두 성공했다. 공개 HTML은 새 JS `index-Bfz6323z.js`와 CSS `index-80jPsnSb.css`를 가리키며 두 자산 모두 200이고 `/src/main.tsx`는 없다. Browser 연결은 코드와 무관한 local runtime asset 경로 오류로 시작되지 않아 별도 Playwright 우회 없이 자동 UI 테스트·artifact·공개 자산 검증을 사용했다. Phase 3 완료 정의를 모두 충족했다.

## Next Steps - Phase 3 최종 범위 정리와 Calendar thumbnail fallback

- Phase 3의 차단 작업은 없다. Preview 일괄 생성·목록 우선 사용·공유 이미지는 보류 결정으로 유지한다.
- 새 세션에서는 Phase 4 또는 별도 사용성 개선을 새 범위로 시작한다.

## Work Summary - Phase 4 Maintenance & Insights 계획

Phase 3 완료 뒤 Roadmap에 후보로만 남아 있던 유지관리·고급 통계 범위를 실제 사용 필요성에 맞춰 다시 나눴다. 세부 카테고리를 통계 태그로 활용하고 싶다는 요구를 첫 구현 축으로 삼고, 데이터 규칙이 확정되지 않은 세탁·알림까지 한 번에 만들지 않는 단계형 Phase 4 계획서를 작성했다.

## Work Completed - Phase 4 Maintenance & Insights 계획

- Phase 4의 현재 기준선, 포함·제외·조건부 범위, 화면 구조, 데이터·보안 계약, P4-0~P4-7 구현 순서, 테스트와 완료 정의를 문서화했다.
- Closet·Outfit 구성 UI의 상위 7개 그룹은 유지하고 원본 세부 카테고리는 Statistics에서 보유 종류와 착용 분석에 사용하는 역할을 명시했다.
- `Unworn`, 장기 미착용, 최근 반복 착용을 서로 다른 계산 상태로 정의하고 정확한 기간·횟수는 production 분포 확인 전 확정하지 않도록 했다.
- 기존 Replacement Line은 명시적 순서가 없으므로 먼저 무방향 읽기 전용 UI와 데이터 품질 검증을 진행하고, 편집·순서 schema는 실제 필요 확인 뒤 여는 순서를 정했다.
- 세탁 기록, 알림, 위젯, 제한적 오프라인은 원본 사건 모델과 지속 가능한 입력 흐름이 정해진 뒤에만 착수하도록 조건부 단계로 분리했다.
- 통계 때문에 Outfit Preview를 일괄 생성하지 않고 숫자 우선·이미지 지연 로드·Replacement Line lazy query로 egress를 억제하는 원칙을 포함했다.
- Roadmap에 Phase 4 상세 계획 링크와 새 다음 작업 순서를 반영했다.

## Decisions - Phase 4 Maintenance & Insights 계획

- Phase 4 Core는 상세 통계, 관리 신호, Replacement Line 읽기까지로 독립 배포 가능하게 한다.
- Roadmap 후보 기능은 모두 구현해야 하는 고정 체크리스트가 아니다. 실제 사용 필요성이 확인되지 않은 Maintenance·Automation 항목은 근거를 남기고 보류할 수 있다.
- 통계 저장값을 새로 중복 저장하지 않고 기존 Wear Log와 relation에서 계산한다. 현재 규모에서는 클라이언트 계산으로 정확성을 먼저 검증하고, 실제 성능·egress 측정이 필요성을 보일 때만 RLS를 지키는 DB 집계로 옮긴다.
- Replacement Line의 연결 순서와 세탁 데이터 모델을 구현자가 추정하지 않는다. P4-0의 읽기 전용 조사와 J의 실제 사례 확인을 선행한다.

## Current Status - Phase 4 Maintenance & Insights 계획

상세 계획서 작성과 Roadmap 연결이 완료됐고 실제 코드·Supabase production 데이터·배포 상태는 변경하지 않았다. Phase 4는 P4-0 착수 전이며, 첫 작업은 production 데이터의 읽기 전용 기준선과 통계 후보 분포 확인이다.

## Next Steps - Phase 4 Maintenance & Insights 계획

- production의 Item·Outfit·Wear Log·Replacement Line 기준선과 누락 상태를 읽기 전용으로 확인한다.
- 기간 후보별 통계 결과를 비교해 기본 기간, 장기 미착용, 최근 반복 기준을 J와 확정한다.
- 확정된 기준으로 P4-1 통계 계산 계약을 구현한다.
